# Design Context

## Users

- **机构经营者本人**（每日全模块使用：排课、学生、费用、薪资、经营概览、审计）
- **1–2 名助理**（主要做录入：录课、录充值，少量学生信息维护；后续可能开放老师只读）
- 使用场景：办公室桌面浏览器，工作时长跨白天上班 + 夜晚对账两个时段
- 数据规模：单月约 50–80 名学生、500+ 节课、十余位老师，按月累积
- 都是熟练电脑用户但不是设计师/技术人员，要求是"高效、不出错、对账清楚"

## Brand Personality

**温暖 / 教育 / 用心**，三个词等权重。

- **温暖**：不是迪士尼式活泼，而是教育者对学生的关切。柔和、有温度，避免冰冷工业感。
- **教育**：传递"这是认真办学的机构"，不是连锁辅导班的廉价感。
- **用心**：每个细节都让使用者觉得"做这个产品的人想过我的痛点"。

## Aesthetic Direction

参考 **Stripe Dashboard 的数据严谨度**——清晰的数字、密集但不拥挤的表格、可信赖的财务视觉。

但要避开 Stripe 的冷峻——通过字体（人文衬线点睛）、暖偏中性色（米黄/暖灰，非纯蓝灰）、克制有温度的微互动注入人情味。

**避免**：

- 国产培训软件的多色块仪表盘（红黄蓝绿满屏跳）
- 纯灰阶冷峻 SaaS（Linear 极简但缺温度）
- Material You 的圆角填色塞满屏
- 模板化"学生头像 + 进度条 + emoji"的幼儿化教育产品视觉

**倾向**：

- 信息密度高，一屏看尽关键数据（参考 Stripe Balance / Payouts）
- 字体里有人文气息（标题点睛用衬线/有性格无衬线，正文用 HarmonyOS Sans SC 已足够）
- 中性色暖偏（neutral 向 hue 30–60 微移，不是纯灰）
- 强调色 1–2 个，常态克制；状态强调（异常/欠费）才加重

## Theme

**亮色 + 暗色双模式，可切换**。

- 默认跟随系统（`prefers-color-scheme`），允许在顶栏手动覆盖并 localStorage 持久化
- 亮色：白天办公场景，纸张感的暖白底，文字深棕灰而非纯黑
- 暗色：夜晚对账场景，深胡桃色背景，避免"科技感冷暗"

## Design Principles

1. **数字优先于装饰**：所有页面视觉重心是数字本身（金额、课时、余额）。装饰让位于数据可读性。
2. **温度藏在细节里**：通过字体、间距、状态文案传递关切感，而不是靠 emoji、插画、彩色 banner。
3. **对账场景至上**：每个视觉决策都问"这能让她在月底对账时少出一次错吗"。
4. **保留信息密度**：不为美观牺牲一屏可见信息量。Stripe 风格紧凑表格 > 过度留白卡片网格。
5. **暖中性，冷强调**：底色和文字偏暖，状态色（错误红、警告橙）保留必要冷峻度作为对比。

## Technical Constraints

- 纯 HTML + 原生 JavaScript + 单个 `styles.css`，**不引入框架/构建工具/新依赖**
- 运行环境：本地 Node.js + SQLite，单端口 5177
- 代码规模：`public/app.js` ~3000 行单文件，`public/styles.css` ~1800 行单文件
- 修改原则：扩展现有 design tokens 而非引入新系统；保留无构建特性
- UI 改造阶段不改 `src/server.js` 的 API 契约
- UI 改造阶段尽量不动 `public/app.js` 的数据流逻辑；如必须改 JS 结构，先确认
- 数字使用 `tabular-nums` 保证对齐
- 颜色只用于业务语义（欠费/异常/警告/盈利/关键 KPI），不做大面积装饰
