# 黎明教育课程管理系统

本地后台 MVP，端口 5177，`npm start` 启动。技术栈：Node.js 原生 http + node:sqlite + 纯静态前端（无框架、无构建）。

数据库：`data/liming-local.sqlite`。导入脚本：`scripts/import_workbook.py`。

## Design Context

### Users

- **机构经营者本人**（每日全模块使用：排课、学生、费用、薪资、经营概览、审计）
- **1–2 名助理**（主要做录入：录课、录充值，少量学生信息维护）
- 使用场景：办公室桌面浏览器，跨白天上班 + 夜晚对账两个时段
- 数据规模：单月约 50–80 名学生、500+ 节课、十余位老师

### Brand Personality

**温暖 / 教育 / 用心**，三个词等权重。

- **温暖**：不是迪士尼式活泼，而是教育者对学生的关切
- **教育**：传递"认真办学的机构"，不是连锁辅导班的廉价感
- **用心**：每个细节都让使用者觉得"做这个产品的人想过我的痛点"

### Aesthetic Direction

参考 **Stripe Dashboard 的数据严谨度**，但通过暖偏中性色和有性格的字体注入人情味。

避免：国产培训软件多色块仪表盘 / 纯灰阶冷峻 SaaS / Material You 圆角填色 / 幼儿化教育产品视觉。
倾向：高信息密度、人文气息字体、暖偏中性、强调色克制。

### Theme

亮色 + 暗色双模式，可切换。默认跟随系统，允许在顶栏手动覆盖并 localStorage 持久化。

### Design Principles

1. **数字优先于装饰**：视觉重心是数字本身（金额、课时、余额）
2. **温度藏在细节里**：靠字体、间距、文案传递关切感，不靠 emoji/插画/彩色 banner
3. **对账场景至上**：每个视觉决策都问"这能让她在月底对账时少出一次错吗"
4. **保留信息密度**：Stripe 风格紧凑表格 > 过度留白卡片网格
5. **暖中性，冷强调**：底色和文字偏暖，状态色保留必要冷峻度作为对比

完整设计上下文见 `.impeccable.md`。

## Technical Constraints

- 纯 HTML + 原生 JavaScript + 单个 `styles.css`，**不引入框架/构建工具/新依赖**
- `public/app.js` ~3000 行单文件，`public/styles.css` ~1800 行单文件
- 修改 UI 时扩展现有 design tokens，不引入新系统
- 不改 `src/server.js` 的 API 契约（除非明确需求）
- 数字使用 `tabular-nums` 保证对齐；颜色只用于业务语义
