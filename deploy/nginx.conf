server {
  listen 80;
  server_name _;

  client_max_body_size 50m;

  location / {
    proxy_pass http://app:5177;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

# HTTPS is enabled after certificates are placed in deploy/certs/fullchain.pem
# and deploy/certs/privkey.pem. Then replace the server block above with this:
#
# server {
#   listen 80;
#   server_name course.example.com;
#   return 301 https://$host$request_uri;
# }
#
# server {
#   listen 443 ssl http2;
#   server_name course.example.com;
#
#   ssl_certificate /etc/nginx/certs/fullchain.pem;
#   ssl_certificate_key /etc/nginx/certs/privkey.pem;
#   client_max_body_size 50m;
#
#   location / {
#     proxy_pass http://app:5177;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
