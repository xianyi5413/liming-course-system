# 黎明教育课程管理系统

## 2026-07-17（排课编辑修复与状态列顺序调整）

1. **排课默认视觉保持统一**：授课老师、状态、时间、教室、年级和科目在排课态默认继续显示浏览态的普通文字或实体标签，不常驻显示原生选择框、白色背景、边框、箭头或输入框外壳。
2. **六个字段可整格编辑**：六类字段的 `td` 统一使用 `data-lesson-edit-trigger`、`data-lesson-id` 和 `data-field`，因此点击单元格空白、文字或标签都能打开同一页面级选择浮层；支持键盘 Tab 聚焦、Enter/Space 打开、Esc 返回焦点。课程表委托在打开时阻止本次点击继续冒泡到通用选择器的外部关闭处理，避免浮层刚创建即关闭；`tbody` 局部重绘后不会失效，只读账号保持不可写。
3. **单实例编辑浮层**：点击后才在当前单元格创建临时选择锚点，并复用挂载到 `body` 的统一搜索浮层。老师、状态、时间、教室、年级和科目的候选及时间/教室冲突均按点击生成；搜索、中文输入、方向键、Enter、Esc、外部关闭、已选高亮和空结果提示继续可用。
4. **冲突语义收口**：课程总表的时间和教室在浏览态、排课态都只以红色文字表示冲突；候选浮层仍区分可选/冲突，服务端校验与 `allow_conflicts` 流程未变。
5. **表格顺序统一**：课程总表、批量复制预览以及家长群/老师课程截图详细模式统一为“授课老师 → 日期 → 星期 → 时间 → 教室 → 状态 → 年级 → 科目 → 学生”。页面预览、Canvas 复制和下载 PNG 共用同一列定义；简洁截图不增加星期字段。
6. **性能边界保持**：进入/退出排课仍只替换 `tbody`，不请求 bootstrap 或课程范围；筛选、统计、工具栏、表头、滚动位置、选中项、草稿、焦点、请求缓存和候选按需计算策略保持不变，也不为每行创建常驻选择器 wrapper。
7. **版本**：后端诊断版本为 `2026.07.17-schedule-editing-status-order-fix`，`app.js` 与 `styles.css` 静态资源参数为 `20260717-schedule-editing-status-order-fix`。

## 2026-07-16（日期控件、截图卡片与基础数据配色优化）

1. **单日期控件统一去图标**
   课程日期、新增课程、冲突处理、充值、开销、学生/教师/员工档案和学生年级阶段等全部单日期字段均通过 `data-date-kind="single"` 接入同一增强器，不再绘制自定义日历 SVG，也不为图标预留右侧空白；字段继续显示和接受 `YYYY-MM-DD`，支持点击日期浮层、手动键入、键盘、严格日期校验、保存、草稿取消及只读权限。
2. **日期范围继续独立**
   课程总表、矩阵课表、课程截图、学生查询、经营概览和操作日志等范围筛选继续使用 `data-date-kind="range"`、右侧日历图标和双日期状态，不受单日期样式影响。双月切换、年份切换、起止选择、范围高亮、hover 预览、快捷日期计算和筛选刷新口径均不变。
3. **快捷日期独立纵向滚动**
   日期范围面板左侧快捷列表不再参与面板固有高度计算，列表高度由右侧双月日历约束；昨日、今日、本月、本周、上月、上周、近三个月、近一年、去年和今年在左栏内部使用滚轮、触控板或触摸纵向滚动，右侧日历和整个面板不随之滚动，分隔线与轻量滚动条保留。
4. **日期面板响应式收口**
   宽中屏继续并排显示左侧快捷项与右侧双月日历；窄屏压缩快捷栏并将双月上下排列，面板宽度限制在视口内。面板局部重绘会保留快捷栏滚动位置，页面切换或局部替换前仍回收日期面板和临时浮层。
5. **简洁截图姓名不拆行**
   家长个人、班级、班级合并和老师发送对象共用的简洁卡片将每个学生标签视为完整单元，姓名使用 `white-space: nowrap`，不拆字、不竖排、不省略；空间不足时整个标签自然换到下一行，截图预览、复制、下载和完成记录逻辑不变。
6. **简洁卡片按最小宽度自适应**
   简洁模式外层网格使用约 220px 的自适应最小卡片宽度，宽屏自然显示较多列，中屏自动减少列数，窄屏降为少列或单列，不再追求固定每行数量，也不产生页面级横向滚动。
7. **基础数据配色控件完整显示**
   科目、课程状态和学生年级等共用配色行改为“弹性标签预览 + 两个不收缩颜色组”，外层按约 260px 最小宽度自动调整列数；“背景”和“文字”文字及颜色框不会再被卡片边缘裁切。颜色输入继续单次事件委托实时更新预览，保存配色、恢复默认、settings 结构和稳定默认色不变。
8. **业务与性能边界**
   本轮只调整共用前端组件和布局，不新增接口、数据库表、第三方依赖、动画、过渡或额外 bootstrap 请求；课程、费用、余额、薪资、冲突、权限、导入导出和操作日志口径保持不变。
9. **自检与浏览器回归**
   前后端 JavaScript 语法、CSS 大括号、`git diff --check` 和学生余额用例通过；隔离 SQLite 数据副本的 `/api/version`、登录与 `/api/auth/me.app_version` 一致。Edge 实页回归 13/13 通过，覆盖 119 行排课日期、188 个跨页面单日期控件、冲突日期草稿/星期、范围图标与快捷滚动、520px 窄屏、家长 38 张和老师 7 张简洁卡片、双方复制/下载、配色 1440/900/500px 三档、实时预览/保存/恢复默认及浮层回收；控制台、页面异常和网络失败均为 0。
10. **版本**
   后端诊断版本为 `2026.07.16-date-picker-notice-color-polish`，`app.js` 与 `styles.css` 静态资源参数同步为 `20260716-date-picker-notice-color-polish`。

## 历史：2026-07-16（趋势图、单日期、课程状态与页面布局优化）

1. **首页课程趋势横向铺满**
   课程趋势继续独占首页第三行，保留原日期范围和 Dashboard 数据接口；图表改为复用单一 ECharts 实例，`grid` 使用紧凑左右边距和 `containLabel`，折线从绘图区左侧延伸到右侧。容器宽度始终为 100%，通过单个 `ResizeObserver` 响应侧栏收折、窗口变化和响应式换行，不重复请求、不叠加图表实例或 resize 监听。
2. **冲突编辑统一单日期控件**
   `课程总表 -> 冲突为 N -> 处理` 的日期改回与新增课程/排课一致的单日期控件，复用相同高度、边框、圆角、字体、内边距、日历图标和日期浮层。日期输入通过 `data-date-kind="single"` 与 `data-date-kind="range"` 明确分流，不再被误判成日期范围，也不会被全局增强器错误接管成无法编辑的控件；修改后仍写入弹窗草稿、同步星期并按新日期重算时间/教室候选，取消、Esc 和关闭不提交。
3. **课程通知状态与简洁学生布局**
   家长群和老师课程截图的详细预览、Canvas 复制及下载统一读取规范化 `status`，`已上` 不再显示成旧字段值“上课”；“只选择”开关同步更名为“只选择‘已上’”，后端过滤也使用 `deriveStatus(row) === '已上'`。简洁模式的学生标签使用稳定三列 Grid，每行最多 3 人，完整姓名允许居中换行，极窄屏才降为 2 列；家长个人/班级/合并发送和老师发送共用同一渲染函数。
4. **学生年级阶段改为独立弹窗**
   学生档案不再向表格中插入展开行；点击学员行的非控件区域会打开宽屏双列、窄屏单列的年级阶段弹窗，展示初一至高三和已毕业七个阶段，其中已毕业只显示起始日期。标题栏和底部操作区固定，只有正文滚动；保存沿用 `/api/student-grade-stages`、原权限、审计和操作日志，事务内只更新当前学员发生变化的阶段，前端仅更新该学员姓名/当前年级单元格。保存失败保留全部弹窗输入，关闭、取消、遮罩和 Esc 会回收日期浮层和弹窗草稿；只读账号可查看但不能保存，批量修改入口保持不变。
5. **基础数据与账号权限布局**
   基础数据四个外层模块固定按“教室/科目、常用时间/课程状态”两列排列，中等宽度仍保留两列，窄屏才改一列；状态、科目内部配色网格规则不变，年级配色卡继续独占整行。账号管理顶部按钮统一左对齐并允许左起换行；账号表格使用 `colgroup + table-layout: fixed + border-collapse: separate`，外框只由滚动容器绘制，单元格只绘制内部右/下分隔线，重置密码输入与按钮由单元格内层 Flex 排列，状态保持紧凑标签，横向滚动只发生在表格容器内。
6. **事件、缓存与局部更新**
   学员阶段弹窗和冲突编辑使用稳定事件委托；局部重绘前统一关闭自定义选择器、单日期和日期范围浮层。侧栏收折只局部更新布局类和按钮属性，并对同一 ECharts 实例执行 `resize()`，不再触发首页整页重绘；Dashboard DOM 真正替换前才回收旧图表和监听。截图状态复用同一取值函数，基础数据只调整 CSS 网格，账号表格不增加接口。课程、权限、费用、薪资、导入导出、操作日志、冲突校验、`allow_conflicts` 和派生缓存失效口径均保持不变。
7. **自检与人工回归结果**
   已通过 `node --check public/app.js`、`node --check src/server.js`、CSS 大括号平衡、`git diff --check` 和 `npm.cmd run test:student-balances`。隔离 SQLite 副本服务的 `/api/version` 与 `boss / 123456` 登录通过；删除最后一条引用课程后，教室、科目、时间和状态四类候选均从新 bootstrap 消失。Edge 实页回归 47/47 通过，覆盖首页宽/中/窄屏和侧栏同实例 resize、课程表两态、冲突日期/学生浮层/取消/保存、家长与老师详细/简洁/复制/下载、阶段弹窗可写与只读、基础数据两列/单列、账号按钮/边框/状态/容器滚动；控制台和网络错误均为 0，结束时无残留浮层或弹窗。
8. **版本**
   后端诊断版本为 `2026.07.16-dashboard-date-notice-layout-polish`，`app.js` 与 `styles.css` 静态资源参数同步为 `20260716-dashboard-date-notice-layout-polish`。

## 历史：2026-07-15（首页、课程表、冲突编辑与基础数据优化）

1. **首页三行响应式布局**
   常用功能继续独占第一行；第二行宽屏按“正在上的课程、待办事项、课程趋势”三等分排列，中屏调整为两列加整行趋势，窄屏依次单列；人数统计独占第三行，学员与老师圆环图宽屏并排、窄屏上下排列。全部复用原 Dashboard 响应，不增加接口和重复渲染。
2. **课程总表两种模式统一**
   浏览态与排课态共享表头高度、普通行高度、单元格内边距、列宽和垂直居中规则。老师、日期、状态、星期、时间、教室、年级、科目及复选框统一居中；学生标签保持单行完整，备注继续左对齐换行。排课字段只在点击后显示既有搜索浮层，冲突时间和教室只保留红色文字。
3. **冲突编辑日期与学生浮层**
   冲突日期改为不被全局只读日期增强器接管的原生日期控件，日期变化立即写回弹窗草稿、同步星期并局部重建基于新日期的时间/教室冲突候选。学生选择复用排课学生组件，将搜索、已选标签、候选、新增输入和确认/取消合并到同一页面级浮层；确认只更新弹窗草稿，最终仍由“保存修改”和服务端冲突校验提交。
4. **课程截图标签对齐**
   家长群与老师课程截图的预览表格、简洁卡片和 Canvas 输出统一年级/科目标签的高度、基线、内边距、间距与纵向定位；窄屏由标签组整体自然换行，复制与下载继续复用同一 Canvas 绘制路径。
5. **基础数据仅取现存课程**
   教室、科目、常用时间和课程状态只由 `lessons` 表中当前仍存在的记录汇总，空值过滤、去重、稳定排序和时间规范化共用一套流程。系统默认、settings 自定义值、已删除课程快照和孤立配色键都不再补回候选；基础数据候选、配色列表和新增课程候选使用同一当前集合，重新进入页面时通过最新 bootstrap 自动失效旧集合。
6. **角色页、性能与版本**
   删除角色管理表格上方重复标题说明，保留切换标签、表格、只读状态和权限编辑。继续使用页面级事件委托、局部弹窗重绘和浮层回收，不新增动画。版本更新为 `2026.07.15-dashboard-course-table-conflict-base-data`，静态资源参数同步为 `20260715-dashboard-course-table-conflict-base-data`。

## 2026-07-14（冲突编辑、统一选择器与操作日志优化）

1. **冲突课程二级编辑修复**
   `课程总表 -> 冲突为 N -> 处理` 的老师、时间、教室、年级、科目、状态和学生选择浮层统一挂载到 `body`，层级高于父冲突弹窗和二级编辑弹窗；修复学生隐藏值查询选择器错误。Esc、外部点击、搜索、方向键、Enter、取消返回父弹窗及保存后的课程/冲突局部刷新继续保留。
2. **排课样式清理与统一选择器**
   教室和时间冲突都只保留红色文字语义，不再显示底部横线或左侧竖线；排课备注使用 `resize: none`。老师、状态、时间、教室、年级、科目和学生默认只显示内容，点击后复用页面级可搜索浮层；学生继续使用确认/取消草稿、多选和仅向当前课程新增姓名的语义。
3. **基础数据与标签配色同源**
   教室、科目、常用时间、课程状态和学生年级统一执行去空、去重、首尾空格清理、稳定排序，时间规范为 `HH:mm-HH:mm`。配色只遍历系统当前允许值、当前自定义值和真实历史课程值，不再把旧配色对象中的孤立键反向注入候选；缺失配色继续使用稳定哈希默认色。
4. **首页与操作日志**
   首页主内容调整为待办事项、课程趋势、人数统计、正在上的课程四个整行区块，两个人数环形图宽屏并排、窄屏上下排列，且只复用原 Dashboard 响应。操作日志删除详情列和 JSON 展开区，将目标、范围、前后变化、数量、日期、金额、文件名及成功/失败摘要整合进可换行的操作内容列。
5. **安全、性能与版本**
   操作日志在写入和输出阶段继续屏蔽密码、token、Cookie、authorization、session 等字段，并新增手机号脱敏；日志失败仍不阻断业务。浮层搜索只修改当前候选 DOM，局部替换前主动回收页面级菜单；基础数据保存直接消费现有响应局部重绘，不再追加一次 bootstrap 加载。版本更新为 `2026.07.14-conflict-picker-audit-layout`，静态资源参数为 `20260714-conflict-picker-audit-layout`。

## 2026-07-14（首页统计、统一筛选与排课候选样式）

1. **首页人数统计重新布局**
   学员与教师两张人数环形图移至首页右上方，宽屏横向并排并与待办、趋势模块对齐；中窄屏按可用宽度自然换行，图表、总数和图例保持完整。调整只复用现有 Dashboard 响应和节点，不新增接口请求、动画或延迟重绘。
2. **排课模式视觉修正**
   排课状态与浏览状态继续复用课程状态标签渲染及基础数据配色，不再附加方形底框；教室普通值恢复为纯文字与下拉箭头，冲突仅保留轻量红色语义且没有左侧竖线；学生标签在单元格内垂直居中，点击学生区域后才打开原有选择浮层。
3. **课程总表统一可搜索筛选**
   老师、学生、教室、状态、年级、科目统一复用同一可搜索浮层。原筛选框保持位置和尺寸，浮层统一提供搜索、清空、结果数、完整候选、空状态、中文输入法、方向键、Enter、Esc、已选高亮及外部关闭；老师和学生保留多选，其余字段保留单选。
4. **时间与教室候选统一**
   排课模式和新增课程中的时间、教室候选统一为“左侧原始值 + 右侧单个可选/冲突提示”，删除值后的括号状态及重复提示。冲突候选在悬停、聚焦、选中状态下继续保留红色语义，回填值仅显示原始时间或教室名称；排序、冲突算法、保存校验及 `allow_conflicts` 逻辑不变。
5. **局部刷新与事件复用**
   搜索只重排当前浮层已有候选节点，不请求 bootstrap、不重绘课程表、顶部栏或导航；筛选继续使用稳定控件监听与既有课程局部刷新入口，局部区域替换前会回收端口化浮层，避免孤立节点和重复事件。
6. **版本与静态资源**
   后端诊断版本为 `2026.07.14-dashboard-filter-option-polish`，前端入口使用 `/app.js?v=20260714-dashboard-filter-option-polish` 与 `/styles.css?v=20260714-dashboard-filter-option-polish`。

## 2026-07-13（首页精简、学生筛选与安全操作日志）

1. **首页首行完整移除**
   首页不再渲染月课程费、应收、今日完成、请假和月消费统计卡，也不保留空白卡片容器；常用功能、待办事项、课程趋势、正在上的课程和人数统计顺序上移并保持原有数据接口。
2. **课程总表显示收口**
   课程状态、年级和科目继续使用统一紧凑标签；排课态时间与教室改为无底色、无胶囊背景的纯文本选择器，冲突教室仅保留窄红色提示。备注正文左对齐并自然换行，备注表头保持居中；日期范围为完整 ISO 日期预留固定宽度并在中窄屏提前换行。
3. **学生筛选根因修复**
   可搜索多选控件维护独立完整候选池，每次搜索只把过滤后的候选节点挂入当前菜单；结果数、空结果提示和候选 DOM 来自同一过滤结果。中文输入法组合输入、清空搜索、键盘操作及已选值均保持稳定，并补充 `[hidden]` 的组件级强制隐藏规则。
4. **学生栏目与实体标签**
   学生费用汇总、充值、期初余额、学生单价和学生档案移除按年级写入的整行背景色；学生、年级、科目和状态复用统一标签尺寸与配色，家长/老师课程通知的页面预览及复制、下载图片也绘制同一类圆角标签。负数、告警、冲突、禁用、选中及悬停语义不受影响。
5. **外观选择器与操作日志**
   自定义选择器只保留伪元素生成的一枚箭头，修复外观设置主题/配色的双箭头。操作日志新增登录成功、登录失败和退出记录，保存结果、目标、客户端 IP、浏览器及脱敏详情；密码、令牌、Cookie、授权头和会话标识不会写入日志。日志写入异常只告警、不阻断业务，日志页支持真实类型、结果、时间、人员、账号、内容筛选与行内详情查看。
6. **版本与检查**
   后端版本为 `2026.07.13-dashboard-search-audit-polish`，入口静态资源查询参数同步更新；通过前后端 JavaScript 语法检查、Git 差异空白检查、数据库迁移启动检查和学生余额回归检查。

## 2026-07-13（导航收折、排课学生入口、标签配色与无动效）

1. 导航一级菜单可独立反复展开、折叠；全展开/全折叠与单组状态都会持久化，用户主动折叠当前组后不会被页面状态强制展开。
2. 课程总表学生列以完整单行标签计算动态宽度；排课态的学生单元格仅显示标签，点击整个单元格后才打开含搜索、多选、新增、统计、取消与确认的草稿浮层。确认只保存当前课程行，取消不写入。
3. 课程状态、学生年级和科目采用标签色彩呈现；状态不会再作为课程整行底色。基础数据页将课程状态和科目字典与各自配色放在同一卡片内，未配置科目也会稳定生成可读的默认色。
4. 所有筛选组合框均支持候选项检索，并在停止输入后自动刷新结果；课程日期快捷方式新增“明日”。金额输入隐藏数字微调按钮，普通数字字段保持原生行为；全站关闭动画与过渡效果。
5. 后端版本为 `2026.07.13-nav-filter-badges-no-motion`，静态资源查询参数同步更新。

面向单店教培机构的本地化教务管理系统，用于“黎明教育”的学生、教师、课程、课时、充值、学生费用、教师薪资、月度数据、数据导入、对账和导出管理。系统以 SQLite 中的结构化数据作为运行时主数据源，源 Excel 主要用于历史导入、对账和结构参考。

## 项目简介

系统当前覆盖的核心工作包括：

- 学生档案、学生费用明细、期初余额、充值流水和余额结转；
- 教师档案、教师课时费、交通补贴和月度薪资汇总；
- 月度课程总表、课程状态、冲突检查和课程通知；
- 标准价格、学生单价规则、单节课费用覆盖、教师薪资规则和班级名管理；
- 源 Excel 导入、源头对账、内部规则校验和操作日志；
- 月度核心 Excel、教师薪资、经营概览等导出。

## 当前前端交互与页面规则

### 排课、课程总表与冲突处理

- 课程总表字段顺序为“授课老师 → 日期 → 星期 → 时间 → 教室 → 状态 → 年级 → 科目 → 学生”；浏览态与排课态共用表头、行高、内边距、列宽和垂直对齐规则。
- 排课态的老师、状态、时间、教室、年级和科目默认只显示浏览态同款文字或标签，不使用常驻原生 `select`、边框或箭头；点击后才创建临时锚点并打开页面级统一候选浮层。时间、教室冲突只显示红色文字。
- 课程日期和新增课程日期属于单日期字段：不显示日历图标，但仍可点击统一日期浮层或手动键入 `YYYY-MM-DD`。冲突处理日期变更会先写入二级弹窗草稿、同步星期，并基于新日期局部重算时间/教室候选；最终保存继续执行服务端冲突校验，取消、Esc 或关闭不提交草稿。
- 单日期和日期范围分别使用 `data-date-kind="single"` 与 `data-date-kind="range"`，具有独立渲染和样式作用域，不通过全局 `input[type=date]` 规则互相覆盖。

### 日期范围选择器与共用前端组件

- 日期范围字段继续显示右侧日历图标并维护开始、结束两个值；双月、年份切换、范围高亮、hover 预览、快捷日期和页面筛选刷新沿用原逻辑。
- 左侧快捷日期在自身区域内独立纵向滚动，面板高度主要由右侧双月日历决定；中窄屏会调整左右栏宽度和双月排列，面板宽度始终受视口限制。
- 单日期、日期范围、搜索选择器和学生选择浮层均复用页面级组件；局部重绘前统一回收旧浮层。日期范围不注册额外滚轮监听，配色实时预览只注册一次内容区事件委托。

### 家长群与老师课程截图

- 家长群个人发送、班级发送、班级合并发送和老师发送的简洁卡片复用同一布局：学生、年级、科目依次显示，完成状态和课程节数位于下方。
- 单个学生姓名标签永不拆行、截断或缩小；多个完整标签在空间不足时整体换行。外层网格以约 220px 最小卡片宽度自动增减列数，不固定每行卡片数量。
- 详细模式的页面预览、Canvas 复制和下载 PNG 共用“授课老师 → 日期 → 星期 → 时间 → 教室 → 状态 → 年级 → 科目 → 学生”列定义；简洁模式维持原卡片结构。
- 详细/简洁预览、复制文案、复制截图、下载截图和完成记录继续使用既有课程通知数据与 Canvas 路径，窗口尺寸变化只触发布局计算，不重新请求数据。

### 基础数据、配色与响应式布局

- 教室、科目、常用时间和课程状态候选仍只来自当前未删除课程并共用去空、去重和时间规范化流程；孤立配色键不会反向补回候选。
- 标签配色项目内部使用弹性预览区和固定宽度的背景/文字颜色组，外层以约 260px 最小宽度自适应列数。空间不足时优先减少列数，保证两个颜色框及文字完整可点击，保存和恢复默认按钮稳定位于网格下方。
- 宽屏尽量利用多列，中屏自动减少，窄屏单列；页面本身不得因日期面板、简洁卡片或配色项目产生横向溢出。

### 当前自检口径

- 静态检查包括前后端 JavaScript 语法、CSS 大括号平衡和 `git diff --check`；业务回归包括学生余额测试。
- 隔离服务检查 `/api/version`、登录和 `/api/auth/me.app_version`；浏览器回归覆盖所有单日期场景、范围图标与快捷滚动、家长/老师简洁卡片及复制下载、基础数据三档宽度、配色实时预览/保存/恢复默认、控制台错误和残留浮层。

## 近期功能迭代记录

### 2026-07-15（首页、课程表、冲突编辑与基础数据优化）

1. 首页改为常用功能、三列业务区、人数统计三行结构，并补齐宽屏、中屏和窄屏断点。
2. 课程总表浏览态与排课态统一表头、行高、内边距、列宽、对齐和学生/备注展示规则。
3. 修复冲突编辑日期被全局只读日期组件接管的问题；日期写回草稿后基于新日期局部重算候选和冲突提示。
4. 冲突学生选择与新增学生合并为一个页面级浮层，搜索、多选、草稿删除、新增、取消和确认复用排课学生组件。
5. 家长群和老师截图的预览、简洁模式及 Canvas 标签基线统一。
6. 基础数据四类候选、配色和新增课程候选只读取当前未删除课程；删除最后引用后，下一次 bootstrap 不再返回该值。
7. 删除角色管理重复说明；保留权限和账号绑定逻辑。页面继续局部刷新并在局部替换前回收浮层。
8. 自检覆盖 JavaScript 语法、CSS 大括号、diff、余额用例、隔离服务版本/登录、三档响应式、课程表切换、冲突草稿、截图和基础数据删除失效。

### 2026-07-14（冲突编辑、统一选择器与操作日志优化）

1. 修复冲突课程二级弹窗选择器层级、裁剪和学生隐藏值定位问题，二级弹窗关闭后父冲突列表保持打开，保存仍只刷新课程表、冲突数量和冲突弹窗。
2. 清除排课教室冲突横线和备注原生 resize；老师、状态、时间、教室、年级、科目和学生统一为默认内容展示、点击后打开搜索浮层。
3. 基础数据候选和状态/科目/年级配色共用规范化集合，剔除仅存在于旧配色设置、没有当前或历史引用的条目。
4. 首页课程趋势与人数统计改为上下整行布局；两个人数环形图保持宽屏并排、窄屏单列。
5. 操作日志删除详情列及 JSON 展开代码，结构化详情经脱敏后转为自然语言并入操作内容；查询、分页、结果标签和每页数量保持不变。
6. 搜索只更新当前菜单 DOM，浮层统一端口化和清理；基础数据增删不再重复调用全局加载。后端版本为 `2026.07.14-conflict-picker-audit-layout`，静态资源参数为 `20260714-conflict-picker-audit-layout`。

### 2026-07-14（首页右上统计、统一筛选与候选去重）

1. 学员、教师人数环形图移动到首页右上侧，宽屏两图并排，中窄屏按断点自然换行；缩小图形但完整保留总数、状态图例和既有统计口径，仍只消费原 Dashboard 数据。
2. 排课态课程状态复用浏览态标签函数和基础数据配色；教室移除多余左竖线与突兀底框，学生标签在单元格中垂直居中，学生选择浮层继续按点击进入。
3. 老师、学生、教室、状态、年级、科目筛选统一为可搜索浮层，支持完整候选 DOM 过滤、结果数与空状态同步、中文输入、清空、方向键、Enter、Esc、外部关闭和已选高亮；多选、单选业务语义保持原样。
4. 时间和教室候选统一为原值加一个右侧低强调状态，去除括号状态和重复标签；冲突候选保持红色交互语义，选中回填只显示原值，原排序、冲突判断和保存规则未改。
5. 候选搜索只更新当前浮层并复用稳定事件绑定；课程筛选仍走局部区域刷新，首页没有重复请求或重复图表渲染，全站无动画约束保持不变。
6. 后端版本为 `2026.07.14-dashboard-filter-option-polish`，静态资源参数同步为 `20260714-dashboard-filter-option-polish`。

### 2026-07-13（首页首行删除、筛选根因修复与操作日志完善）

1. 首页完整删除月课程费、月应收、今日已上、今日请假和月消耗统计行及其容器，常用功能、待办、课程趋势和正在上的课程直接上移。
2. 课程总表状态统一为紧凑圆角标签，备注正文左对齐并自然换行；排课时间和教室恢复为无背景纯文本选择，日期范围完整显示并在中等宽度提前换行。
3. 学生搜索改为“稳定候选池 + 过滤结果 DOM”单一数据流，0 项时旧候选会从菜单节点中移除；中文输入、清空、已选值、键盘和重复开关保持稳定。
4. 学生费用汇总、费用明细、充值、期初余额、学生查询、学生单价、班级和学生档案不再使用年级整行底色；状态、学生、年级和科目通过共享圆角标签区分，通知预览和实际复制/下载的 Canvas 图片同步绘制标签。
5. 外观主题和配色选择器只渲染一枚自定义箭头。操作日志新增认证成功/失败分类、退出、月份切换、排课开关、通知生成/复制/下载、外观修改，并补强课程变更、导出、备份、对账和账号绑定的结构化详情及失败结果。
6. 后端版本与静态资源版本统一为 `2026.07.13-dashboard-search-audit-polish`。

### 2026-07-13（排课标签、课程筛选与首页精简）

1. **排课学生浮层紧凑化**
   已选学生区仅在存在草稿学生时渲染，占用实际标签高度；清空后立即移除该区域，不保留空白行。候选区稳定显示约五行并独立滚动，学生增删才重新计算浮层位置，候选列表滚动不会影响高度。
2. **排课实体标签**
   排课状态、年级、科目和教室选择器统一显示紧凑圆角标签；状态、年级和科目继续读取既有配色，教室使用中性配色，冲突教室保留红色提示。所有保存值仍为原始文本。
3. **课程总表可搜索筛选**
   学生、老师、教室、状态、年级和科目均使用可搜索候选控件；学生和老师支持既有多选语义，其他字段维持单选。控件支持中文输入、清空、结果数、无结果、键盘、Esc 与外部关闭，筛选只刷新课程相关区域。
4. **课程筛选布局与首页**
   课程总表筛选区收敛为日期/老师/学生、教室/状态/年级/科目、搜索/统计三行布局，并在窄屏自动换行。首页已移除“机构数据”标题说明行，保留并上移所有机构统计卡。
5. **版本**
   后端诊断版本为 `2026.07.13-course-filter-popover-dashboard`，入口静态资源参数同步更新。

### 2026-07-13（统一标签、学生浮层、可搜索筛选与独立滚动）

1. **统一标签与配色**
   学生、年级、科目和课程状态统一为紧凑单行圆角标签，复用基础数据中的背景色和文字色；标签不会竖排、换行或被拉伸成大块背景，数据库接口和 Excel 导出仍保留纯文本。
2. **排课学生选择浮层**
   已选学生以紧凑标签换行展示，候选区固定显示约五行（210px）且独立滚动；搜索、新增学生、取消和确认区保持可见。浮层打开时只计算一次方向与可用高度，候选列表滚动不会触发重新定位。
3. **统一可搜索筛选**
   学生、老师、教室、状态、年级和科目筛选复用可搜索选择控件，支持中文输入法、清空搜索、结果数、无结果、键盘上下键、Esc 与点击外部关闭。未筛选时明确显示“全部……”，候选标签按实体类型渲染。
4. **页面滚动与日期范围**
   应用固定在视口高度，桌面端侧栏导航与主内容独立纵向滚动，不会因展开菜单撑高 body。日期范围控件为开始日期、结束日期、箭头与日历图标保留稳定宽度；窄屏允许控件整项换行，不截断日期。
5. **版本**
   后端诊断版本为 `2026.07.13-badges-popover-filter-scroll-date`，入口静态资源参数同步更新。

### 2026-07-13（学生标签、配色配置与导航全折叠）

1. **排课学生搜索修复**
   排课行学生浮层改为按当前浮层实例维护候选与草稿；搜索只重绘候选列表，支持中文输入法组合事件，已选学生不会因筛选或清空搜索而丢失，统计会同步更新，Enter 不会提交课程行。
2. **课程学生标签与局部移除**
   浏览态和排课态均按学生独立标签完整显示，标签可自然换行。排课态点击学生标签上的移除按钮只更新当前课程行的学生草稿并沿用既有局部保存/冲突检查机制，不会删除学生档案或重建课程页。
3. **统一课程状态与学生年级配色**
   课程状态改为可配置的彩色标签，待上课程取消整行底色；学生标签按学生档案当前年级优先、课程年级兜底着色。新增状态与无法识别年级使用中性灰，保存值仍是原始状态文本。
4. **基础数据配色配置**
   `设置 -> 基础数据` 新增“课程状态配色”和“学生年级配色”卡片，可保存背景色/文字色并恢复默认。配色复用既有 settings 持久化、基础数据写权限与操作日志机制，旧数据库在缺少配置时自动使用默认色。
5. **费用明细与导航**
   仅收紧学生费用明细表的表头、普通行、状态标签与复选框间距，备注多行仍可自然增高。导航总开关现在覆盖仅有一个二级页的首页和经营总览；“全部折叠”保存为显式状态，当前页不会被渲染逻辑强制重新展开。
6. **版本与静态资源**
   后端诊断版本为 `2026.07.13-student-badges-colors-nav-collapse`，前端入口使用 `/app.js?v=20260713-student-badges-colors-nav-collapse` 与 `/styles.css?v=20260713-student-badges-colors-nav-collapse`。

### 2026-07-13（导航总开关、学生列完整显示与切页性能）

1. **导航总开关**
   左侧品牌区新增“展开全部菜单 / 折叠全部菜单”按钮。它只处理当前账号有权限且具有可见二级页面的菜单组；单个菜单仍可独立展开或收起。展开集合沿用 `liming:nav-expanded-groups` 保存，读取时校验 JSON、去除无权限组，并在总开关与单组操作后立即同步图标和语义标签。
2. **课程总表学生列**
   学生列按当前筛选结果中最长的完整学生名单，使用 Canvas `measureText()` 结合单元格内边距和排课态下拉箭头空间计算统一宽度。表头、浏览行与排课编辑行共用 CSS 变量和 `colgroup`；筛选、局部 `tbody` 刷新及学生确认修改后都会重新计算。学生姓名不再以省略号或“等 X 人”截断，超宽内容仅在课程表容器内横向滚动。
3. **导航切页与数据加载性能**
   点击二级导航会立即更新选中态、页面标题和轻量骨架，数据随后局部填充。稳定 GET 请求按当前账号角色和完整 URL 使用 15 秒会话内缓存并复用进行中的 Promise；任意成功写操作会保守清空读取缓存，避免显示旧数据。教师/学生候选、账号与角色、员工与薪资考勤等相互独立请求已改为 `Promise.all()` 并行，旧切页响应继续由加载序号忽略。
4. **维护与版本**
   保留现有课程局部 `tbody` 刷新、冲突检查、学生选择浮层、权限、导入导出、备份和操作日志逻辑，未删除存在兼容风险的历史分支。后端诊断版本为 `2026.07.13-nav-toggle-student-width-performance`，前端资源版本同步为 `20260713-nav-toggle-student-width-performance`。

### 2026-07-13（导航事件委托与保守维护）

1. **导航失效修复**
   左侧导航改为在稳定的 `#nav` 根节点只绑定一次点击事件。一级菜单局部重绘后不再丢失监听；多个菜单组同时展开时，后续菜单仍可展开、折叠并进入二级页面。
2. **展开状态安全恢复**
   `liming:nav-expanded-groups` 读取时校验 JSON 和菜单 key；渲染时按当前权限过滤不存在的组，并自动保留当前页面所属组，异常存储内容会回退为安全状态。
3. **导航布局与临时层清理**
   导航按钮使用唯一 `data-nav-group` / `data-view`，组容器保持自然文档流和最小宽度，不覆盖后续菜单。现有弹窗、浮层、下拉菜单均继续通过关闭路径回收临时 DOM 与交互状态。
4. **代码与性能维护**
   保留课程 `tbody` 局部更新、课程候选缓存、冲突/学生浮层的页面级事件管理；未删除任何费用、薪资、权限、导入导出、备份、审计或历史兼容逻辑。

### 2026-07-12（排课浮层稳定性、候选来源与导航交互）

1. **排课学生选择浮层稳定化**
   学生浮层在打开时一次性确定向上/向下方向与候选列表高度；页面滚动仅更新浮层坐标，候选列表自身滚动不会重新定位或重算高度。外层不再纵向滚动，搜索、统计、新增学生与取消/确认按钮持续可见。
2. **课程总表交互性能整理**
   开始/结束排课继续仅替换课程 `tbody`，复用当前课程范围、筛选区和统计卡；编辑候选按页面级数据复用并按当前字段懒计算，避免进入排课时重新请求完整 bootstrap。
3. **新增课程弹窗数据复用**
   弹窗沿用已加载的在读学生、教师、课程候选与当前课程范围；打开和关闭仅更新弹窗区域，不重建筛选区、统计卡或课程表。冲突计算只在相关字段改变时刷新，并保留中文输入法、防抖序号与关闭时的请求失效逻辑。
4. **基础数据候选纠正**
   基础数据页将“自定义值”与“历史使用 / 当前候选”明确分开；后者只来自真实课程记录。教室、科目、状态会规范空白去重，时间统一为 `HH:mm-HH:mm` 后去重，不再把未使用的系统预置值混入历史候选。
5. **侧栏多组展开**
   一级导航组的展开状态独立保存；展开其他组不会收起已有组，当前页面所属组始终保持展开，权限过滤后不渲染空组。
6. **保守代码维护**
   统一学生浮层的临时布局状态与清理路径，复用已存在的课程局部刷新、候选缓存和事件委托；未删除费用、薪资、权限、导入导出、结转和操作日志的兼容逻辑。

本节记录近期多轮开发中的主要功能变化、问题修复及影响范围。若历史方案与后续记录存在冲突，以时间较新的记录和当前“权限说明”为准。

### 2026-07-12（冲突弹窗事件与学生浮层布局修复）

1. **冲突列表全量可操作**
   课程总表冲突弹窗改为统一事件委托。每条课程的“处理”按钮都通过唯一 `data-lesson-id` 打开对应课程草稿，不再依赖数组序号或仅绑定首个按钮；老师、教室、学生冲突中的每一条课程均可独立处理。
2. **关闭与二级弹窗层级**
   冲突列表的关闭按钮、遮罩和 Esc 均调用幂等关闭函数，移除遮罩并恢复页面滚动，不改变筛选、勾选、滚动位置或排课状态。二级编辑弹窗使用更高层级显示，取消后回到冲突列表；保存后继续局部刷新课程行与当前可见冲突列表。
3. **排课学生选择浮层尺寸**
   学生浮层桌面端调整为约 400px 宽，候选列表默认显示约五行学生。仅候选列表滚动，搜索、统计、新增学生和取消/确认按钮始终可见；下方空间不足时优先向上展开，右侧空间不足时限制在视口内。
4. **版本与静态资源**
   后端诊断版本为 `2026.07.12-conflict-modal-events-picker-layout`，前端入口使用 `/app.js?v=20260712-conflict-modal-events-picker-layout` 与 `/styles.css?v=20260712-conflict-modal-events-picker-layout`。

### 2026-07-12（可见课程冲突统计与学生浮层优化）

1. **课程总表冲突范围一致**
   工具栏“冲突为 N”和冲突弹窗均直接使用课程总表当前可见课程集合：日期范围、老师、学生多选、教室、状态、年级、科目、关键字及角色预筛选均已生效。筛选为 0 条时冲突为 0；只有同时在当前结果中的两节课程才参与同一组冲突判断，筛选外课程不会进入按钮数量或弹窗条目。
2. **排课学生选择浮层完整可见**
   学生浮层加宽为桌面端优先约 480px，并按搜索/统计、候选列表、新增学生、操作按钮分区。浮层本体不再滚动，只有学生候选列表滚动；搜索、新增学生、取消和确认持续可见。浮层会根据单元格周边空间向上或向下展开，并约束在视口内。
3. **智能冲突候选交互态**
   时间、教室冲突候选使用统一语义 class；默认、hover、active、focus、键盘选中和 `aria-selected` 状态均保留业务冲突红色，在亮色、暗色与黑白配色中保持清晰。下拉候选仍可显示状态文字，选择后单元格只显示原始时间或教室值。
4. **版本与静态资源**
   后端诊断版本为 `2026.07.12-visible-conflicts-student-picker`，前端入口使用 `/app.js?v=20260712-visible-conflicts-student-picker` 与 `/styles.css?v=20260712-visible-conflicts-student-picker`。

### 2026-07-12（排课模式推荐与冲突弹窗收尾）

1. **排课模式推荐与展示值分离**
   排课行仅对时间、教室候选按“可选 / 冲突”排序；老师沿用原有顺序正常选择，不显示推荐标签。候选的 `value` 始终是可保存的原始值，带状态的 `label` 只用于候选展示；时间统一保存为 `HH:mm-HH:mm`，已选时间、教室和老师在单元格中只显示原始值。
2. **排课模式学生编辑面板**
   点击学生单元格会在单元格旁打开独立浮层，支持在读学生搜索、中文输入法组合事件、多选、已选与当前结果人数、新增学生文本、取消和确认。搜索不会清除已选项；确认时只提交当前课程行的规范化学生集合，取消、遮罩关闭或 Esc 不会修改课程数据。
3. **课程冲突独立弹窗**
   “冲突为 N”打开冲突列表而不改变日期范围、筛选、勾选、滚动位置或排课状态。每条冲突可在二级课程修改弹窗内处理日期、老师、时间、教室、年级、科目、状态、学生和备注；取消不写入，保存后只局部更新对应课程行、统计、冲突列表和冲突数。只读账号可查看冲突，但不可进入修改。
4. **统一冲突口径与最终校验**
   新增课程、排课候选、冲突修改弹窗和服务端保存均使用同一时间段重叠规则，覆盖不可解析时间、同日老师、教室和共同学生冲突；编辑时会排除课程自身。服务端对新增与修改均进行最终拦截，用户确认后才可带冲突保存，并继续记录操作日志和既有费用/薪资缓存失效标记。
5. **版本与静态资源**
   后端诊断版本为 `2026.07.12-schedule-conflict-modal`，前端入口使用 `/app.js?v=20260712-schedule-conflict-modal` 与 `/styles.css?v=20260712-schedule-conflict-modal`。

### 2026-07-12（页面结构、局部刷新与账号弹窗）

#### 学生、教师、设置与首页 UI 优化

1. **新增课程学生搜索与费用明细操作区**
   `排课 -> 课程总表 -> 新增课程` 的学生搜索已改为前端实时模糊过滤，并显式控制候选项可见性；中文输入法组合期间不触发过滤，确认上屏后才更新。搜索不会清除已勾选学生，清空关键字会恢复全部在读学生，手动新增学生文本框保持独立；搜索区持续显示“已选 / 当前结果”人数，Enter 不会提交表单。`学生 -> 费用明细` 的“按规则更新所选费用”按钮已回到操作区左侧，说明文字紧随其后，禁用、只读和批量更新口径不变。
2. **充值记录与期初余额页面层级**
   `学生 -> 充值记录` 已按“紧凑数据分析 → 筛选区 → 操作按钮区 → 表格”排列，保留本月实际/赠送/净充值、记录数、学生数、退费以及按年级、来源拆分，并收紧卡片和标签留白。`学生 -> 期初余额` 已按“筛选区 → 操作按钮区 → 表格”排列，操作按钮固定为“新增期初余额、批量删除、下载模板、导入 Excel、导出 Excel”；两页在窄屏均按既定顺序自然换行，原字段、导入导出、禁用态和财务口径保持不变。
3. **学生查询无动画局部刷新**
   首次进入后，学生选择、范围切换、日期控件、八个统计卡、月份汇总表和明细表容器均保持稳定。切换“全部月份 / 日期范围”时只修改既有统计卡文字和数值、月份表行、课程明细行及导出/复制按钮状态；不会重建统计卡、触发渐入/位移动画或改变页面滚动位置。学生与有效日期范围相同的账单请求继续复用页面缓存，切回全部月份会保留但不应用上次日期范围。
4. **重复说明清理与教师页面操作区**
   学生单价、班级管理、学生档案、教师档案和薪资规则页面已移除与 topbar 重复的页内标题/说明，筛选和表格自然上移。薪资规则的“新增薪资规则”已置于筛选区与规则表之间的独立左对齐操作行；筛选、自动保存、规则匹配、薪资计算、档案编辑和阶段展开逻辑均保持不变。
5. **黑白主题、账号管理与首页密度**
   黑白亮色主题提升为深黑正文、表头、按钮和输入文字，次级文字保持可辨识灰度；黑白暗色主题使用高对比白色正文和浅灰次级文字，不影响其他配色或业务警示红色。`设置 -> 账号权限 -> 账号管理` 的新增账号已改为工具栏按钮，和模板下载、模板导入、同步老师账号并列；点击后在弹窗填写账号/手机号、显示姓名、角色、绑定老师和初始密码，沿用原有校验、只读、权限、接口和审计日志，成功后关闭弹窗并更新账号列表。首页机构数据、常用功能、待办和无数据趋势区已减少无效高度与留白，同时保留响应式和可点击区域。
6. **版本与静态资源**
   后端诊断版本已更新为 `2026.07.12-ui-layout-account`，前端入口使用 `/app.js?v=20260712-ui-layout-account` 与 `/styles.css?v=20260712-ui-layout-account`。

### 2026-07-11

#### 课程工作流、截图工具与黑白配色优化

1. **矩阵课表可读性与局部切换**
   老师课表、教室课表的日期列、老师/教室首列和课程卡片已加宽；时间、年级、科目、老师或教室、学生信息支持自然换行，不再因过窄而裁切或逐字竖排。三种矩阵视图沿用统一表头、首列、卡片、边框和空状态规则，超宽内容仅在矩阵容器内横向滚动，视图切换继续只替换矩阵内容区域。
2. **课程总表字段、序号与工具栏**
   课程总表已删除前端“学生人数”列，保留原业务字段和 Excel 口径；复选框右侧新增按当前筛选与排序实时计算的“序号”列，最右侧“累计序号”继续保持业务语义。浏览态和排课态的日期列已统一字体、字号、字重、行高、对齐与颜色。工具栏固定为“开始/结束排课、 新增课程、整周复制、批量复制、批量已上、批量删除、冲突数量”顺序；新增课程为白底按钮，0 冲突保持灰色中性样式。
3. **筛选与新增课程学生搜索**
   课程总表和矩阵课表的“已筛选 X / 共 Y 节”和重置按钮保持在筛选卡片最右侧，并在窄屏换行后继续右对齐。新增课程弹窗新增在读学生姓名搜索、已选择人数和当前结果人数；搜索在中文输入法组合期间不刷新，过滤或清空搜索均不会丢失已勾选学生，Enter 不会提交新增表单，手动新增学生输入区继续保留。
4. **家长群与老师课程截图**
   两个截图页面的第一行已统一日期范围、勾选项、模式切换、按钮和完成统计的高度、基线与间距；“全局后半句”固定在第二行并占用剩余宽度。老师课程截图新增并持久化“预览模式 / 简洁模式”：预览模式保留原大卡片、称呼、文案、复制、下载和完成反馈；简洁模式按老师展示课程数量与完成状态，点击复用截图/文案复制与现有老师完成记录，不新增后端完成口径。
5. **费用明细与学生查询局部刷新**
   学生费用明细的“按规则更新所选费用”已移至操作区右侧，说明文字位于左侧，禁用和只读规则不变。学生查询首次进入后保留标题、学生选择、范围切换和日期控件 DOM；切换全部月份/日期范围只更新 8 个统计卡、月份汇总、课程明细和导出/复制按钮状态，并保留滚动位置和上次日期范围。相同学生与有效范围复用页面级请求缓存，未选择学生时不请求账单数据。
6. **黑白配色与课程截图配色**
   黑白亮色主题改为纯白背景、黑色文字、极浅边框和浅灰导航选中态；暗色主题改为近纯黑背景、白色文字与高对比黑白按钮，减少大面积灰色和蓝绿偏色。错误、危险和负数仍保留必要红色语义。开启“课程截图跟随当前配色方案”后，黑白截图使用黑白灰；关闭时无论亮暗主题均固定使用黎明蓝截图。
7. **版本与静态资源**
   后端诊断版本已更新为 `2026.07.11-ui-course-workflow`，`app.js` 与 `styles.css` 均使用 `20260711-ui-course-workflow` 静态版本参数。

### 2026-07-09

#### 顶部月份切换策略、矩阵课表与截图工具栏优化

1. **通用布局 / topbar / sidebar 继续压缩**
   左侧品牌区已与右侧 topbar 统一为紧凑高度，logo 方块、品牌文字行高、分隔线和菜单首项间距同步收紧；亮色、暗色和黑白配色均沿用同一组 sidebar / topbar 变量。
2. **课程总表与顶部月份彻底解耦**
   `排课 -> 课程总表` 进入时默认显示自然本周课程，页面数据继续以课程总表自己的日期范围为准；切换顶部月份只更新全局月份和本地存储，不刷新课程总表、不清空筛选、不重拉表格。本月快捷仍保留，只有用户主动点击 `本月` 时才按顶部当前月份计算首末日。
3. **矩阵课表与顶部月份解耦并局部切换**
   `排课 -> 矩阵课表` 按页面自己的日期范围请求全局课程数据，顶部月份改变不刷新矩阵页、不重置日期范围、老师 / 学生 / 教室 / 年级 / 科目筛选和当前视图类型。`时间课表 / 老师课表 / 教室课表` 切换时只更新 active 按钮和矩阵内容区域，日期筛选区和筛选控件 DOM 保持稳定。
4. **老师课表、教室课表滚动体验统一**
   老师课表和教室课表已改用与时间课表一致的矩阵容器节奏，横向滚动收敛在表格容器内，纵向随页面自然滚动；表头、首列、卡片间距、空状态和时间课表保持统一，老师或教室较多时可完整横向浏览。
5. **课程截图工具栏重排**
   `排课 -> 家长群课程截图` 与 `排课 -> 老师课程截图` 的首行保留日期范围、只选择“已上”、详细 / 简洁模式、生成课程通知、完成对象统计和清除打勾记录等操作；`全局后半句` 标签与输入框独立放在第二行并横向铺满剩余空间。个人发送、班级发送、班级合并发送、复制文案、复制截图、下载截图和完成打勾记录继续沿用原能力，暑期班截图模式未恢复。
6. **学生模块月份切换局部刷新**
   `学生 -> 费用汇总`、`费用明细`、`充值记录` 在顶部月份改变时只刷新当前月份相关数据、统计、表格行和批量按钮状态，保留当前筛选条件和页面结构；`期初余额`、`学生查询`、`学生单价`、`班级管理`、`学生档案` 与顶部月份无关，月份改变不刷新当前页面。学生查询内部的 `全部月份 / 日期范围` 切换继续只更新统计卡片、月份汇总、明细课程表和导出 / 复制图片按钮状态。
7. **教师、运营、经营概览和设置按相关性刷新**
   `教师 -> 薪资汇总`、`车费明细`、`课时明细` 切换月份只刷新薪资 / 车费 / 课时数据和当前表格；`薪资规则`、`教师档案` 与顶部月份无关。运营中的员工薪资、员工考勤、日常开销按月份或日期范围局部刷新；经营概览只刷新指标、图表和表格数据；外观设置、基础数据、费用标准、账号权限、操作日志等全局配置页不因顶部月份改变重建页面。
8. **统一顶部月份改变入口**
   前端新增 `onActiveMonthChanged(newMonth, oldMonth)` 作为顶部月份切换的唯一入口：月无关页面只同步 `activeMonth`、`state.settings.month_key` 和 `localStorage`；月相关页面调用当前页面专用刷新函数；复杂派生数据页复用现有 bootstrap 派生口径但只重绘当前页面内容，避免走全局 `load()` 的整页启动链。

#### 学生档案、topbar 与外观配色 UI 优化

1. **全局 topbar 改为紧凑布局**
   首页、排课、学生、教师、设置等页面共用的顶部栏已压缩高度；左侧折叠按钮、页面标题区、月份选择、新建月份、删除月份、用户菜单和顶部操作按钮同步收紧并保持垂直居中，内容区仍由主布局自然下移，不与 topbar 重叠。
2. **学生档案表格收紧并固定列宽**
   `学生 -> 学生档案` 的姓名列改为只读文本，行内不再暴露姓名编辑输入框；普通学生行进一步压缩行高、单元格 padding、字号和 input/select 高度，checkbox 居中，姓名、当前年级、状态、入学日期、离校日期、电话、备注等列通过 `colgroup` 固定宽度，内容较多时在表格容器内横向滚动，阶段展开行保持自适应。
3. **学生档案删除入口调整**
   学生档案表格已删除最后一列 `操作` 和单行删除按钮；工具栏新增 `批量删除` 按钮，未勾选时禁用，勾选学生后可二次确认删除。后端新增 `POST /api/students/batch-delete`，复用现有单个学生删除语义：无历史业务数据的学生物理删除，有历史课程、充值或学生单价规则的学生改为 `离校` 并补离校日期。只读账号前端和后端均不能批量删除。
4. **操作日志补充学生批量删除**
   学生批量删除会写入审计事件和操作日志，记录操作者、删除数量、改为离校数量、前若干学生姓名、目标 id 和操作时间。
5. **外观设置下拉框和黑白配色**
   `设置 -> 外观设置` 的主题、配色方案等原生 `select.control` 已统一隐藏浏览器原生箭头并只保留一个系统下拉箭头。配色方案新增 `黑白`，采用黑、白、灰中性色，按钮、选中态、左侧导航、卡片强调、表格 hover/选中、日期图标、用户头像和课程截图跟随配色时的截图变量均不使用彩色强调；关闭课程截图跟随配色时仍固定使用黎明蓝截图。

### 2026-07-08

#### 截图反馈 UI 修正

1. **教师课时明细备注列恢复**
   `教师 -> 课时明细` 已恢复 `备注` 列，表格与复制图片均展示课程备注内容；筛选、复制图片、按规则更新所选薪资和薪资计算逻辑保持不变。`教师 -> 车费明细` 仍按当前口径不展示备注列。
2. **备份记录与整周复制弹窗继续放大**
   `设置 -> 数据对账 -> 查看备份记录` 弹窗调整为桌面端约 80vw、最大 1280px 的大尺寸面板，备份时间、类型、状态、月份数、大小、文件名和下载按钮在表格内横向舒展显示；窄屏下使用可用宽度并允许表格区域滚动。`排课 -> 课程总表 -> 整周复制` 弹窗调整为桌面端约 84vw、最大 1360px，原课程、目标课程、老师、时间、教室、年级、科目、学生、备注和冲突提示完整展示，底部操作按钮保持可见。
3. **0 冲突改为灰色中性状态**
   课程总表工具栏 `冲突为0` 按钮、冲突检查空状态和整周复制弹窗 `冲突数量 0` 提示均改为灰色中性样式；有冲突时继续使用醒目的红色 / 警示样式。
4. **筛选统计最右对齐**
   `排课 -> 课程总表` 与 `排课 -> 矩阵课表` 的 `已筛选 X / 共 Y 节  重置` 作为整体贴近筛选卡片右边缘展示，窄屏换行时继续右对齐，不挤压老师、学生、状态、日期等主要筛选框。矩阵课表仍不显示 `当前周` 按钮。
5. **档案、日志和日期图标细节**
   `学生 -> 学生档案` 普通行的行高、单元格 padding、字号、输入框高度、按钮高度和 checkbox 垂直居中继续与 `教师 -> 教师档案` 统一，阶段展开行保持自适应。`设置 -> 操作日志` 的 `操作内容` 列已加宽并支持多行自动换行，表格横向空间不足时可滚动。所有日期范围筛选框统一使用新版简洁日历图标，保留 hover 清空和 `今日` 快捷方式。

#### UI、局部刷新与操作日志完善

1. **备份记录弹窗放大**
   `设置 -> 数据对账 -> 查看备份记录` 改为宽高更大的弹性弹窗，备份表格、状态、大小、月份数和下载按钮可完整展示；窄屏下弹窗自适应宽度，表格内容区域独立滚动，标题和底部按钮不被遮挡。
2. **日期范围筛选器统一优化**
   所有起止日期范围筛选框右侧改为简洁日历图标；日期范围面板左侧新增 `今日` 快捷项，点击后开始日期和结束日期都设为浏览器当前自然日期。原开始 / 结束日期、hover 清空、双月日历、快捷日期、日期排序和筛选刷新能力保持不变。
3. **排课筛选与矩阵课表布局**
   `排课 -> 课程总表` 和 `排课 -> 矩阵课表` 的 `已筛选 X / 共 Y 节  重置` 统一靠右展示，窄屏换行后仍尽量靠右，不挤压主要筛选框。矩阵课表删除 `当前周` 按钮，保留日期范围、重置和 `时间课表 / 老师课表 / 教室课表` 三种视图切换。
4. **家长群课程截图移除暑期班模式**
   `排课 -> 家长群课程截图` 删除 `暑期班截图模式`、模式切换按钮、前端状态和后端 `mode=summer` 分支；当前保留默认 / 日常班截图逻辑，并继续支持个人发送、班级发送、班级合并发送、预览模式、简洁模式、复制截图、复制文案、下载 PNG 和完成打勾记录。
5. **整周复制弹窗放大**
   `排课 -> 课程总表 -> 整周复制` 弹窗增大，预览表展示原课程、目标课程、日期、老师、时间、教室、年级、科目、学生、备注和冲突提示；弹窗内部可滚动。冲突数量为 0 时使用灰色提示，有冲突时继续醒目显示。
6. **课程总表局部刷新优化**
   课程总表在修改顶部月份、调整筛选、点击重置、批量已上、批量删除、批量复制和整周复制后，优先只刷新统计数字、筛选区、工具栏、表格 tbody 和弹窗区域，不再无意义重建整个内容容器，减少统计卡片和表格消失再出现的闪烁。
7. **学生与教师页面细节**
   `学生 -> 费用明细` 的 `按规则更新所选费用` 工具区增加上下间距和顶部分隔，批量更新逻辑不变。`学生 -> 学生档案` 与 `教师 -> 教师档案` 的行高、padding、输入框高度和字号统一。`教师 -> 车费明细` 前端不再展示 `备注` 列；`教师 -> 课时明细` 已恢复课程备注列。数据库兼容字段保留，车费保存、薪资汇总、课时薪资计算、复制图片和按规则更新薪资不受影响。
8. **操作日志补齐**
   操作日志新增或增强：查看备份记录、下载备份、手动生成备份、修改自动备份设置、单月核心 Excel 导出、批量导出全部月份核心 Excel、课程整周复制 / 批量复制 / 批量已上 / 批量删除、学生费用按规则更新、教师课时薪资按规则更新、教师车费保存、学生档案阶段保存 / 批量界定时间、学生 / 教师档案新增编辑删除、充值记录新增编辑删除 / 批量删除、期初余额新增编辑删除 / 批量删除、学生单价规则修改、教师薪资规则修改、基础数据修改、权限 / 角色 / 账号相关修改。日志内容使用中文描述关键对象、月份、数量和操作者，不记录密码明文、token、cookie 或完整数据库路径。

#### 课程筛选、截图、充值与首页实时课程

1. **课程总表新增“今日”快捷筛选**
   `排课 -> 课程总表` 的日期快捷按钮调整为 `今日 / 本周 / 下周 / 本月`。`今日` 按服务器当前自然日期设置开始和结束日期，其他老师、学生、状态、教室、年级、科目、搜索等筛选仍按原联动逻辑工作；快捷按钮改为轻量无边框样式，并保留 hover、focus 和选中态。
2. **家长群课程截图新增班级合并发送**
   `排课 -> 家长群课程截图` 在 `个人发送`、`班级发送` 之外新增 `班级合并发送`。同一日期范围内，人数大于 1 且同一班级学生集合存在多个不同学科课程时，会合并为一张截图 / 一个发送对象；只有单学科的班级仍走原 `班级发送`。合并截图展示班级 / 年级、学生名单或人数、日期、不同学科课程、老师、时间和教室等信息，复制、下载、简洁模式、日常班截图和已完成打勾记录继续沿用原机制。
3. **充值记录新增数据分析汇总**
   `学生 -> 充值记录` 表格上方新增 `充值数据分析汇总`，随来源、学生姓名、年级等当前筛选实时联动，展示本月实际充值合计、本月赠送充值合计、本月净充值合计、充值记录数、充值学生数、退款 / 负充值合计，并按年级和来源做拆分。金额格式沿用系统 `¥` 口径，负数使用加粗和下划线辅助识别，黑白打印下仍能区分。
4. **首页正在上的课程修正**
   首页 `正在上的课程` 按当前真实日期和当前时间判断：课程日期必须等于今天，当前时间必须落在 `time_slot` 的开始和结束之间，并优先保留 `上课中 / 未下课 / 已上 / 待上 / 未缴费 / 上课 / 未上 / 试课` 等有效状态；`请假 / 取消 / 停课 / 暂停 / 休息 / 异常 / 作废 / 删除 / 待定 / 考试 / 监考` 等无效状态会被排除。时间段兼容 `18:00-20:00`、`18:00 - 20:00` 等写法。

#### 数据对账导出与自动备份

1. **单月核心 Excel 保留原逻辑**
   `设置 -> 数据对账` 的 `导出核心 Excel` 仍导出当前顶部月份，sheet 顺序、字段来源和金额口径继续复用原 `coreWorkbookSheets(monthKey)`。
2. **新增批量导出全部月份**
   `批量导出全部月份` 会把系统中所有月份的核心 Excel 打成 zip，文件名为 `黎明教育_全部月份_核心数据_YYYYMMDD_HHmmss.zip`。zip 内每个月的 xlsx 仍使用单月核心 Excel 同一套生成逻辑，避免字段不一致。
3. **新增自动备份**
   `自动备份` 默认启用，每周三检查一次。系统在服务启动和访问备份接口时检查是否到达计划日；同一天已自动备份成功后不会重复生成，手动 `立即备份` 不受此限制。每次备份内容为所有月份核心 Excel 的 zip。
4. **备份记录与保留策略**
   自动 / 手动备份记录保存备份时间、备份类型、包含月份数量、文件名、路径、文件大小、状态和计划日期。成功备份 zip 最多保留最近 5 次，第 6 次成功备份生成后会删除最早的成功备份文件和记录；失败记录保留用于排查。
5. **查看与下载历史备份**
   `查看备份记录` 可查看历史备份时间、类型、大小、包含月份和状态，并下载对应 zip。备份文件存放在 `data/backups/`，不放入源码目录。

新增接口与数据结构：

- `GET /api/export/core-workbooks-all.zip`：批量导出全部月份核心 Excel zip。
- `GET /api/backups`：读取备份设置与历史记录，并触发一次到期自动备份检查。
- `PUT /api/backups/settings`：保存是否启用自动备份和每周几执行。
- `POST /api/backups/run`：手动立即生成一次全部月份核心 Excel zip 备份。
- `GET /api/backups/:id/download`：下载历史备份 zip。
- 新增 SQLite 表 `backup_records`，用于保存备份元数据。
- 复用 `settings` 表新增键：`auto_backup_enabled`、`auto_backup_weekday`、`auto_backup_last_date`。
- 备份目录为 `data/backups/`。

### 2026 年 6 月（2026-06-19 前）

#### 筛选、课程总表与基础体验

1. **筛选框支持搜索**
   课程、学生、教师等主要筛选框支持输入搜索、中文过滤和下拉选择，减少候选项较多时的查找成本。
2. **课程总表与顶部月份解耦**
   课程总表以页面内部的日期范围为准，不再被顶部全局月份强制重置；切换月份时可继续保留当前课程查询范围。
3. **精简页面文案**
   删除重复、冗余或容易造成误解的提示文字，使课程、费用、账号等页面的信息层级更清晰。
4. **操作日志统一为北京时间**
   日志时间统一按北京时间展示，并补充课程、月份、账号、权限等更多业务操作记录，便于问题回溯。
5. **新增账号导入模板**
   提供账号导入模板和模板下载入口，支持批量准备账号、显示姓名、角色、绑定老师、初始密码、只读状态等信息。
6. **优化费用截图**
   调整费用相关截图的排版、尺寸和内容密度，提高在聊天工具和不同屏幕环境中的可读性。

#### 首页仪表盘与权限体系

1. **新增首页仪表盘**
   增加首页入口，集中展示课程、学生、费用等关键指标、待办事项和快捷入口。
2. **优化费用汇总页面**
   删除底部重复统计信息，避免与上方汇总指标重复展示。
3. **拆分角色管理与账号管理**
   账号权限模块拆分为“角色管理”和“账号管理”：角色负责页面权限、只读属性和预筛选；账号负责登录信息、所属角色、绑定老师、状态及账号级覆盖配置。
4. **新增角色与权限数据表**
   引入 `roles`、`role_permissions`、`role_filter_presets` 等结构化权限表，替代仅依赖前端固定角色判断的方式。
5. **菜单按页面权限显示**
   侧边栏菜单和子菜单根据当前账号实际页面权限动态隐藏，无权限页面不再显示入口。
6. **收敛内置角色**
   当前主要角色统一为老板、教务、小助手、助教和老师五类，减少重复或语义相近的角色配置。
7. **兼容历史账号与角色代码**
   保留 `admin`、`boss`、`finance`、`jiaowu` 等历史角色或账号映射，升级后旧账号仍可登录并迁移到当前权限体系。
8. **优化首页视觉展示**
   调整首页标题、指标卡、趋势图和学生分布饼图，使首页更适合作为登录后的工作入口。
9. **补充首页实时课程与老师统计**
   首页增加“正在上的课程”，并在学生分布旁补充老师总数量圆环图。
10. **统一金额和表格显示细节**
    金额展示统一带 `¥`，负数显示为 `-¥160.00`；学生、充值、费用明细等表格继续优化行高、备注对齐和价格状态标识位置。

#### 登录页、教师账号同步与只读机制

1. **美化登录页**
   重做登录页布局、视觉层级和账号密码输入体验，并保留记住账号、记住密码等原有能力。
2. **修复切换账号后的权限残留**
   登录和退出时重置页面位置及预筛选应用状态，避免不同账号之间复用上一账号的无权限页面或筛选状态。
3. **修复绑定老师与电话变更的关联问题**
   调整教师档案电话、老师账号和绑定关系的处理，避免修改电话后出现错误绑定或账号识别异常。
4. **新增同步老师账号**
   可根据教师档案中的手机号批量创建或补齐老师账号；相同手机号可对应多个绑定老师，同时保留冲突和跳过结果。
5. **加入只读角色与账号只读覆盖**
   角色可配置为只读，账号也可在角色基础上单独覆盖；只读账号不能执行新增、修改、删除、导入、重置密码等数据写入操作。
6. **阶段性增加老师数据范围限制**
   此阶段曾按绑定老师限制老师账号的数据范围。该方案已在 2026-06-19 的后续迭代中调整为“页面权限 + 前端预筛选”，不再作为当前数据权限规则。

#### 老师登录、充值与跨平台体验

1. **修复老师账号登录权限**
   补齐老师角色所需页面权限和接口校验，解决部分老师账号登录后无法进入已授权页面的问题。
2. **修复子权限菜单显示**
   当角色只拥有某个子页面权限时，对应一级菜单仍会正确显示，避免出现有权限但没有入口的情况。
3. **充值记录支持同月多条流水**
   充值记录改为按真实流水保存，同一学生同一月份可以存在多条充值、赠送或退费记录，不再因唯一月份记录而覆盖旧数据。
4. **优化充值弹窗、趋势图和 Mac 截图**
   调整充值录入弹窗和趋势图展示，并修复 macOS 环境下部分截图区域被裁切的问题。

### 2026-06-19

#### 老师多选、账号级权限与多老师绑定

1. **老师筛选支持多选**
   课程总表和矩阵课表的老师筛选支持同时选择多位老师，并保留搜索、清空和动态候选能力。
2. **新增账号级权限、只读和默认筛选**
   在角色权限之外增加账号级页面权限覆盖、只读覆盖和筛选配置兼容能力，用于处理个别账号的特殊授权。
3. **账号支持绑定多位老师**
   新增 `user_teacher_bindings` 关联表，账号可以绑定多位教师档案；`users.teacher_name` 仅保留第一位老师作为兼容字段。
4. **修复 `teacher-cs` 看不到课程**
   修正老师账号与课程数据的匹配逻辑，避免因账号名、教师姓名或绑定关系不一致而错误返回空课程。
5. **增加页面级接口校验**
   后端接口根据对应页面权限进行校验，前端隐藏菜单之外，直接请求无权限页面接口也会被拒绝。

#### 默认筛选迁移到角色管理

1. **默认筛选改为角色预筛选**
   默认筛选从账号管理迁移到角色管理，同一角色的账号共享页面初始筛选配置，减少逐账号重复维护。
2. **新增动态日期规则**
   预筛选日期支持今天、昨天、明天、本周、上周、下周、本月、上月、下月、固定日期等动态规则，并在登录时解析为实际日期。
3. **清理无关联异常老师**
   删除历史数据中无有效关联的 `???` 老师记录，并增加教师姓名校验，防止明显乱码或拼接姓名继续进入教师档案。
4. **账号支持软删除**
   删除账号时保留历史课程、教师档案、绑定关系和审计线索，账号状态改为已删除，不直接物理清除业务数据。

#### 预筛选文案与绑定老师动态来源

1. **优化预筛选文案**
   将“默认筛选”等容易被理解为权限限制的文案统一调整为“预筛选”，明确其仅用于页面初始状态。
2. **支持绑定老师动态预筛选**
   角色预筛选可选择“绑定老师”，登录后动态读取当前账号的多老师绑定列表，而不是在角色中写死教师姓名。
3. **精简账号管理表格**
   删除账号列表中的“权限配置”列，将复杂权限配置集中到角色管理和独立配置入口，减少表格横向宽度。
4. **修复绑定老师污染教师档案**
   账号绑定只允许选择已有教师档案，保存时仅写账号绑定关系，不再创建教师档案，也不会把多位老师拼接成一个教师姓名。
5. **修复小助手登录**
   修正小助手历史角色映射、页面权限和只读状态读取，确保旧小助手账号升级后可正常登录。
6. **教师明细按月份展示**
   教师明细根据当前月份筛选课程记录，避免跨月数据混入当月薪资和课时统计。

#### 绑定老师、预筛选与只读截图最终规则

1. **修复绑定老师下拉层被遮挡**
   账号管理中的绑定老师菜单展开时挂载到页面顶层，并使用固定定位跟随滚动，避免被表格、卡片和滚动容器裁切；课程页面的老师多选筛选不受影响。
2. **绑定老师不再硬限制数据**
   绑定老师仅作为账号资料和预筛选的动态来源，不再天然限制课程、教师档案、首页、教师明细或课程通知的数据范围。账号能否进入页面仍由角色或账号页面权限决定。
3. **明确三种老师预筛选模式**
   - **不预设**：不设置老师筛选，账号绑定老师不参与页面初始化。
   - **绑定老师**：首次进入页面时，以当前账号绑定的全部老师作为初始筛选；账号未绑定老师时保持空筛选。
   - **指定老师**：使用角色配置中指定的老师列表，与账号绑定关系无关。
4. **预筛选仅应用一次**
   预筛选只在登录后首次进入相关页面时应用。用户可以自由清空或修改筛选，本次会话内不会再次被覆盖；退出后重新登录会按当前角色配置重新应用。
5. **只读账号可复制和下载截图**
   家长群课程截图、老师课程截图的生成、复制和下载属于读取或导出类操作，不视为业务数据修改。只读账号只要拥有对应页面权限，即可正常复制或下载截图。
6. **保留只读写操作限制**
   截图复制完成后的打勾记录接口加入只读安全白名单；新增、修改、删除课程，导入数据，修改档案，重置密码等写操作仍然被禁止。

### 2026-06-20 权限登录兜底与线上权限自修复

#### 背景问题

线上部署后，小助手账号登录时出现“当前角色无权访问此功能”提示。该问题会使账号在登录后直接停留于无权限页面，属于严重的权限兜底异常。

系统要求所有角色均可正常完成登录，并自动进入当前账号拥有权限的第一个页面；如果账号暂未配置任何可访问页面，也应显示明确、友好的空状态，而不是卡死或持续请求无权限接口。

#### 主要原因

1. **历史角色代码未完全兼容**
   部分小助手账号可能仍使用 `xiaozhushou` 等历史角色值，旧逻辑未将其映射到当前标准角色 `helper`。
2. **角色权限记录存在但实际无效**
   `helper` 角色可能没有权限记录，或者已有记录全部被禁用、权限 key 已失效，导致登录接口最终返回空权限。
3. **初始化检查条件不完整**
   旧初始化逻辑只判断角色是否存在权限记录，没有进一步确认记录是否处于启用状态且属于当前有效页面权限。
4. **前端空权限回退错误**
   当前账号权限为空时，前端曾错误回退到 `week` 等无权访问的页面，随后请求课程接口并触发 403。
5. **线上静态资源缓存**
   浏览器或中间缓存可能继续使用旧版 `app.js`，使已经部署的新权限逻辑未能及时生效。

#### 修复内容

1. **补充历史角色映射**
   新增 `xiaozhushou -> helper` 映射，同时保留 `boss -> owner`、`admin -> owner`、`jiaowu -> academic`、`finance -> helper` 等已有兼容关系。
2. **确保五个内置角色存在**
   系统启动时确认老板 `owner`、教务 `academic`、小助手 `helper`、助教 `assistant` 和老师 `teacher` 五个内置角色均已初始化。
3. **增加角色权限自修复**
   如果内置角色没有任何“启用且有效”的页面权限，启动时自动补齐该角色的默认权限；已有人工配置的有效权限不会被覆盖。执行修复时会输出启动日志，例如：

   ```text
   role=helper restored=11 reason=no-enabled-valid-permissions
   ```

4. **修复登录后的页面兜底**
   登录成功后清理上一个账号遗留的页面、导航和筛选缓存，不再沿用旧页面；系统根据当前账号权限计算首个可访问页面。账号没有任何可访问页面时，显示“当前账号尚未配置可访问页面，请联系管理员”，并停止请求课程、教师等业务接口。
5. **补充登录权限诊断字段**
   `/api/auth/me` 增加 `status`、`first_accessible_view` 和 `app_version`，用于确认账号状态、登录后目标页面及当前部署版本。
6. **新增老板专用权限诊断接口**
   新增 `GET /api/debug/permissions`，用于检查角色、账号、有效权限、首个可访问页面、空权限角色、无效权限 key 和历史角色映射。接口仅允许老板账号访问，且不返回密码或密码哈希。
7. **修复共用读取接口的权限匹配**
   调整课程、教师等共用读取接口与页面权限的对应关系，避免账号已经拥有相关页面权限，但接口仍因只校验单一权限 key 而返回 403。只读账号可以读取已授权页面的数据，写操作仍按只读规则返回 403。
8. **降低旧静态资源缓存影响**
   前端入口增加版本参数，例如 `/app.js?v=20260620-permissions-1`，同时为 HTML、CSS 和 JavaScript 静态资源增加禁缓存响应头，减少线上继续加载旧权限逻辑的情况。

#### 验证结果

本地使用数据库副本验证五类账号均可正常登录，并进入各自的首个可访问页面：

| 账号 | 角色 | 首个页面 |
| --- | --- | --- |
| `boss` | 老板 `owner` | `dashboard` |
| `jiaowu` | 教务 `academic` | `dashboard` |
| `xiaozhushou` | 小助手 `helper` | `dashboard` |
| `zhujiao` | 助教 `assistant` | `dashboard` |
| `teacher` | 老师 `teacher` | `lessons` |

同时确认：

1. 历史角色值 `xiaozhushou` 可以正常映射到 `helper`。
2. `helper` 权限清空并重启后会自动恢复默认权限。
3. 权限为空的账号不会卡死，而是进入友好空状态。
4. 小助手读取课程接口返回 200。
5. 小助手写入课程接口返回 403，只读写入限制仍然有效。
6. 老板账号可以访问 `/api/debug/permissions`。
7. 新版 `/app.js?v=20260620-permissions-1` 可以正常加载。

#### 线上排查建议

如果线上再次出现角色登录后无权限，可按以下顺序检查：

1. 清理当前浏览器保存的旧页面和静态资源缓存：

   ```js
   localStorage.clear();
   location.reload();
   ```

2. 检查 `/api/auth/me` 返回的 `role`、`permissions`、`first_accessible_view` 和 `app_version`。
3. 使用老板账号访问 `/api/debug/permissions`，检查空权限角色、无效权限 key、账号状态和历史角色映射警告。
4. 检查线上数据库中的账号、角色和权限数据：

   ```sql
   SELECT username, role, status, readonly_override, permission_override_enabled
   FROM users
   WHERE username = 'xiaozhushou';

   SELECT code, name, readonly
   FROM roles
   ORDER BY code;

   SELECT role_code, permission_key, enabled
   FROM role_permissions
   WHERE role_code = 'helper';

   SELECT upp.*
   FROM user_page_permissions upp
   JOIN users u ON u.id = upp.user_id
   WHERE u.username = 'xiaozhushou';
   ```

5. 如果账号级权限覆盖被误开启且没有配置任何页面权限，可关闭该账号的权限覆盖：

   ```sql
   UPDATE users
   SET permission_override_enabled = 0
   WHERE username = 'xiaozhushou';
   ```

6. 在浏览器网络请求中确认线上加载的是 `/app.js?v=20260620-permissions-1`，而不是旧版无版本参数的前端资源。

### 20260620 小助手登录无权限页修复

#### 背景问题

线上部署后，小助手账号登录时仍然出现“当前角色无权访问此功能”，页面几乎空白，只显示这一行文字。该问题属于严重权限兜底问题。

系统要求所有账号、所有角色都应该能正常登录，并自动进入自己有权限的第一个页面；即使账号没有任何页面权限，也不应该出现空白死页，而应该显示完整系统框架下的友好提示。

#### 根因说明

本轮排查确认：

1. “当前角色无权访问此功能”不是前端写死的页面，而是后端 `authorizeApi(user, req, url)` 授权失败后返回的 403 错误。
2. 旧前端在 `load().catch` 中直接把 `error.message` 写入 `contentEl`，导致 app shell、sidebar、topbar 没完整渲染时，只剩顶部一行错误。
3. 线上小助手触发该问题的可能原因包括：
   - 线上仍加载旧版 `app.js`；
   - 旧 `localStorage` 还保留上一个账号的 `liming:view`；
   - 小助手账号被错误地带到无权限页面；
   - 小助手账号开启了账号级权限覆盖，但覆盖权限为空；
   - 线上权限数据和本地初始化逻辑不一致。

#### 修复内容

1. **修复登录后的页面选择逻辑**
   登录后清理旧页面状态，不再沿用上一个账号的 view，并根据当前账号权限重新计算首个可访问页面。
2. **优化小助手和助教首屏**
   `helper` / 小助手、`assistant` / 助教优先进入 `lessons`，避免默认进入不稳定或依赖较多权限的 dashboard。
3. **修复空权限兜底**
   如果账号没有任何可访问页面，不再 fallback 到无权限页面；显示“当前账号尚未配置可访问页面，请联系管理员。”，并停止请求业务 API，避免连环 403。
4. **修复 403 渲染方式**
   新增 `renderLoadFailure()`；任何漏出的 403 都显示在完整系统框架内，不再出现页面只剩一行“当前角色无权访问此功能”的空白页。
5. **处理账号级空权限覆盖**
   如果账号级权限覆盖开启但为空，自动回退角色权限，避免账号因误配置空覆盖而无法进入系统；debug 接口会提示 `empty_permission_override` warning。
6. **补充 `/api/auth/me` 诊断字段**
   `/api/auth/me` 增加 `role`、`role_label`、`permissions`、`first_accessible_view` 和 `app_version`。
7. **增强 `/api/debug/permissions`**
   诊断接口返回 `normalized_role`、`permission_override_enabled`、权限来源和空覆盖 warning，便于线上排查账号和角色权限问题。
8. **更新前端资源版本**
   app 版本更新为 `2026.06.20-permissions-2`，前端资源升级为 `/app.js?v=20260620-permissions-2`，用于减少线上浏览器继续加载旧版 `app.js` 的概率。

#### 验证结果

本地验证结果：

1. `xiaozhushou / 123456` 登录成功。
2. `/api/auth/me` 返回：
   - `role=helper`
   - 权限数非空
   - `first_accessible_view=lessons`
3. 小助手关键读取接口均返回 200：
   - `/api/months`
   - `/api/bootstrap`
   - `/api/dashboard`
   - `/api/teachers`
   - `/api/students`
   - `/api/lessons-range`
4. 小助手写入课程仍返回 403：“当前账号为只读，不能修改数据”，说明只读限制仍然有效。
5. 五类账号均可登录并获得首个可访问页面：
   - `boss`
   - `jiaowu`
   - `xiaozhushou`
   - `zhujiao`
   - `teacher`
6. 测试库中清空 helper 角色权限后，重启会自动补齐默认权限。
7. 测试库中开启小助手账号级空覆盖后，小助手仍回退角色权限，debug 接口显示 warning。

#### 线上排查步骤

如果线上再次出现小助手或其他角色登录后无权限，请按以下顺序检查：

1. 浏览器控制台执行：

   ```js
   localStorage.clear();
   location.reload();
   ```

2. Network 中确认加载新版前端资源：

   ```text
   /app.js?v=20260620-permissions-2
   ```

3. 小助手登录后检查：

   ```text
   /api/auth/me
   ```

   重点查看：

   - `role`
   - `permissions`
   - `first_accessible_view`
   - `app_version`

   正常应看到：

   ```text
   role=helper
   permissions 非空
   first_accessible_view=lessons
   ```

4. 老板账号访问诊断接口：

   ```text
   /api/debug/permissions
   ```

   重点查看小助手账号：

   - `role`
   - `normalized_role`
   - `permission_override_enabled`
   - `permission_count`
   - `first_accessible_view`
   - `warnings`

5. 数据库检查：

   ```sql
   SELECT username, role, status, permission_override_enabled
   FROM users
   WHERE username = 'xiaozhushou';

   SELECT role_code, permission_key, enabled
   FROM role_permissions
   WHERE role_code = 'helper';

   SELECT *
   FROM user_page_permissions
   WHERE user_id = (SELECT id FROM users WHERE username='xiaozhushou');
   ```

6. 如果线上账号级权限覆盖为空，可临时修复：

   ```sql
   UPDATE users
   SET permission_override_enabled = 0
   WHERE username = 'xiaozhushou';
   ```

#### 注意事项

1. “当前角色无权访问此功能”仍可用于真正访问无权限页面时的提示，但不能作为登录后的空白死页出现。
2. 所有角色都必须能登录系统，并进入有权限的第一个页面。
3. 只读只限制写操作，不影响登录和查看页面。
4. 线上部署后应确认浏览器没有继续缓存旧版 `app.js`。

### 20260620 角色预筛选改为隐形基础数据池

#### 规则调整

本轮将角色预筛选从“把页面筛选框填成默认值”调整为“角色层面的隐形基础数据池”。普通账号登录后，页面筛选框仍然是用户自己的二次筛选条件；清空或修改页面筛选只会影响二次筛选，不会取消角色预筛选。

最终数据口径为：

```text
最终数据 = 角色预筛选 ∩ 用户页面筛选
```

#### 时间预筛选

课程总表、矩阵课表的时间预筛选已由后端参与过滤：

1. 角色预筛选开始日期作为隐形下界。
2. 角色预筛选结束日期作为隐形上界。
3. 用户页面日期筛选只能进一步缩小范围。
4. 用户清空日期筛选时，仍只能看到角色预筛选范围内的数据。
5. 用户选择超出角色预筛选范围的日期时，后端返回两者交集；没有交集则返回空数据。

#### 老师预筛选

老师预筛选支持三种模式：

1. **不预设**：不限制老师数据池。
2. **绑定老师**：以当前账号绑定老师作为基础数据池。
3. **指定老师**：以角色配置中的指定老师列表作为基础数据池。

账号绑定老师本身不等于数据限制；只有角色预筛选选择“绑定老师”时，绑定老师才作为角色基础数据池来源。用户在页面老师筛选框中选择老师时，仍然只是在该基础数据池内继续筛选。

#### 接入范围

本轮已优先接入：

- 课程总表；
- 矩阵课表。

Dashboard、充值、费用汇总、学生/教师档案等其他独立接口仍可后续逐步接入角色预筛选。

### 20260620 学生扣费 FCFS 拆分修复

#### 背景问题

线上出现学生扣费边界异常：学生在最后一节课前实际余额为 120、赠送余额为 250，最后一节课费用为 370。正确扣费应拆分为实际扣 120、赠送扣 250，扣完后实际余额和赠送余额均为 0。

修复前系统可能显示为实际余额 -250、赠送余额 250，说明课程费用没有正确拆分到实际余额和赠送余额，而是继续落到了实际余额欠费中。

#### 修复后的扣费规则

学生扣费使用 FCFS / 先进先出账本口径：

1. 期初余额和充值批次按时间顺序进入账本。
2. 每笔余额批次内部先扣实际金额，再扣赠送金额。
3. 一节课费用可以拆分到多个批次、多个来源。
4. 只有实际和赠送都不足时，剩余部分才记为欠费。
5. 赠送余额不会变成负数。
6. 赠送仍有余额时，不允许实际余额继续变负。

#### 核心逻辑

本轮新增现金欠费抵扣逻辑和 FCFS 扣费函数；课程扣费统一走新的扣费函数。后续现金充值会先抵扣已有现金欠费，赠送充值不会抵扣现金欠费，只进入赠送批次。

#### 影响范围

修复影响所有使用学生汇总账本的页面和导出：

- 学生费用汇总；
- 学生查询 / 学生账单；
- 学生费用截图数据；
- 核心 Excel 的学生费用汇总；
- Dashboard 中依赖学生汇总的课程费 / 消耗金额等；
- 财务汇总中依赖学生汇总和余额表的部分。

`学生 -> 费用明细` 仍显示每节课的应收单人费用，不展示现金/赠送拆分。

#### 验证案例

已使用临时数据验证：

1. 实际 120、赠送 250、课费 370，扣完实际 0、赠送 0。
2. 实际 100、赠送 300、课费 250，扣完实际 0、赠送 150。
3. 实际 100、赠送 50、课费 200，扣完赠送 0，剩余 50 进入欠费。
4. 多笔充值按时间顺序 FCFS 扣费。
5. 同一学生同月多条充值仍然保留，不被覆盖。

### 20260620 学生表格视觉统一与局部刷新优化

#### 视觉统一

学生单价、学生档案复用学生费用汇总的年级配色逻辑，按学生年级为表格行设置相同的背景色。无年级学生保持默认底色。该调整不改变表格字段、排序、筛选、编辑或导入导出逻辑，也不影响课程总表、教师档案、充值记录等其他表格的视觉规则。

#### 学生档案分组调整

学生档案删除“初一 / 初二 / 初三 / 高一”等年级分组标题行，表格行改为连续展示；年级字段本身、原有排序、学生姓名筛选、状态筛选、补齐入学日期、新增学生和显示历史/离校数据逻辑均保持不变。年级区分改由行底色承担。

#### 局部刷新优化

前端优化了部分单条数据修改后的刷新链路，减少不必要的全局 `load()` 和整页数据重拉。优先调整的页面包括：

- 学生档案：新增和修改单行档案后合并本地 `state.profile_students`，只重渲当前档案模块；
- 教师档案：新增和修改单行档案后合并本地 `state.profile_teachers`，只重渲当前档案模块；
- 学生单价：新增、修改、删除规则后刷新当前学生单价模块数据，不重新初始化全系统；
- 账号权限：修改账号基础字段、状态、绑定老师、保存账号权限和重置密码时，优先更新本地账号列表并重渲账号管理模块；
- 课程总表：保留既有课程行内局部更新和表体重绘逻辑，避免已优化的课程编辑链路退回整页刷新。

涉及充值、期初余额、费用明细批量更新、档案删除、月份切换、导入导出、角色权限变更等会影响多模块统计或权限结构的操作，仍保留全量刷新或模块级重新拉取，以保证财务、权限、预筛选、只读、扣费和导出结果一致。

## 修复学生赠送余额负数与扣费账本优化

### 根因与修复

线上出现“实际余额为正、赠送余额为负”的根因是历史账本允许负赠送记录继续形成负赠送批次：当一条赠送扣减大于此前可用赠送余额时，旧逻辑会把未抵扣部分直接保存为负 `gift_balance`。例如账户原有实际 250、赠送 0，再进入赠送 -250，旧结果会汇总为实际 250、赠送 -250。

现已统一为：

1. 负赠送只用于冲减此前已有赠送余额。
2. 超出可用赠送的部分转到现金侧继续扣减；现金仍不足时形成现金欠费。
3. 赠送批次最低为 0，不允许形成负赠送批次。
4. 汇总收口时再次保护赠送余额不低于 0，并保留严重诊断信息，避免异常被静默展示。
5. 新增或导入期初余额时禁止负赠送，也禁止同时录入“现金欠费 + 正赠送”的矛盾期初状态。

### 统一扣费规则

课程费用统一通过 `consumeStudentBalance()` 扣减：

1. 期初余额、充值日期、创建顺序和记录 id 共同确定 FCFS / 先进先出顺序。
2. 每个批次内部先扣实际余额，再扣赠送余额。
3. 当前批次扣完后再进入下一批次。
4. 所有实际余额和赠送余额都不足时，剩余部分才记为现金欠费。
5. 后续现金充值优先抵扣已有现金欠费；如果抵扣后仍有现金欠费且账本中存在赠送余额，继续用赠送余额抵扣，直到现金欠费或赠送余额归零。
6. 扣费结果明确返回 `cashUsed`、`giftUsed`、`bonusUsed`、`debt`、`afterCashBalance`、`afterGiftBalance` 和 `updatedLedger`，同时保留原有消费字段供现有页面和导出使用。

学生费用汇总、学生查询与账单、费用截图数据、核心 Excel、Dashboard 和经营总览继续共享 `studentSummary()` / `calculateStudentAccountTimeline()` 的同一套账本结果。学生费用明细仍按原有口径展示每节课应收单价，不改变字段和显示。

### 余额扫描与验证

新增命令：

```bash
npm run test:student-balances
npm run audit:student-balances
```

扫描覆盖以下异常：

- 赠送余额小于 0；
- 实际余额为正且赠送余额为负；
- 实际余额为负且赠送余额为正；
- 实际余额或赠送余额不是有效数值；
- 历史超额赠送扣减被防御性转入现金侧的诊断记录。

已验证：

- 实际 250、赠送 0、扣 500，结果实际 -250、赠送 0；
- 实际 250、赠送 250、扣 500，结果实际 0、赠送 0；
- 实际 250、赠送 250、扣 600，结果实际 -100、赠送 0；
- 实际 120、赠送 250、扣 370，结果实际 0、赠送 0；
- 实际 100、赠送 300、扣 250，结果实际 0、赠送 150；
- 两笔充值 `100+50`、`200+100` 后扣 180，第一批全部扣完，第二批只扣实际 30，剩余实际 170、赠送 100；
- 临时 SQLite 强制写入实际 250、赠送 -250 后，汇总结果不再出现负赠送，并产生异常诊断；
- 本地 2026-02 至 2026-06 全月份扫描未发现异常余额。

### 低风险性能整理

- `buildStudentSummary()` 复用同一次查询得到的充值记录，不再重复查询同月充值。
- Dashboard 同一请求内复用月度课程、费用明细和学生汇总，指标与待办不再各计算一次。
- `financeBase()` 按日期范围使用派生缓存，并直接复用已计算的期末月份学生汇总。
- 教师汇总一次读取当月全部交通补贴，移除按教师逐条查询。
- 保留既有前端局部刷新、权限、预筛选、只读、账号绑定、充值多记录和导入导出格式；本轮未调整 UI 与 CSS。

## 修复真实数据下学生余额正负冲突

### 秦小沫真实数据复现

本轮使用已导入本地 SQLite 的真实数据复现，不再只依赖构造用例：

- 学生：秦小沫，初三，在读；
- 2026 年 2 月期初：实际 5820，赠送 200；
- 2026 年 5 月月末：实际 -1270，赠送 0；
- 2026 年 6 月 7、10、12、13 日共 4 节课，每节 370，充值前余额降为实际 -2750、赠送 0；
- 2026 年 6 月 19 日充值记录 `#807`：实际 +2500、赠送 +250；
- 旧逻辑只用实际充值抵扣欠费，得到实际 -250，同时把赠送 250 留在新批次，因此页面显示“实际 -250、赠送 250”。

错误发生在 2026 年 6 月 19 日充值批次进入账本后。课程扣费、月份切分和页面取数都走了统一账本；真正缺口是 `settleStudentCashDebt()` 只结算后续现金，没有在现金仍欠费时继续使用可用赠送余额。此前模拟测试覆盖了“扣课时已有赠送”和“负赠送调整”，没有覆盖“先欠费，后同时充值现金和赠送，且现金仍不足”的真实事件顺序。

### 修复后的账本规则

1. 期初、充值和课程按日期、时间及记录 id 排序，继续使用 FCFS。
2. 每个余额批次先扣实际，再扣赠送；两者都不足才形成实际欠费。
3. 后续实际充值先抵实际欠费。
4. 如果仍有实际欠费且任一批次存在赠送余额，赠送继续抵欠费，不允许保留“实际负、赠送正”。
5. 赠送为负时，超扣额转为实际欠费，赠送归零并记录 warning。
6. 赠送在充值事件中抵扣本月课程欠费时，同步把对应消费从“实际消费”重分类到“赠送消费”，保证充值、消费和余额恒等式一致。
7. 最终输出前再次归并并检查：赠送不能为负，不允许实际负且赠送正，也不允许实际正且赠送负。

秦小沫 2026 年 6 月修复后汇总为：

| 上月实际 | 上月赠送 | 本月实际充值 | 本月赠送充值 | 本月实际消费 | 本月赠送消费 | 月末实际 | 月末赠送 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| -1270 | 0 | 2500 | 250 | 1230 | 250 | 0 | 0 |

### 真实数据追踪与全量扫描

```bash
npm.cmd run trace:student-balance
npm.cmd run test:student-balances
npm.cmd run audit:student-balances
```

- `trace:student-balance` 默认输出秦小沫的档案、期初余额、充值记录、课程费用和逐事件账本；也可运行 `node src/server.js --trace-student-balance=学生姓名`。
- 每个期初、充值、课程和月末事件都输出实际/赠送余额并检查账本不变量。
- `test:student-balances` 固化秦小沫真实事件顺序，验证充值后现金抵欠费 2500、赠送抵欠费 250，最终为 0/0。
- `test:student-balances`、`audit:student-balances` 和账本追踪均使用只读数据库连接；扫描发现异常时附带学生、月份、余额和关键账本事件，不修改正式数据库。
- 当前本地数据扫描 2026 年 2 月至 6 月，每月 69 名学生，异常余额 0；秦小沫真实数据回归通过。

### 页面、导出与性能

学生费用汇总、学生查询/账单、学生费用截图、核心 Excel、Dashboard 和经营概览均使用 `studentSummary()`、`calculateStudentAccountTimeline()` 或基于它们缓存的财务汇总，没有保留另一套余额公式。学生费用明细仍只展示每节课应收费用，UI、权限、预筛选、只读和导入导出格式未改变。

性能方面，`feeDetails()` 在单次构建中预加载手动费用、学生单价规则和标准价，消除按学生课次重复查询的 N+1；学生历史记录也一次读取充值月份，移除循环内 SQL。测试会逐条比较预加载与原查询路径的单价、来源和规则 id，确保结果一致。

## 2026-06-26 登录加载、表格与批量删除优化

### 启动方式与版本

- 本地仍使用 `npm start` 启动，默认访问 `http://localhost:5177`；端口被占用时通过 `PORT=5178 npm start` 或 PowerShell `$env:PORT="5178"; npm start` 指定。
- 当前后端诊断版本为 `2026.07.17-schedule-editing-status-order-fix`，前端入口脚本为 `/app.js?v=20260717-schedule-editing-status-order-fix`，样式入口为 `/styles.css?v=20260717-schedule-editing-status-order-fix`。
- 静态前端没有构建步骤，修改后重启服务并强刷页面即可验证。

### 登录后首页加载

- `/api/bootstrap` 新增 `lite=1` 轻量模式。登录后首页默认只读取月份、基础枚举、当月老师/学生候选、价格标准等全局轻量数据，不再一次性计算全量课程明细、学生汇总、老师汇总和所有管理页数据。
- 前端 `load()` 改为按当前页面懒加载：进入课程页才取课程范围，进入充值页才取充值流水，进入期初余额、账号、审计、员工、经营概览等页面时再取对应接口。
- 费用明细、学生费用汇总、老师薪资、学生单价等依赖派生计算的页面仍会按需触发完整 bootstrap，保证金额和薪资口径不变。
- 课程总表新增/编辑和排课模式切换继续走局部刷新，减少整页重绘、滚动位置丢失和重复请求。

### 表格渲染规则

- 主要业务表统一接入 `smooth-table-wrap`、`uniform-table`、`nowrap-table`：横向滚动更顺滑，表头居中，数字/状态类字段居中，备注、学生名单、说明等长文本左对齐。
- 课程总表去掉最右侧独立“操作/删除”列，冲突提示移动到首列附近；排课模式下的“添加课程”改为局部插入和刷新。
- 家长/老师课程截图中，预览表格与复制/下载 PNG 的时间、年级、科目数据统一为常规字重，不再出现预览加粗、图片不加粗的不一致。

### 2026-06-27 表格 UI 对齐与列宽细化

本轮继续统一教务系统表格 UI，不调整费用、薪资、导入导出、权限或审计口径。

- 表格对齐规则：所有表头统一居中；金额列数据右对齐；备注列名居中、备注数据左对齐；学生/学生集合列名居中、数据左对齐；checkbox 选择列收紧并居中。
- 列宽优化：课程总表和矩阵课表的日期、时间列按 `YYYY-MM-DD`、`HH:mm-HH:mm` 收紧；课程总表学生列适当加宽；备注列不再无意义过宽；统一表格不再强制 `min-width: 100%` 平均撑满。
- 涉及页面：课程总表、矩阵课表、学生费用明细、学生费用汇总、充值记录、期初余额、学生查询、学生单价、班级管理、学生档案、课时明细、教师档案。
- 自检方法：启动项目并登录后，逐页检查表头、金额列、备注列、学生列、checkbox 列和日期/时间列宽；同时确认课程新增/编辑/筛选/批量删除、费用汇总、充值记录、期初余额导入导出、学生查询导出/复制图片、课时明细和档案编辑等核心功能没有异常。

#### 课程总表滚动性能与空白修复

- 浏览态轻量渲染：课程总表默认只渲染文本、状态标签和 checkbox，不再为每行生成授课老师、状态、教室、年级、科目、学生和备注等 input/select 控件；进入“排课模式”后才渲染可编辑控件。
- 事件委托：课程表 checkbox、全选和排课模式新增课程按钮统一走表格容器委托，避免每次重绘后给大量行重复绑定监听。
- 滚动容器：课程总表使用单一 `lesson-table-scroll` 横向滚动容器，关闭两侧稳定 gutter，减少左右空白和嵌套 overflow 带来的滚动阻塞。
- 列宽配置：课程总表列增加 `col-select / col-date / col-time / col-students / col-note` 等语义 class；checkbox、日期、时间、状态、星期、教室、年级、科目等短列固定收紧，学生列保留较宽范围，备注列按短内容收窄。
- 风险说明：本次主要优化 DOM 渲染、事件绑定、滚动容器和 CSS 列宽，不改变课程数据、学生费用、教师薪资、导入导出、权限或审计逻辑。
- 自检方法：打开课程总表，上下滚动和触控板横向滚动；进入/退出排课模式；检查新增课程、筛选、整周复制、批量已上、批量复制、批量删除、checkbox 选择和字段编辑保存。

#### 交互状态、充值删除与弹窗布局修复

- 批量操作按钮禁用态：费用明细“按规则更新所选费用”、充值记录“批量删除”、期初余额“批量删除”、课时明细“按规则更新所选薪资”、教师档案“批量删除”统一为未选择时灰色禁用，勾选至少一条可处理记录后恢复可用；禁用态使用真实 `disabled`，不会弹确认框或发请求。
- 充值记录批量删除：修复后端 `uniqueSorted is not defined`，批量删除会在事务内去重排序受影响月份、删除充值流水、刷新后续月份结转，并继续写入审计日志和操作日志；删除后前端刷新数据，费用汇总和余额按剩余充值重新计算。
- 表格空白优化：矩阵课表和学生费用明细使用专用 `compact-table-scroll`，取消左右稳定 gutter；checkbox 列紧凑居中，日期、状态、星期、时间、教室、年级、科目等短列收紧，学生列保留适度宽度，备注列限制在合理范围，金额列右对齐、表头居中。
- 新增期初余额弹窗：弹窗宽度调整到约 640px，表单改为两列布局，学生姓名、年级、期初实际余额、期初赠送余额对齐展示，备注独占整行；窄屏自动切换单列，底部取消/保存按钮保持右对齐。
- 自检方法：逐页确认上述批量按钮未勾选时灰色禁用、勾选后恢复；充值记录批量删除不再出现 `uniqueSorted is not defined`；矩阵课表/费用明细左右白色空白消失或明显减少；新增期初余额弹窗自然排开，保存、默认金额 0、学生/年级选择仍正常。

#### 课程候选项统一与批量复制目标表

- 新增课程弹窗候选项：授课老师来自教师档案且只展示 `在职` 老师，学生勾选区来自学生档案且只展示 `在读` 学生；下方“新增学生，可用逗号、顿号或换行分隔”的手填逻辑保留。
- 新增课程状态：`课程状态` 移到表单上半部分，与科目相邻；状态下拉复用统一候选项，包含已有状态和 `手动添加新状态`，手动输入的新状态会直接写入新增课程。
- 排课模式编辑：课程总表浏览态仍保持轻量文本渲染；进入排课模式后才渲染控件。授课老师、状态、教室、年级、科目、时间统一使用可搜索下拉，并支持 `手动添加新老师/状态/教室/年级/科目/时间`；老师候选项只主动展示在职老师，历史课程当前值仍可保留显示。
- 排课模式样式：课程总表数据区、单元格 input/select 和自定义下拉按钮统一为普通字重，表头保持原有加粗；状态标签颜色不变。
- 批量复制课程：目标课程表格列宽按字段收紧，老师、状态、星期、时间、教室、年级、科目等短字段不再撑开，学生和备注列保留适度宽度；原课程预览不受影响，整体平移天数和教师薪资为空时按当前规则自动匹配的逻辑不变。
- 批量复制目标课程候选项：目标表中的授课老师、状态、时间、教室、年级、科目与新增课程/排课模式复用同一套候选项逻辑，并支持手动添加新值后保存到批量新增 payload。
- 自检方法：新增课程只看到在职老师和在读学生；新增课程状态可手动添加；排课模式下数据不加粗，并能手动添加老师/状态/教室/年级/科目/时间；批量复制目标课程输入框不再过宽，目标课程手动添加新值后可确认新增。

#### 首页、课程总表与学生模块显示细节

本轮只调整前端展示和首页补充数据，不改变课程、余额、薪资、导入导出和审计口径。

- 课程总表：排课模式按钮文案改为 `开始排课` / `结束排课`；排课模式行高、字号和普通字重与浏览态保持一致，浏览态轻量渲染继续保留；日期筛选中的“快捷”按钮独立换到下一行。
- 首页：新增“正在上的课程”，按当天当前时间落在课程时间段内、且课程处于有效排课状态筛选；人数统计从单个学生圆环扩展为学员总数量和老师总数量两个圆环。
- 学生模块：充值记录页面隐藏“上月实际结转 / 上月赠送结转”两列，核心 Excel 导出仍保留历史列；费用汇总固定展示当前选择月份数据，不再提供“本月 / 迄今为止”切换。
- 表格细节：费用明细和学生查询的备注表头居中、备注内容左对齐；学生查询价格状态标识不再压住金额；学生档案行高收紧到与其他业务表一致。
- 金额显示：前端金额文本统一使用 `¥`，负数显示为 `-¥160.00` 形式；输入框仍保留纯数字，导入导出格式不变。
- 自检方法：执行 `node --check public/app.js`、`node --check src/server.js`、CSS 大括号平衡检查，并启动服务确认 `/api/version` 可访问；登录后检查首页、课程总表、充值记录、费用明细、费用汇总、学生查询和学生档案。

### 批量删除

系统批量删除支持勾选、多选、二次确认、删除后刷新和审计/操作日志：

| 页面 | 接口 | 说明 |
| --- | --- | --- |
| 充值记录 | `POST /api/recharges/batch-delete` | 删除真实充值流水后会刷新后续月份余额结转 |
| 期初余额 | `POST /api/opening-balances/batch-delete` | 仅删除全局期初余额记录 |
| 学生档案 | `POST /api/students/batch-delete` | 无历史课程、充值或单价规则的学生物理删除；有历史业务数据的学生改为离校并保留档案 |
| 教师档案 | `POST /api/teachers/batch-delete` | 无历史课程的老师物理删除；有历史课程的老师改为离职并保留档案 |

删除前请先用当前数据做备份，尤其是充值记录、学生档案和教师档案。充值流水会影响费用汇总、学生余额、经营概览和后续月结转；学生或教师档案若已有历史业务数据，不会硬删课程关联。

### 自检口径

本轮代码自检覆盖：

- `node --check public/app.js`
- `node --check src/server.js`
- CSS 大括号平衡检查与 `git diff --check`
- `npm.cmd run test:student-balances`
- 启动服务后访问 `/api/version`
- 登录后检查 `/api/auth/me` 的 `app_version`
- 浏览器按宽屏、中屏、窄屏检查首页排列；来回切换课程总表浏览态/排课态；检查截图详细/简洁模式、复制/下载、档案与账号状态编辑、角色权限和基础数据配色网格
- 检查 Toast/选择浮层在连续提示、局部刷新、切换页面和退出时不会残留或重复绑定
- 在隔离数据库中删除某候选的最后一条课程引用，确认下一次 bootstrap 与基础数据页面同步移除该候选和孤立配色

## 技术栈

- 后端：Node.js 24，使用内置 `http` 服务和 `node:sqlite`。
- 前端：静态页面，主要代码在 `public/app.js`，样式在 `public/styles.css`，无前端框架和构建步骤。
- 数据库：SQLite，默认路径 `data/liming-local.sqlite`，WAL 模式。
- Excel / xlsx：服务端通过自实现的 `unzipXlsx`、`zipStore`、`sheetXml` 等轻量函数读写 `.xlsx`，当前未引入 `exceljs`、`xlsx` 等运行时依赖。
- 外部依赖：`package.json` 当前无第三方运行时依赖；前端经营图表通过 ECharts CDN 加载。
- 部署：可本地单进程运行，也可通过 Docker / Docker Compose 部署。

## 目录结构

```text
src/server.js                 # 后端 HTTP API、SQLite 初始化、业务计算、Excel 导入/对账/导出
public/app.js                 # 前端单页应用逻辑
public/styles.css             # 前端样式、主题和配色
data/liming-local.sqlite      # 本地 SQLite 主库
data/source-workbooks/        # 历史源工作簿/参考工作簿，例如 2026年5月.xlsx
data/uploads/                 # 上传并对账的临时 xlsx 文件
data/backups/                 # 导入、审计修复、删除等操作前的数据库备份
data/templates/               # 本地模板文件
data/debug/                   # 本地排查输出，可按需清理
scripts/                      # 数据库备份、恢复、同步、审计等维护脚本
docs/                         # 部署、审计和数据流文档
deploy/                       # 部署相关辅助文件
```

数据库文件、源 Excel、上传文件和备份都属于业务数据，默认不随 Git 提交。

## 运行

```bash
npm install            # 当前无第三方运行时依赖，但建议先执行以保持 npm 环境一致
npm start              # 启动服务，默认端口 5177
npm run init           # 仅初始化数据库后退出（不开服务）
```

浏览器访问：

```text
http://localhost:5177
```

如果 `5177` 已被占用，当前代码不会自动换端口，需要手动指定，例如：

```powershell
$env:PORT="5178"; npm start
```

或在 bash / Docker 环境中设置 `PORT=5178`。启动后访问对应端口，例如 `http://localhost:5178`。

Docker 试用部署：

```bash
cp .env.example .env
docker compose up -d --build
```

详细服务器、域名、HTTPS 和数据库迁移说明见 [docs/deployment.md](docs/deployment.md)。
审计核实记录见 [docs/audit-phase2-verification.md](docs/audit-phase2-verification.md)。

## 月份数据机制

系统按月份组织数据，月份统一使用：

```text
YYYY-MM-01
```

例如 `2026-05-01` 表示 2026 年 5 月。

- `/api/months` 的月份列表来自 SQL 中已有的 `lessons.month_key` 和真实 `recharge_records.month_key`，不是来自 `data/source-workbooks/`。全局期初余额不产生月份。
- 月份数据主要保存在 SQLite 中；源 Excel 和 SQL 数据不是同一个东西。
- 新建月份会在 SQL 层建立月份上下文并补齐结转逻辑，但不会自动生成 `data/source-workbooks/2026年N月.xlsx`。
- 如果某个月没有课程但有真实充值记录，该月份也可能出现在月份列表中。
- Excel 工作簿中的跨月课程会按课程实际日期归入对应 `month_key`。

## 数据导入与源 Excel

- `data/source-workbooks/` 保存历史源工作簿或参考工作簿，可用于源文件导入、源头对账和结构参考。
- “上传并对账”的文件可能写入 `data/uploads/`，用于本次对账，不等同于长期源模板。
- 导入源工作簿会解析课程总表、充值记录、学生费用明细中的手填单价、学生单价、费用标准、教师交通费等，并写入 SQLite。
- 运行时页面、费用计算、薪资计算和核心导出均以 SQLite 当前数据为准。
- Excel 不是实时更新的数据源；用户在系统中修改课程、充值、费用或薪资后，原始 Excel 不会自动同步更新。
- “导入源文件并对账”不是整月简单覆盖，也不是只保存一个 source snapshot：课程目前按源文件中出现过的日期局部替换，充值按月份替换，其他月度配置按对应导入逻辑写入正式表。
- 对账扫描和正式导入共用有效课程行判断，缺日期、学生、教师或有效上课时间的底部残留行不会计为课程。

### 导入源文件并对账的课程同步语义

当前源文件导入的课程处理是“按源文件出现日期局部替换”：

1. 系统先解析 Excel 的 `N月总表`。
2. 对 Excel 中出现过的每个课程日期，删除数据库中该日期、该月份的旧课程。
3. 再插入源文件中这些日期对应的课程。
4. 如果某个日期没有出现在 Excel 源文件中，数据库里该日期原有课程不会在导入阶段自动删除。

因此，它不是整月 `replace`，也不是简单 `append`。例如源文件没有 `2026-05-18`，数据库中这一天的课程不会仅因为源文件缺少这一天就被自动删掉。后续是否删除，应通过对账结果人工确认。

其他数据的导入语义：

- `recharge_records`：按月份替换。
- `teacher_adjustments_monthly`：按月份写入教师交通费/车票。
- `fee_overrides`：从“学生费用明细”中的手填单价写入单节课、单学生覆盖价。
- `student_pricing`、`pricing_standards`：根据源工作簿中的学生单价表和费用标准写入。

### 有效课程行判断

系统不会把 Excel 中任意有值的行都当作课程。当前一条课程至少需要满足：

- 有有效日期 `date`；
- 有学生姓名 `student_names`；
- 有教师姓名 `teacher_name`；
- 有有效上课时间 `time_slot`。

这套判断由 `readXlsxTotalSheet()` 和 `readXlsxTotalRowsForImport()` 共用，避免“扫描课程”和“正式导入课程”口径不一致。

曾经的 4 月问题是：`4月总表!H1234=¥` 是 Excel 底部残留值，旧扫描逻辑把它算成第 132 条候选课程，但正式导入会跳过。现在它只会作为被跳过的候选行出现在日志里，不会进入 `scanned_lessons`，也不会写入数据库。

验证口径：

```text
2026-04:
rawCandidateRows=132
scanned_lessons=131
import_lessons=131
db=131

2026-05:
rawCandidateRows=158
scanned_lessons=158
import_lessons=158
db=158
```

对应日志示例：

```text
[xlsx audit][lessons] month=2026-04 rawCandidateRows=132 validLessonRows=131 skippedRows=1
[xlsx audit][lessons][skip] sheet=4月总表 row=1234 cells=H1234=¥ reason=missing-date|missing-student|missing-teacher|missing-time
```

## 功能说明

### 学生费用与余额口径

系统支持 `学生 -> 期初余额`，用于记录系统上线前学生账户已有的现金余额和赠送余额。期初余额是全局账户初始化数据，不属于某一个月份，不进入 `recharge_records`，也不计入本月充值。

`学生 -> 充值记录` 只记录真实流水，包括现金充值、赠送充值和退费。`实际充值 = 0 且 赠送充值 = 0` 的记录不视为真实充值记录，不在页面、接口和核心 Excel 的充值记录口径中展示。

系统已删除手动“从上月结转”功能，不再需要用户人工结转；下月余额由自动结转逻辑承接。当前余额链路为：

```text
期初余额 -> 真实充值流水 -> FIFO 扣费 -> 月末余额 -> 下月自动结转
```

学生扣费采用按时间顺序的批次 FIFO 口径：

- 期初余额作为第一笔虚拟批次；
- 后续每条真实充值形成一个新批次；
- 课程按日期和时间顺序扣费；
- 扣费按批次顺序进行，每个批次内部先扣现金，再扣赠送；
- 现金不足时可以形成现金欠费；
- 一节课费用可以拆分到多个批次、多个来源；
- 赠送余额仍可抵扣当前课程费用时，不允许现金余额继续变负；
- 赠送余额不会抵扣现金欠费，后续赠送充值只进入赠送批次；
- 后续现金充值会先抵扣已有现金欠费。

这套口径影响 `学生 -> 费用汇总`、`学生 -> 学生查询`、学生账单/复制图片，以及核心 Excel 中的学生费用汇总。`学生 -> 费用明细` 仍显示每节课的应收单人费用，不显示现金/赠送拆分。

### 学生费用汇总

`学生 -> 费用汇总` 现在是一张完整横向表格，不再依赖展开行查看核心余额数据。主表固定 12 列：

```text
学生姓名
年级
上课次数
课程总费用
上月实际结转
上月赠送结转
本月实际充值
本月赠送充值
本月实际消费
本月赠送消费
本月实际余额
本月赠送余额
```

学生显示范围为：本月有课程记录的学生、本月有真实充值记录的学生、月初现金或赠送余额不为 0 的学生。排序按年级顺序和学生姓名，年级顺序为：

```text
初一 -> 初二 -> 初三 -> 高一 -> 高二 -> 高三 -> 已毕业
```

前端页面固定展示顶部当前选择月份的数据，不再提供“本月 / 迄今为止”范围切换；核心 Excel 仍使用月度汇总口径。

### 充值记录与期初余额

- `学生 -> 充值记录` 只显示本月真实充值、退费、赠送流水。
- 页面表格只展示本月实际充值、本月赠送充值、充值日期和备注等本月流水信息；“上月实际结转 / 上月赠送结转”仅在核心 Excel 导出中保留，不再显示在充值记录 UI。
- 本月实际充值和赠送充值都为 0 的记录不显示，也不算充值记录。
- 新增充值记录使用弹窗。
- `学生 -> 期初余额` 用于系统上线前余额承接，支持新增、编辑、删除、学生筛选和年级筛选。
- 期初余额支持下载导入模板、通过 Excel 导入和导出全部全局余额。
- 期初余额是全局数据，不随顶部月份切换变化。
- 期初余额不进入充值记录，不计入本月充值。

### 学生单价规则与费用明细

`学生 -> 学生单价` 是学生收费规则表，规则唯一标识为：

```text
学生 + 年级 + 科目 + 学生集合
```

学生集合会规范化处理：拆分、去空、去重、排序、统一分隔符。规则候选会从历史课程自动生成，页面只保留 `学生 / 年级 / 科目 / 学生集合 / 单价 / 价格状态 / 备注`。学生、年级、科目、学生集合只读；单价和备注可编辑。价格状态为 `自动 / 手动 / 未设置`。

`学生 -> 费用明细` 中，非 `已上` 课程费用为 0。单人费用右上角用 `自 / 手 / 未` 标识价格状态，并支持勾选后点击 `按规则更新所选费用`。该操作只更新用户勾选的明细，不会自动批量改历史费用。批量按钮工具区与明细表之间保留明确上下间距和分隔，视觉层级更清晰，勾选、禁用和批量更新逻辑不变。

费用明细的考试行和异常行不再使用整行黄色背景；价格来源、异常、负数、未配置及其他业务警告仍通过原有标签和文字颜色表达，不改变费用计算或筛选口径。

### 薪资规则、课时明细与班级管理

`教师 -> 薪资规则` 支持按规则自动计算教师薪资，规则唯一标识为：

```text
老师 + 年级 + 科目 + 学生集合
```

学生集合同样会规范化。规则金额为 `每2小时薪资`，1 课时按 2 小时计算。规则价格状态为 `已设置 / 未设置`。

薪资规则页面默认勾选 `隐藏非在职老师`，按教师档案状态隐藏非在职老师的规则；取消勾选后显示全部规则，规则编辑、保存和按规则更新逻辑不变。

`教师 -> 课时明细` 中，非 `已上` 课程薪资为 0。教师薪资右上角用 `自 / 手 / 未` 标识来源状态，并支持勾选后点击 `按规则更新所选薪资`。独立来源列和学生人数列已删除；备注列展示课程备注内容。手动薪资不会被自动覆盖，除非用户主动批量按规则更新。

`学生 -> 班级管理` 自动汇总历史课程和薪资规则中的班级候选，唯一标识为：

```text
老师 + 年级 + 科目 + 学生集合
```

学生集合会做去空、去重、排序和统一分隔符处理。页面只维护班级名；若班级名存在，家长群课程截图和老师课程截图中学生列会显示为 `班级名：学生`。班级管理默认勾选 `隐藏非在职老师`，只在前端隐藏非在职老师相关班级，取消勾选后可查看全部班级。

### 首页

首页首行机构统计卡（本月课程费、本月应收、今日已上、今日请假和本月消耗）及其容器已完整删除。第一行“常用功能”继续整行展示。宽屏第二行按“正在上的课程、待办事项、人数统计”三列等分，三个外层卡片顶边、底边和标题高度一致；人数统计列内的学员、教师圆环上下排列。第三行课程趋势独占整行并保留日期范围与数据来源。趋势图复用单一 ECharts 实例，容器宽度 100%，绘图区使用紧凑边距和 `containLabel` 从左铺到右，并由单个 `ResizeObserver` 响应侧栏、窗口和断点变化。中屏正在上的课程和待办事项左右并排，人数统计换到下一行且两个圆环左右并排；窄屏依次显示正在上的课程、待办事项、学员统计、教师统计和课程趋势。所有区块不额外请求、不重复渲染且不使用动画；正在上的课程内容区自动占用剩余高度，空状态在区块中居中且不过度撑高，任何断点都不横向溢出。

### 排课与课程总表

`排课 -> 课程总表` 支持 `批量已上`、`批量复制` 和 `批量删除`。批量已上只能处理状态为 `待上` 的课程；如果选中课程中存在非 `待上`，会报错并拒绝整个操作，保留原选中状态。成功后会将选中课程状态改为 `已上`，并按现有规则触发教师薪资匹配和派生缓存失效。每行操作列中的单条 `复制` 按钮已删除，`批量复制` 功能保留。

批量复制弹窗支持整体平移天数，默认 `+7` 天；上方显示原课程，下方显示复制后课程。原课程只读，目标课程可编辑。复制后的课程不继承原教师薪资，而是按新增课程逻辑重新匹配薪资规则。

课程总表日期筛选已统一为日期范围选择器，选择后完整展示 `YYYY-MM-DD → YYYY-MM-DD` 和日历图标；筛选区保留 `昨日 / 今日 / 明日 / 本周 / 下周 / 本月` 快捷按钮。日期控件保留 282px 固有宽度，老师和学生筛选不会覆盖结束日期，中等宽度不足时第一行整体换行。切换顶部月份不会强制重置课程总表的日期筛选。

课程行、新增课程、整周复制等单个日期字段统一使用无图标的单日期输入，右侧不再保留图标空白；点击仍打开单月日期浮层，也可手动键入 `YYYY-MM-DD`，Enter 失焦后沿用原 change/save 流程。无效日期会标记校验错误，日期范围筛选的图标与双日期状态不受影响。

课程状态在浏览态、排课态和下拉候选中复用 24px 紧凑圆角标签，背景色和文字色来自基础数据配色；年级、科目和学生采用同一标签契约。排课状态不附加方形底框；时间与教室为无底色、无胶囊的普通文字选择器，教室普通值无左侧竖线，冲突只使用轻量红色提示。学生单元格垂直居中并保持单行完整标签，点击学生区域后才打开既有选择浮层。备注表头居中，正文左对齐并自然换行，不覆盖相邻列。

浏览态和排课态现在共享同一表头高度、普通课程行高度、单元格上下内边距与固定列宽；切换模式只替换单元格内容，不移动表头和表格。课程字段固定按“老师、日期、星期、时间、教室、状态、年级、科目”排列，序号、老师、日期、星期、时间、教室、状态、年级、科目及复选框水平和垂直居中，学生标签整体单行居中，备注正文保持左对齐换行。排课态老师、状态、时间、教室、年级、科目和学生未点击时只显示当前值，点击后才打开原搜索浮层。

课程总表的老师、学生、教室、状态、年级和科目筛选复用统一可搜索浮层：原筛选框不变为输入框，搜索、清空、结果数、滚动候选和空状态结构一致。完整候选池与实际挂载 DOM 共用同一过滤结果，中文输入法组合期间不误过滤，上屏后立即刷新；0 项时旧节点不再显示，清空恢复全部。老师、学生保留多选，教室、状态、年级、科目保留单选；搜索只更新当前浮层，选择后只刷新课程相关区域，不重新请求 bootstrap 或重绘全页。

排课模式与新增课程弹窗中的时间、教室候选共用“原始值 + 单个右侧状态”结构。可选项使用低强调中性标签，冲突项在悬停、聚焦、激活和选中时仍保留红色语义；候选值不再拼接 `（可选）` / `（冲突）`，选中回填仅显示原始值。无冲突在前、有冲突在后的排序及冲突检查、最终保存校验和 `allow_conflicts` 语义保持不变。

排课模式的老师、状态、时间、教室、年级和科目默认只显示当前文字或标签，不常驻下拉箭头、搜索图标和大号输入边框；点击后才显示统一搜索浮层。教室与时间冲突统一为红色文字，不显示横线、竖线或删除线。排课备注使用语义类限制 `resize: none`，仍可换行并在既有最大高度内滚动。

排课态状态、时间和教室的按钮、文字或标签在现有单元格内部水平和垂直居中，控件外层不再造成视觉偏移；浏览态与排课态仍共享列宽、行高和表格位置。

#### 冲突检查与二级编辑

课程总表冲突弹窗的每一条课程均可通过“处理”打开二级编辑。单选菜单和学生多选菜单都端口化到 `body`，z-index 高于两层弹窗，避免父面板 `overflow` 裁剪和遮罩拦截；局部重绘前主动关闭旧浮层。二级弹窗取消或 Esc 只关闭当前编辑层并返回父冲突列表；保存复用服务端冲突校验，成功后只更新对应课程、本页统计、冲突数量和冲突列表。

冲突编辑中的日期复用新增课程/排课的无图标单日期控件，使用 `data-date-kind="single"` 与日期范围组件的 `data-date-kind="range"` 明确分流；控件可输入 `YYYY-MM-DD` 或打开同款单月日历，且不会为日历图标预留空白。日期变化会先收集当前弹窗所有字段并写回草稿，同步星期，再只重建弹窗区域，使时间和教室候选按新日期重新计算；日期浮层层级高于二级弹窗，关闭浮层不会关闭编辑弹窗，取消或 Esc 仍直接丢弃草稿。学生字段复用排课学生浮层，搜索、已选标签、候选、新增学生输入、取消和确认集中在同一浮层；确认只更新弹窗草稿，最终点击“保存修改”才调用课程 PATCH 和服务端冲突校验。

排课模式中“为老师新增某日课程”按钮已优化响应速度，点击后更快反馈；保存课程后会局部更新课程相关数据，并正确触发后续费用 / 薪资派生数据刷新。课程总表和其他使用表格空状态的页面已统一空状态居中显示，例如无数据时的 `暂无课程记录`。

### 学生/老师档案

- 学生档案显示全部学生，学生状态包含 `已毕业`。
- 年级列显示为 `当前年级`，由 `student_grade_stages` 中的阶段界定时间按当前日期自动判断；无匹配阶段时回退旧 `students.grade` 或 `未设置`。
- 点击学生行的非控件区域会打开独立阶段弹窗，维护 `初一` 至 `高三`、`已毕业` 的起始 / 截止日期；`已毕业` 只需要起始日期。弹窗桌面端双列、窄屏单列，仅正文滚动，保存后只更新当前学员及当前表格行。
- 勾选学生后可使用 `批量修改界定时间`，只写入选定阶段，不影响其他阶段；原 `批量升年级` 按钮已移除。
- 学生入学日期可补齐为该学生第一节课日期。
- 老师档案显示全部老师。
- 老师入职日期可补齐为该老师第一节课日期。
- 学生费用汇总、费用明细、充值记录、期初余额、学生查询、学生单价、班级管理和学生档案的普通行不再按年级使用整行背景；表头、hover、选中、禁用、异常、负数和业务警告继续保留。
- 学生姓名、当前年级、课程科目和课程状态通过共享紧凑标签展示，颜色只属于前端显示层。
- 学生和老师的状态字段默认显示无常驻边框、无箭头的紧凑彩色标签；点击可编辑标签时打开共享页面级搜索选择器，保存成功只更新当前字段，失败恢复原值并显示错误 Toast，只读账号保持不可点击。

### 基础数据

`设置 -> 基础数据` 第一阶段支持维护教室、科目、常用时间和课程状态。基础数据复用 `settings` 存储，不新增专门字典表。候选、标签配色和新增课程选择器统一采用：

```text
lessons 表中当前仍存在课程的 DISTINCT 规范化值
```

老师、学生仍以档案页为准；年级仍保持固定体系。课程删除为物理删除，查询不会包含已删除或归档快照；每次 bootstrap 都直接重新查询当前 `lessons`，不复用旧候选缓存。

状态、学生、年级和科目统一复用 `renderEntityBadge()` 及状态/年级/科目/学生渲染器；颜色映射按 settings 原始值缓存，缺失配置使用稳定默认色。标签 HTML 和颜色变量不会写入 SQLite、API 或 Excel，所有业务数据仍为纯文本。

四类值统一去首尾空格、过滤空值、去重并稳定排序，常用时间额外规范为 `HH:mm-HH:mm`。系统默认数组、settings 自定义数组、旧课程快照和孤立配色 JSON 键都不能反向补回候选；删除某值的最后一条引用课程后，重新进入基础数据页面时，该值从候选和对应配色同时消失，其他仍有课程引用的值保持不变。

教室、科目、常用时间和课程状态四个外层模块按两列排列，中等宽度保持两列，窄屏切换为单列；年级配色卡独占整行。状态、科目和年级的内部配色网格按约 260px 最小项目宽度自动增减列数，不再强制固定 4/3/2 列。每项使用弹性标签预览和两个不收缩颜色组，背景与文字标签、颜色框保持同一行并完整可点；输入颜色通过单次内容区事件委托实时更新预览，保存、恢复默认和 settings 结构不变。

### 前端交互组件

枚举、实体和多选字段复用共享搜索关键字规范化、候选过滤、结果计数、视口定位、向上/向下展开和关闭逻辑。单日期输入与日期范围分别使用 `data-date-kind="single"` 和 `data-date-kind="range"`，互不接管：单日期无图标但支持点击日期浮层和手动输入，日期范围保留图标及开始/结束双值。范围面板左侧快捷列表在自身区域纵向滚动，右侧双月固定，列表滚动位置在面板局部重绘时保留，且不额外注册 wheel 事件。浮层挂载在页面级容器，支持中文输入法组合事件、Esc、外部点击、上下方向键与 Enter；搜索只处理当前浮层既有 DOM，不请求 bootstrap，页面或局部弹窗替换前统一回收端口化菜单和日期浮层，避免旧节点和重复监听。

排课和冲突编辑的学生字段使用同一个草稿选择组件与页面级事件委托。浮层关闭时清空未确认搜索和新增输入，确认后只向隐藏草稿字段派发一次 change；日期导致冲突弹窗局部替换前会先回收单选、多选浮层，避免 body 上残留旧节点。

全站成功与失败消息复用单例 Toast 容器和单个提示节点，默认约 2.6 秒后移除；连续提示会替换内容并重置计时，不依赖动画结束事件。切换页面、进入登录页或退出时主动清理；局部重绘前同步回收可搜索选择器、日期范围面板和临时端口节点，避免旧节点及重复监听。

学生/老师档案与账号状态复用同一套标签、页面级搜索选择器和保存/回滚流程。账号页操作按钮从左侧起排并允许换行；表格使用固定 `colgroup`，滚动容器绘制唯一外框，单元格只绘制内部右/下边，重置密码控件在单元格内层 Flex 中排列，横向滚动只发生在表格容器内。账号状态保存成功仅更新当前行，角色、绑定老师和权限编辑保持原逻辑。

### 图片复制与 UI 优化

- `学生 -> 学生查询` 支持复制图片。
- `教师 -> 课时明细` 支持复制图片。
- 家长群课程截图、老师课程截图的快捷时间已整合进日期范围选择器，不再单独显示“本周”按钮。
- 家长群和老师课程截图的年级、科目标签在详细表格、简洁卡片和 Canvas 中使用相同高度、行高、内边距与居中基线；复制和下载使用同一 Canvas 绘制函数，不会与页面展示出现纵向偏移。详细表格及 Canvas 表头统一显示“状态”，取值统一为规范化 `status`，因此 `已上` 不再显示成旧 `lesson_status` 的“上课”；“只选择”筛选同步按规范化 `已上` 判断。
- 页面可见模式名统一为“详细模式 / 简洁模式”，内部继续使用 `preview / simple` 状态值以兼容已有偏好。简洁卡片固定按“学生、年级、科目”三部分展示完整内容；单个学生姓名标签始终单行完整，不拆字或省略，多个标签按实际宽度整体换行。卡片外层按约 220px 最小宽度自适应增减列数，年级/科目标签可整组自然换行，课程节数位于底部。
- 家长群课程截图个人发送覆盖所有学生；详细模式按个人发送 / 班级发送 / 班级合并发送分区展示卡片，简洁模式按三类发送对象和年级分区展示小矩形，小矩形点击轮流复制截图和文案，复制文案后变绿。老师课程截图复用相同模式、字段和反馈组件。
- 只读账号只要拥有对应页面权限，也可以复制或下载家长群课程截图和老师课程截图；截图生成和导出不属于业务数据修改。
- 侧边栏 emoji 已改为线性图标。
- 多个表格和弹窗做了布局优化。

## 数据导出功能

### 月度核心 Excel

核心导出接口：

```http
GET /api/export/core-workbook.xlsx?month=2026-05-01
```

特点：

- 数据全部来自当前 SQLite / SQL；
- 不依赖 `data/source-workbooks/`；
- 没有源 Excel 也可以导出；
- 生成静态 Excel，不保留原 Excel 公式；
- 不读取源 Excel 模板，不提取原 sheet，不隐藏辅助 sheet；
- 数据排列结构参考原 Excel 的核心业务表；
- 如果该月没有数据，也会导出 5 个空表头 sheet。

固定导出 5 个 sheet，顺序如下：

1. `N月总表`
2. `N月学生费用汇总`
3. `学生费用明细`
4. `充值记录`
5. `薪资汇总`

例如导出 `2026-05-01` 时 sheet 为：

1. `5月总表`
2. `5月学生费用汇总`
3. `学生费用明细`
4. `充值记录`
5. `薪资汇总`

### 核心 Excel 字段来源

`N月总表`：

- 来自 `lessons`；
- 包括授课老师、日期、上课情况、星期、时间、教室、年级、科目、学生、备注、课程状态、教师薪资、学生人数、累计序号等。

`N月学生费用汇总`：

- 来自 `feeDetails(monthKey)` 和 `studentSummary(details, monthKey)` 的后端计算结果；
- 使用与 `学生 -> 费用汇总` 相同的 12 列和批次 FIFO 扣费口径；
- 导出层会过滤学生范围，只保留：
  - 本月课程中出现过的学生；
  - 本月有真实充值记录的学生；
  - 月初现金余额或赠送余额不为 0 的学生；
  - 本月末仍有现金或赠送余额的学生。

`学生费用明细`：

- 来自 `feeDetails(monthKey)`；
- 单人费用、费用来源和课程状态由系统现有费用规则计算，导出为静态值。

`充值记录`：

- 来自 `recharge_records`；
- 只导出真实现金充值、赠送充值、退费等流水；
- `cur_recharge = 0 且 cur_gift = 0` 的记录不视为充值记录；
- 全局期初余额不进入充值记录 sheet；
- 保留“未登记充值提醒”相关列，用于提示本月有课程但没有充值记录的学生。

`薪资汇总`：

- 来自 `teacherSummary(monthKey)` 及现有教师薪资计算逻辑；
- 车费来自 `teacher_travel_fees`，并兼容读取 `teacher_adjustments_monthly` 旧 1-4 周字段；
- 薪资合计为课时合计加当前月份动态周车费。

### 导出文件命名

核心 Excel 文件名格式：

```text
黎明教育_YYYY年M月_核心数据_YYYYMMDD_HHmmss.xlsx
```

示例：

```text
黎明教育_2026年5月_核心数据_20260601_153012.xlsx
```

时间戳由服务器当前时间生成，用于避免多次导出覆盖。文件名通过后端 `Content-Disposition` 返回，前端下载逻辑优先使用后端文件名。

### 批量核心 Excel 与备份

批量导出全部月份接口：

```http
GET /api/export/core-workbooks-all.zip
```

该接口会读取 SQL 中已有月份列表，为每个月调用与单月核心 Excel 相同的工作簿生成逻辑，并打包成 zip。zip 文件名格式：

```text
黎明教育_全部月份_核心数据_YYYYMMDD_HHmmss.zip
```

数据对账页面的备份能力同样以全部月份核心 Excel zip 为内容：

- `GET /api/backups`：读取自动备份设置和历史备份记录，并在访问时检查是否需要执行当日自动备份。
- `PUT /api/backups/settings`：保存自动备份启用状态和每周几执行，默认启用且为每周三。
- `POST /api/backups/run`：手动立即生成一次备份，不受当天自动备份去重限制。
- `GET /api/backups/:id/download`：下载某条成功备份的 zip 文件。

备份文件保存在 `data/backups/`。系统只保留最近 5 次成功备份 zip，第 6 次成功备份生成后会删除最早的成功备份文件和记录；失败记录保留在 `backup_records` 中用于排查。`查看备份记录` 弹窗为桌面端约 80vw、最大 1280px 的大尺寸滚动面板，可查看备份时间、类型、状态、大小、包含月份、文件名和下载按钮。

核心 Excel 单月导出、批量导出全部月份、查看备份记录、下载备份、手动备份和修改自动备份设置都会写入 `operation_logs`，日志只记录文件名、月份数量、状态、大小和操作人，不记录完整磁盘路径。

### 其他导出

当前还保留教师薪资和经营概览等导出接口，例如：

- `GET /api/export/teacher-salary.xlsx?month=YYYY-MM-01`
- `GET /api/export/finance-summary.csv?...`

这些接口沿用各自原有命名和字段逻辑，不受核心 Excel 文件名规则影响。

## 权限说明

当前权限体系由角色权限、账号级覆盖、只读状态和角色预筛选共同组成：

1. **角色范围**
   内置角色主要为老板、教务、小助手、助教和老师。历史 `admin`、`boss`、`finance`、`jiaowu` 等角色代码会映射到当前角色体系。
2. **页面权限**
   角色通过 `role_permissions` 配置可见页面；账号可通过账号级权限覆盖处理特殊授权。侧边栏菜单、子菜单和对应后端接口均按实际页面权限校验。
3. **角色预筛选**
   `role_filter_presets` 保存角色在课程总表、矩阵课表等页面的隐形基础数据池。预筛选不再等同于页面筛选框默认值；用户页面筛选属于二次筛选，最终数据按“角色预筛选 ∩ 用户页面筛选”返回。
4. **绑定老师**
   `user_teacher_bindings` 保存账号与教师档案的多选绑定。绑定老师本身不限制数据，仅在角色预筛选选择“绑定老师”时作为基础数据池来源。
5. **只读状态**
   角色和账号均可配置只读。只读账号不能执行新增、修改、删除、导入、密码重置等写操作；截图复制、截图下载等读取或导出操作仍可使用。
6. **本轮新增功能权限口径**
   学生年级阶段单行保存、批量修改和学生档案批量删除接口归属 `学生档案(studentProfiles)` 页面权限，并继续受只读状态限制；班级管理权限保持不变。家长群截图的预览 / 简洁模式 / 班级合并发送、课程总表 `今日 / 本周 / 下周 / 本月` 快捷按钮、班级管理和薪资规则的 `隐藏非在职老师` 开关都不新增权限。全部月份核心 Excel zip 和备份查看 / 下载 / 设置归属核心导出能力。

### 核心 Excel 权限

核心 Excel、全部月份核心 Excel zip 和备份 zip 包含学生费用、充值记录和教师薪资等敏感信息。当前代码中这些接口归属 `coreExport` 权限域：

- `owner` 和 `admin` 可访问；
- `academic`、`finance`、`teacher` 等非最高权限角色不可访问该接口；
- 具体以 `src/server.js` 中 `apiArea()` 和 `authorizeApi()` 的当前实现为准。

## 已知限制

- 核心 Excel 第一版导出的是静态值，不保留公式。
- 不追求完全复刻原 Excel 的颜色、边框、列宽、行高、冻结表头、打印设置和复杂样式。
- 日期当前按基础单元格值写入，可能表现为文本。
- 核心目标是保证 SQL 最新数据和 5 个核心 sheet 的稳定结构。
- 后续可以继续增强列宽、金额格式、日期格式、冻结表头、样式和打印体验。
- 顶部月份切换已做性能优化：复用学生档案、老师档案、期初余额和规则等全局数据，只刷新当前月份数据，减少全局数据重复请求。
- 后端派生数据使用内存缓存；写入课程、充值、期初余额、学生单价规则、教师薪资规则等数据后会统一清理缓存，避免脏数据。
- `[perf]` 性能日志默认关闭；排查慢请求时可设置 `PERF_LOG=1`，需要同时查看缓存命中与失效细节时可设置 `DERIVED_CACHE_DEBUG=1`。

## 常见问题

### Q：为什么导出的核心 Excel 和原始 Excel 不完全一样？

因为当前核心导出以 SQL 当前数据为准，直接生成静态核心数据表，不依赖原 Excel 模板、公式和辅助 sheet。

### Q：新建月份后没有源 Excel，能否导出？

可以。核心 Excel 导出不依赖源 Excel。SQL 中有数据时导出对应数据；没有数据时也会导出 5 个只有标题和表头的 sheet。

### Q：为什么学生费用汇总里不是所有历史学生？

核心导出和页面只保留本月相关学生：本月上课、本月有真实充值流水，或月初/期末仍有现金或赠送余额的学生。只有 0+0 空充值记录、且没有课程和余额的历史学生不会出现在该 sheet。

### Q：源 Excel 还需要保留吗？

需要。源 Excel 仍可作为历史参考、导入和对账来源，但不是核心 Excel 导出的数据源。

## 仓库与本地文件边界

GitHub 只保留系统运行、部署和维护所必需的源码、脚本、配置模板和文档：

- 应上传：`src/`、`public/`、`scripts/`、`docs/`、`deploy/`、`Dockerfile`、`docker-compose.yml`、`package.json`、`.env.example`、`.gitignore`、`.dockerignore`。
- 不上传：`data/*.sqlite*`、`data/backups/`、`data/source-workbooks/`、`data/uploads/`、`data/.audit-temp/`、压缩包、Python 缓存、设计系统原始目录、本地 AI/设计工具上下文。
- 本地保留但不提交：`AUDIT-REPORT-*.md`、`AUDIT-PHASE*.md`、`data/audit_*.md`。这些报告可能包含业务数据或审计线索，只用于本地排查。
- `.impeccable.md`、`CLAUDE.md` 属于本地工具说明，不再跟随 GitHub 发布。

数据库和源 Excel 是业务数据，不走 GitHub。上线代码通过 `git push` / `git pull` 更新；线上数据库通过 Docker volume 和备份脚本维护。

Excel 工作簿权威同步：

```bash
# 把 data/source-workbooks/ 下的多个月一次性同步进数据库（含同步前整体备份 + 每月单独备份）
node scripts/sync_source_workbooks.js --months=2026-02-01,2026-03-01,2026-04-01,2026-05-01

# 复核源表 vs 系统：默认输出到 data/audit_source_vs_summary.md
node scripts/audit_source_vs_summary.js

# 在临时 SQLite 副本里模拟重放，不动正式库
node scripts/audit_source_vs_summary.js --simulate-sync=2026-02-01,2026-03-01,2026-04-01,2026-05-01
```

跨月课程（例如 2026年5月.xlsx 5月总表中日期为 4-30 的行）会被识别为 `month_key=2026-04-01`，归入 4 月费用汇总。审计脚本同时按行的实际日期月份做跨工作簿去重。

服务器更新：

```powershell
# 在本机 PowerShell 一键提交、推送 GitHub，并让服务器拉取重建
.\scripts\publish-and-update.ps1 -Message "本次更新说明"
```

脚本会先运行基础语法检查，再提交本地变更、推送 `origin/main`，最后通过 SSH 进入服务器执行 `scripts/server-update.sh`。如果本地存在 `scripts\黎明教育.pem`，脚本会自动用它登录；也可以用 `-KeyPath <pem路径>` 指定其他密钥。`*.pem` 已加入忽略规则，不要把服务器密码或私钥内容提交到 GitHub。

也可以在服务器上手动更新：

```bash
cd /root/liming-course-system
sh scripts/server-update.sh
```

该脚本会自动执行“备份 SQLite -> 拉取 GitHub -> 重建 Docker -> 检查 HTTPS -> 检查数据库计数”。服务器文件清点使用：

```bash
sh scripts/server-inventory.sh
```

如果源 Excel 的「教师薪资汇总」sheet A 列是未计算出来的动态公式（导入时报无法识别教师姓名），先用 Excel/WPS 打开并保存一次；或临时用 `--teacher-order 老师1,老师2,...` 指定第 3 行起的老师顺序。

数据库文件、上传文件、备份都在 `data/` 下：

- `data/liming-local.sqlite`：主库（WAL 模式）
- `data/backups/`：每次审计/月份强删/审计修复前自动留底的快照
- `data/uploads/`：xlsx 对账时上传的文件

运行时可用环境变量：

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `PORT` | `5177` | HTTP 服务端口 |
| `DATA_DIR` | `data/` | 数据目录，Docker 中为 `/app/data` |
| `DB_PATH` | `DATA_DIR/liming-local.sqlite` | SQLite 主库路径 |
| `SESSION_COOKIE_SECURE` | `true` | HTTPS 正式部署保持 `true`；纯 HTTP 临时试用才改为 `false` |

Docker 线上环境中，代码在 `/root/liming-course-system`；数据库通过 Docker 数据卷挂载到容器内的 `/app/data/liming-local.sqlite`。更新代码不会覆盖数据卷，但不要执行 `docker compose down -v` 或 `docker volume prune`，除非已经确认要销毁数据库。

## 数据模型

| 表 | 用途 |
| --- | --- |
| `lessons` | 月度课程总表，每行一节课，`student_names` 用顿号分隔多个学生，`status` 是规范化后的状态机字段，`teacher_salary` 是手填课时费 |
| `fee_overrides` | 单节课、单个学生的手动单价覆盖（最高优先级） |
| `student_opening_balances` | 全局学生期初余额，记录系统上线前现金余额和赠送余额；不属于某个月，不计入充值流水 |
| `student_pricing` | 学生 + 年级 + 科目 + 学生集合的学生单价规则候选；金额为 0 表示未设置 |
| `pricing_standards` | 年级×班型（1对1/1对2/1对3/1对多）的标准单价 |
| `students` / `teachers` | 档案，软删除字段 `status` 标记在读/在职/离校/离职 |
| `student_grade_stages` | 学生年级阶段界定时间，唯一键为 `student_name + stage`；普通阶段保存起止日期，`已毕业` 可只保存起始日期，旧 `students.grade` 保留为 fallback |
| `recharge_records` | 学生月度真实充值、赠送充值、退费流水；`cur_recharge = 0 且 cur_gift = 0` 不视为真实充值记录 |
| `teacher_salary_rules` | 老师 + 年级 + 科目 + 学生集合的教师薪资规则，金额为每 2 小时薪资 |
| `class_groups` | 老师 + 年级 + 科目 + 学生集合的班级名配置，学生集合规范化后唯一 |
| `staff` / `staff_salary_monthly` | 非教师员工档案与月薪流水 |
| `teacher_adjustments_monthly` | 教师每月车贴兼容汇总，保留旧 1-4 周字段和备注 |
| `teacher_travel_fees` | 教师每月各周车费明细；周一至周日为一周，按周日归属月份生成动态周列 |
| `operating_expenses` | 房租/水电/食材等运营开销 |
| `audit_logs` / `audit_ignores` | 审计问题流水与忽略名单 |
| `audit_events` | 操作审计日志，记录谁改了课程、充值、价格、薪资、账号等关键数据 |
| `operation_logs` | 面向管理员的操作日志，按北京时间展示课程、月份、账号、权限等关键操作的可读描述 |
| `roles` / `role_permissions` | 角色定义、只读属性和页面权限配置 |
| `role_filter_presets` | 角色级页面预筛选，支持动态日期、绑定老师和指定老师等隐形基础数据池规则 |
| `users` | 登录账号、角色、兼容教师姓名、只读覆盖、页面权限覆盖开关和账号状态 |
| `user_teacher_bindings` | 账号与教师档案的多对多绑定关系，仅作为账号资料和动态预筛选来源 |
| `user_page_permissions` / `user_filter_presets` | 账号级页面权限覆盖及历史账号预筛选兼容数据 |
| `parent_message_greetings` | 家长群课程通知的发送对象称呼、全局尾句和完整文案缓存 |
| `course_notice_completion_records` | 家长群课程通知完成记录，按“年级 + 科目 + 学生名单 + 老师 + 日期 + 时间”唯一识别已发送课程 |
| `settings` | 当前选定月份、基础数据自定义项和配色等系统配置；教室、科目、常用时间、课程状态候选只读取当前 `lessons` 记录生成的 `used_lesson_lookups`，不会由 settings 或孤立配色键反向补回 |

所有按月数据用 `month_key='YYYY-MM-01'` 分区，月份切换不影响历史数据。

权限角色：`Qing(owner)` 和兼容的管理员角色拥有最高权限；教务、小助手、助教和老师的页面范围由角色权限或账号级覆盖决定。老师绑定不再作为天然数据限制；若角色预筛选选择“绑定老师”，系统会将当前账号绑定老师作为隐形基础数据池，用户页面筛选只能在该范围内继续筛选。老师档案删除时会自动停用关联的老师登录账号，但账号和绑定记录仍保留，便于恢复、重置密码和审计追溯。

## 维护参考：模块与审计细节

### 模块总览

| 模块 | 入口 | 核心职责 | 主要数据表 |
| --- | --- | --- | --- |
| 排课 | 线性图标 | 月度课程录入、状态管理、冲突检测、矩阵课表、家长群课程通知截图 | `lessons` / `parent_message_greetings` / `course_notice_completion_records` |
| 学生 | 线性图标 | 费用汇总、费用明细、充值记录、期初余额、学生查询、学生单价、班级管理、学生档案 | `students` / `student_opening_balances` / `recharge_records` / `student_pricing` / `class_groups` / `fee_overrides` |
| 教师 | 线性图标 | 薪资汇总、车费明细、课时明细、薪资规则、教师档案、xlsx 导出 | `teachers` / `teacher_salary_rules` / `teacher_adjustments_monthly` / `teacher_travel_fees` |
| 运营 | 线性图标 | 员工档案与薪资、日常开销、月份生命周期 | `staff` / `staff_salary_monthly` / `operating_expenses` |
| 经营概览 | 线性图标 | 财务汇总、环比、分布、6 月趋势、资产负债 | 跨表只读 |
| 设置 / 数据对账 | 线性图标 | 标准单价、内部审计、xlsx 对账、档案 CRUD | `pricing_standards` / `audit_logs` / `audit_ignores` |

---

### 1. 排课

核心数据表 `lessons`，每行 = 一节课，多个学生用顿号分隔写在 `student_names`。

| 子功能 | 实现 | 说明 |
| --- | --- | --- |
| 月度总表 | 表内直接编辑 | 字段：授课老师 / 日期 / 状态 / 时间 / 教室 / 年级 / 科目 / 学生名单 / 课时费 / 备注 |
| 状态机 | 权威字段 `status` | 取值：待上 / 已上 / 请假 / 试课 / 考试 / 未缴费；`legacyStatusFields` + `deriveStatus` 与旧 xlsx 的 `lesson_status` / `course_status` 双向同步 |
| 批量复制课程 | `POST /api/lessons/copy` | 接受 `pairs:[{source_id, target_date}]` 或 `(source_lesson_ids[] × target_dates[])` 笛卡尔积；单次 ≤ 200 行；目标日期按 `MAX(sort_order)+1` 追加，状态重置为「待上」 |
| 冲突检测 | `scheduleConflicts(monthKey)` | 同日同时段同老师 / 教室 / 共同学生 → `teacher` / `classroom` / `student` 三类；时间段无法解析 → `invalid_time` |
| 矩阵课表 | 全局课程数据按日期范围展示 | 提供时间课表、老师课表、教室课表三视图，保留日期范围、重置和视图切换；不受顶部月份限制，冲突检查入口已迁移到课程总表 |
| 家长群课程截图 | 页面「排课 / 家长群课程截图」+ `/api/course-notice` | 按日期范围直接读取数据库课程，支持个人发送、班级发送、班级合并发送、详细模式（内部兼容 `preview`）、简洁模式、修改称呼、复制文案、复制/下载课程截图、完成打勾和清除所有打勾记录 |

#### 1.1 家长群课程截图与文案生成

用于教务每周或每阶段向家长群发送课程安排，不再需要手动上传 Excel。

| 能力 | 说明 |
| --- | --- |
| 筛选课程 | 每次打开默认本周一到本周日；选择起始日期、终末日期和“只选择已上”后，页面按规范化 `status=已上` 自动刷新发送对象 |
| 发送对象 | 1V1 学生会生成“个人群”，并展示该学生在日期范围内的所有课程；1V2 及以上仍按班级生成“班级群”，避免混入其他班级 |
| 班级合并发送 | 人数大于 1 的同一班级学生集合在当前日期范围内存在多个不同学科课程时，会额外生成 `班级合并发送` 对象；只有一个学科的班级仍按普通班级发送处理 |
| 班级唯一标识 | 使用 `年级 + 科目 + 标准化学生完整名单 + 授课老师`，学生名单去空格、排序后用顿号拼接；老师不同会被识别为不同班级 |
| 课程截图 | 前端 canvas 绘制简洁课程表，第一行居中显示“课程通知”，表格单元格文字居中，列宽按内容自适应，可复制到微信或下载 PNG；截图使用 `--shot-*` 专用变量跟随当前 `data-palette`，但不跟随暗色主题变黑 |
| 截图视觉 | 课程截图仅保留标题区域 + 课程表格；不显示发送对象副标题、节数徽标和底部说明行；表头使用浅品牌底，正文仅按行隔行变色，时间 / 年级 / 科目只做文字强调 |
| 文案生成 | 每个发送对象独立称呼 + 全局统一尾句，默认尾句为“这是我们本周的上课安排哦[玫瑰]”；修改称呼或尾句后文案即时更新 |
| 称呼持久化 | 称呼、发送对象信息、尾句和完整文案保存到 `parent_message_greetings`，下次进入自动恢复 |
| 完成标记 | 复制截图成功后写入 `course_notice_completion_records`，发送对象变为淡绿色并在课程区域最上层显示大对勾；右侧状态显示“✓ 该发送对象已完成”；刷新后如果该对象全部课程已完成，会自动恢复打勾状态 |
| 清除记录 | 页面提供“清除所有打勾记录”，仅清空完成记录，不影响称呼和文案 |
| 页面体验 | 切换到其他页面时会自动回到顶部，避免继承课程通知页或其他长页面的滚动位置 |

#### 1.2 课程字段行级编辑

课程总表字段编辑从"改一个字段 → 整页重渲染"升级为按影响范围分级局部更新，由 `FIELD_TIERS` 配置表驱动。

| 档位 | 含义 | 覆盖字段 | 更新粒度 | 额外操作 |
| --- | --- | --- | --- | --- |
| A | 纯本行展示 | `notes`、`grade`、`subject` | 只更新当前单元格 | — |
| B | 跨行排课 | `teacher_name`、`date`、`time_slot`、`classroom`、`student_names`、`status` | `#lessons-tbody` 局部重绘 | 乐观更新 `state.lessons`，PATCH 失败回滚 + `alert`；重跑月度冲突数据并更新课程总表当前筛选冲突按钮 |
| C | 经营/费用汇总 | `teacher_name`、`date`、`student_names`、`status`、`grade`、`subject`、`teacher_salary` | 标记 `dirtyFlags` key，不刷新当前页 | 进入经营概览 / 费用汇总 / 教师页时 `consumeDirty` 触发 `load()` 重拉 |

**关键约束**：

| 约束 | 实现方式 |
| --- | --- |
| 事件绑定 | `contentEl` 上一次性 `change` 事件委托（`lessonFieldDelegatedBound` 守卫），不再 `forEach` 逐行绑定 |
| B 档乐观更新 | PATCH 发出时立即 `patchLessonInState`，失败回滚并 `alert` |
| 冲突检测 | B 档只调 `GET /api/schedule-conflicts?month=` 刷新月度冲突数据，不调 `/api/bootstrap` 和 `/api/lessons-range`；课程总表冲突按钮按当前筛选结果复用同一时间重叠判断口径 |
| 滚动位置 | 捕获 `.table-wrap` 的 `scrollTop`/`scrollLeft`，双 `requestAnimationFrame` 恢复 |
| 未提交草稿 | `captureLessonDrafts` 快照 DOM 值与 state 不一致的单元格，渲染后 `restoreLessonDrafts` 回填 |
| 焦点恢复 | 按 `data-row-id` + `data-field` 重新定位，不按 `rowIndex` |
| Warnings 展示 | PATCH 返回的 `warnings` 缓存到 `lessonWarningsMap`，渲染时行尾 ⚠️ 图标 + `title` tooltip，行加 `has-warnings` 类 |
| tbody 重绘后自定义控件 | `reRenderLessonsTbody` 末尾补调 `enhanceCustomSelects` + `enhanceCustomDateInputs`，防止下拉框和日期选择器退化为原生组件 |

---

### 2. 学生

#### 2.1 学生费用来源（`unitPriceFor`）

`feeDetails` 把每节课按学生展开成明细行，费用来源只返回三种：`自动`、`手动`、`未设置`。

| 场景 | 单人费用 | 来源 |
| --- | --- | --- |
| 非 `已上` 课程 | 0 | 自动 |
| `已上` 且命中有效学生单价规则，当前费用等于规则单价 | 当前费用 | 自动 |
| `已上` 且命中有效学生单价规则，当前费用不等于规则单价 | 当前费用 | 手动 |
| `已上` 但没有有效学生单价规则，或规则单价为 0 | 当前费用 | 未设置 |

有效学生单价规则必须精确匹配：

```text
学生 + 年级 + 科目 + 学生集合
```

学生集合会拆分、去空、去重、排序并用顿号统一拼接。旧的空年级 / 空学生集合的 `学生 + 科目` 规则不再参与匹配；启动时会把这些旧规则曾经动态影响的已上课程费用补写到 `fee_overrides`，且不覆盖已有手动覆盖价。

当前费用仍优先读取 `fee_overrides`，没有覆盖价时沿用 `pricing_standards` 的标准价作为当前账面费用；点击“按规则更新所选费用”才会把勾选明细写入 `fee_overrides`，未勾选历史费用不会被批量清洗。

#### 2.2 子功能

| 子功能 | 实现 | 说明 |
| --- | --- | --- |
| 学生费用汇总 | `studentSummary` | 详见下文 [金额结算口径](#金额结算口径) |
| 充值记录 | 弹窗新增 + upsert | 只保存真实 `cur_recharge / cur_gift` 流水；0+0 不算充值记录；期初余额不写入 `recharge_records` |
| 期初余额 | `student_opening_balances` | 全局账户初始化余额，不随顶部月份变化；系统起始月作为第一笔虚拟批次参与余额计算 |
| 自动结转 | `previousCarryOverBalances()` | 有上一有效月时自动使用上一月月末余额；没有上一有效月时读取全局期初余额；不再提供手动“从上月结转”入口，也不把期初余额当作本月充值 |
| 学生查询 | `renderStudentQuery` | 选中学生 → 当月汇总 + 当月明细 + `studentHistoryRows` 跨月历史对比 |
| 学生单价规则 | `student_pricing` | 进入页面时根据历史课程自动生成 `学生 + 年级 + 科目 + 学生集合` 候选；学生、年级、科目和学生集合只读，只有单价和备注可编辑；0 表示未设置 |
| 价格重算 | `POST /api/pricing-recompute` | 删除当月该学生该科目所有 `fee_overrides`（让单价回到专享/标准价）；变更前后总价写入审计日志 |

---

### 3. 教师

| 子功能 | 实现 | 说明 |
| --- | --- | --- |
| 薪资汇总 | `teacherSummary(monthKey)` | 输出：有效课时数 / 课时合计 / 动态周车费 / 备注；车费金额只读，备注可编辑 |
| 车费明细 | `teacher_travel_fees` + `teacher_adjustments_monthly` | 可编辑老师每月各周车费；前端不显示备注列；保存写入操作日志，并同步旧 1-4 周兼容字段 |
| 课时明细 | 选老师 → 当月课程明细 | 展示课程备注列；表底显示合计课时费，支持复制图片和按规则更新所选薪资 |
| 薪资规则 | `teacher_salary_rules` | 维护老师 + 年级 + 科目 + 学生集合的课时薪资自动匹配规则 |
| xlsx 导出 | `GET /api/export/teacher-salary.xlsx` | 由 `xlsxBuffer` 自实现拼包，无第三方依赖 |

---

### 4. 运营

| 子功能 | 实现 | 说明 |
| --- | --- | --- |
| 员工档案 | `staff` | 六类：教学主管 / 教务主管 / 小助手 / 做饭阿姨 / 前台 / 其他；有薪资历史 → 软删（status = 离职），无历史 → 硬删 |
| 员工薪资 | `staff_salary_monthly` + `ensureStaffSalaryRows` | 访问月份时 `INSERT OR IGNORE` 把所有「在职/暂停」员工 seed 进当月；初值取档案 `base_salary`，`bonus / deduction` 默认 0 |
| 日常开销 | `operating_expenses` | 按 `category` 分类、`expense_date` 时间筛选、`vendor` / `notes` 模糊搜索 |
| 创建月份 | `POST /api/months` | 从最早数据月起一路 `ensureCarryOver` 到目标月，补全所有中间月份的余额结转 |
| 删除月份 | `DELETE /api/months/:key?force=1` | 有数据时返回 `blocked: 'has_data'` + 各表行数；UI 弹出二次确认弹窗，需手输 `month_key` 字符串才能解锁删除按钮；执行时清空 `lessons` / `recharge_records` / `teacher_adjustments_monthly` / `staff_salary_monthly` / `operating_expenses`，并写入 `pre_month_delete` 备份 |

---

### 5. 经营概览

入口：`financeBase(range)`，输出 10 项核心指标。

#### 5.1 核心指标（`financeBase`）

| 指标 | 含义 |
| --- | --- |
| `revenue` | 收入（按学生当月实付占比认现，详见金额口径） |
| `gift_consumption` | 赠送余额消耗 |
| `teacher_cost` | 教师课时费 |
| `transport_cost` | 教师车贴 |
| `operating_cost` | 员工薪资 + 日常开销 |
| `gross_profit` | 毛利 = `revenue` − `teacher_cost` − `transport_cost` − `operating_cost` |
| `gross_margin` | 毛利率 |
| `cash_in` | 当期现金充值 |
| `gift_issued` | 当期赠送发放 |
| `net_cash_flow` | 净现金流 |

#### 5.2 衍生分析

| 板块 | 实现 | 说明 |
| --- | --- | --- |
| 环比 | `previousEqualRange(range)` | 取相邻等长区间 |
| 分布 | `financeBreakdowns` | 年级×收入、科目×收入、班型×收入×毛利率、Top 10 学生消费、老师 ROI（贡献/课时费）、低余额名单（实际余额 < 平均单次课费）、未缴费课时清单；收入拆分统一引用 `allocated_revenue`，与顶部收入口径一致 |
| 6 月趋势 | `financeTrend6m` | 以 `range.end` 所在月为锚，往前 6 个月，每月独立跑一次 `financeBase(monthRange)` |
| 资产负债 | `balance_sheet` | 四档拆分：`total_actual_balance`（月末沉淀正现金）/ `total_gift_balance`（月末赠送余额）/ `unpaid_lesson_receivable`（`状态=未缴费` 的课时金额）/ `account_debt_receivable`（学生 `actual_balance` 负值合计，即已上但欠款）；`accounts_receivable` = 按学生取 `unpaid` 与 `debt` 的最大值后求和，避免双重计入 |
| CSV 导出 | `GET /api/export/finance-summary.csv` | 当期快照 |

---

### 6. 设置 / 数据对账

#### 6.1 子功能

| 子功能 | 实现 | 说明 |
| --- | --- | --- |
| 标准单价 | `pricing_standards` | 年级 × 班型 → 单价；高/初年级班型档位略有差异（见 `priceBucket`） |
| 内部审计 | `internalAudit(monthKey)` | 检测项见下表；离校 / 已流出学生不参与姓名相似 / 孤儿学生单价规则检查 |
| xlsx 对账 | `runNodeXlsxAudit` | 上传 xlsx「N月总表」，按 `(date, teacher, time_slot)` 三元组比对每节课字段差异，输出可一键回填的 patch，并统计源文件新增、系统多余和字段变更 |
| 核心导出与备份 | `coreWorkbookSheets` / `backup_records` | 支持当前月份核心 Excel、全部月份核心 Excel zip、手动备份、每周自动备份、最近 5 次成功备份保留和历史备份下载 |
| 审计修复 | `applyAuditIssues` | 批量应用 patch；CRITICAL 默认拒绝，需 `confirm_critical=true` |
| 问题去重 | `auditIssueKey` + `visibleAuditIssues` | 用 `issue_key`（来源/类型/实体/字段/前后值的拼串）做主键，相同问题重复出现时复用同一条 |
| 忽略列表 | `audit_ignores` | 用同一 `issue_key` 持久化已忽略问题，跨审计运行保留，UI 侧显示「已忽略」分组 |
| 档案管理 | 教师 / 学生 / 员工 CRUD | 软删除策略统一 |

#### 6.2 源头对账与 internal-only

`internal-only` 指：系统数据库中存在，但当前 Excel 源文件中不存在的课程记录。

当前设计原则：

- 导入阶段不会自动删除 `internal-only` 课程。
- 对账阶段会识别并展示这类差异。
- 这类问题标记为 `HIGH / type=internal-only`。
- 页面顶部统计会显示“系统多余”数量。
- HIGH 分组默认展开，便于用户优先发现。
- 常规“一键修复”不会自动处理 `internal-only`，避免误删课程。

不自动删除的原因：源文件缺少一条课不一定等于课程应该删除，也可能是 Excel 漏填、源文件覆盖日期不完整、匹配规则变化或人为临时录入。因此系统先展示差异，由用户确认。如需清理 `internal-only`，必须使用独立的 dry-run 预览和二次确认流程，按明确的 lesson id 处理，不能在导入阶段自动删除。

源头对账相关日志：

```text
[reconcile][course records][summary]
[reconcile][course records][internal-only]
```

这些日志用于确认：

- Excel 原始候选行数量；
- 有效课程数量；
- 跳过行原因；
- 源文件有、系统没有的 `source-only`；
- 系统有、源文件没有的 `internal-only`；
- 两边都有但字段不一致的 `changed`；
- 重复匹配组和无法唯一定位的课程。

#### 6.3 对账验证流程

基础语法检查：

```bash
node --check src/server.js
node --check public/app.js
```

建议回归：

1. 在“设置 / 数据对账 / 源头对账”导入或运行 `2026年4月.xlsx`，确认扫描课程为 131。
2. 导入或运行 `2026年5月.xlsx`，确认扫描课程为 158。
3. 检查页面顶部统计：导入前课程、导入后课程、系统多余、源文件新增、字段变更。
4. 检查 HIGH 分组中是否存在 `internal-only`，并确认不会在常规“一键修复”中被自动删除。
5. 确认月度核心 Excel 导出仍能生成 5 个核心 sheet。

#### 6.4 源头对账维护注意事项

- 不要把 Excel 中任意有值的行都当作课程。
- 不要只凭某个业务列残留值判断课程有效。
- 不要让扫描函数和导入函数使用两套不同的有效行规则。
- 不要在导入阶段自动删除 `internal-only`。
- 不要为了让数量一致而粗暴去重。
- 不要跨月份删除课程。
- 如需处理 `internal-only`，必须通过带 dry-run 和二次确认的独立功能，并只按当前月份、明确 lesson id 处理。

#### 6.5 内部审计检测项

| 类型 | 严重度 | 触发条件 |
| --- | --- | --- |
| `grade_inconsistency` | CRITICAL | 同一学生同月出现在多个年级 |
| `missing_grade` | MEDIUM | 学生有当月课程，但 `students.grade` 为空（自动建议最后一节课的年级） |
| `teacher_typo` | WARN | 两个老师姓名 Levenshtein 距离 ≤ 1（去空格后比较） |
| `student_typo` | WARN | 两个学生姓名 Levenshtein ≤ 1，且共享某个已知年级或一方年级缺失，且双方均非离校 / 已流出 |
| `orphan_pricing` | WARN | `student_pricing` 行历史从未在任何 `lessons.subject` 中出现，且学生未离校 |
| `zero_custom_pricing` | HIGH / MEDIUM | `custom_price ≤ 0`；当月有非试课节课命中 → HIGH，否则 MEDIUM |
| `price_outlier` | WARN | 学生单价规则相对标准价偏离 ≥ 30% |
| `price_zero` | HIGH | 有效课时（已上 / 未缴费）单价 = 0 |

---

### 通用基础设施

| 项 | 说明 |
| --- | --- |
| xlsx / zip 自实现 | 服务端 `unzipXlsx` / `zipStore` 直接处理 ZIP 文件结构，无 `xlsx` / `archiver` 依赖；前端 `zipStoreFiles` 镜像同实现，用于客户端 PNG 打包 |
| 审计日志去重 | 用 `issue_key` 做去重 key，相同问题再次出现时只更新 `run_at`，不新建条目 |
| 多月结转链 | `ensureCarryOverChain`：进入任何月份页面前，自动从最早数据月推导到当前月，确保中间月份的结转记录齐全；上游充值或费用补录后，`refreshCarryOverAfter` 会清理已经失效的下游自动结转记录，避免旧欠款继续滚到后续月份 |
| 设计系统 | `public/styles.css` 维护品牌 token、语义别名、字体工具类、亮色侧栏、蓝黑暗色后台，以及课程截图专用 `--shot-*` 浅色家长版变量 |
| 主题切换 | 左侧导航底部切换亮色 / 暗色，`localStorage` key 为 `liming:theme`，默认跟随系统 `prefers-color-scheme`；暗色模式采用近黑 / 蓝黑基底 |
| 配色方案 | 在 `设置 -> 外观设置` 中选择 `data-palette`，`localStorage` key 为 `liming:palette`，默认 `liming-blue`（品牌色 `#002147`）；明暗主题与配色方案互不混用，配色方案主要影响品牌色、按钮、选中态、截图强调色；黑白亮色以纯白/黑色为主、暗色以近纯黑/白色为主，普通品牌强调不使用彩色，错误和负数保留必要红色语义 |
| 矩阵课表 | 支持时间课表、老师课表、教室课表三种视图；三种视图均使用矩阵页日期范围内的全局课程数据，不受顶部月份限制；老师/教室矩阵的首列、日期列和课程卡片已加宽，课程字段可自然换行，横向滚动限制在矩阵容器内；三视图继续局部切换，冲突检查 UI 已迁移到课程总表 |

## 维护参考：界面与交互历史

以下内容仅用于追溯旧版本设计与回归背景；若与前文“功能说明”或最新迭代说明不同，以前文当前行为为准。

### 课程总表与排课功能优化

- 课程总表默认展示本周课程，日期筛选统一为带左侧快捷时间和右侧双月日历的日期范围选择器；筛选区旁保留轻量快捷按钮 `今日 / 本周 / 下周 / 本月`，快捷时间按真实自然日期计算。
- 新增“排课模式”，支持在老师 + 日期课程块后点击加号，快速新增“待上”课程，不影响原有行内编辑能力；新增按钮响应已优化，保存后局部更新课程数据并触发费用 / 薪资派生刷新。
- 将“排课模式 / 整周复制 / 新增课程 / 批量已上 / 批量复制 / 批量删除”统一放到课程表格上方工具栏，原有整周复制和新增课程逻辑继续复用。
- 课程总表已删除前端“学生人数”列；复选框右侧新增仅用于当前可见行的连续“序号”列，业务“累计序号”继续保留在最右侧。浏览态和排课态的日期列采用相同字体、字号、字重、行高、对齐和颜色。
- 工具栏固定顺序为“开始/结束排课、 新增课程、整周复制、批量复制、批量已上、批量删除、冲突数量”；新增课程使用白底样式，冲突为 0 使用灰色中性样式。
- 课程总表筛选统计与重置按钮贴近筛选卡片最右侧；整周复制弹窗为桌面端约 84vw、最大 1360px 的大尺寸面板，0 冲突提示使用灰色中性状态，有冲突时继续醒目提示。
- 表格新增复选框选择列，支持单选、全选、半选状态、批量已上、批量复制和批量删除。
- 表格空状态文案统一居中，课程总表无数据时 `暂无课程记录` 会居中显示，并复用于多个表格空状态页面。
- 修正课程总表统计口径：标题下方显示当前月份总览，统计卡片显示当前筛选结果。
- “新增课程”改为弹窗式新增，避免直接生成空课程。
- 新增课程弹窗中，老师、时间、教室、年级、科目、学生均支持选择已有值或手动填写。
- 新增课程弹窗的在读学生列表支持中文姓名实时搜索，展示已选择人数和当前结果人数；过滤、清空搜索和中文输入法组合输入均保留既有勾选，Enter 不提交表单。
- 下拉候选值统一来自 `lessons` 表中仍存在的真实课程记录；相关课程全部删除后，对应选项会自动从下拉中消失。

### 已下线周课表与学生档案样式优化

- 历史 `排课 -> 周课表` 页面曾支持按“授课老师 + 日期”连续分组显示；该页面当前已下线，旧路由会回到课程总表 / 矩阵课表。
- `学生 -> 学生档案`：年级分组行（如“初一”“初二”“高三”）优化为白底、居中、加粗、更醒目，并去掉额外明显的上下细线；边框 / 分隔线保持和普通学生行一致，仅影响学生档案年级分组行，不影响普通学生行和其他表格。

### 页面功能补充

#### 学生查询、课程批量复制与基础数据

学生查询页面优化：

- `学生 -> 学生查询` 恢复并保留 `月份汇总` 表。
- 页面顶部只保留 8 个核心数据卡，桌面端固定为 `2 行 × 4 列`。
- 8 个核心数据为：有效上课次数、课程费用、开始日期前剩余现金、开始日期前剩余赠送、期间充值现金、期间充值赠送、结束日期后剩余现金、结束日期后剩余赠送。
- 已移除重复的“期间消费情况”4 个统计卡。
- 余额逻辑沿用系统现有口径：充值分现金和赠送，消费时先扣现金、再扣赠送。
- “全部月份 / 日期范围”切换仅更新 8 个统计卡、月份汇总、课程明细和导出/复制状态，页面标题、学生选择、切换按钮、日期控件和滚动位置保持稳定；相同学生与有效范围使用页面级缓存，不重复请求账单数据。

课程总表批量操作：

- 页面位置：`排课 -> 课程总表`。
- 删除了重复的 `当前可见 X 节` 文案，只保留筛选区的 `已筛选 X / 共 X 节`。
- 工具栏保留 `批量复制`，并新增 `批量已上`。
- `批量已上` 只能将状态为 `待上` 的课程标记为 `已上`；如果选中课程中包含非 `待上`，会报错并拒绝整个操作，原选中状态保留。
- `批量已上` 成功后清空选中状态，并按课程状态更新的现有规则触发教师薪资匹配和缓存失效。
- 批量复制支持选择多节课程，默认整体平移 `+7` 天。
- 弹窗展示原课程和复制后的目标课程；原课程只读，目标课程可编辑。
- 确认后新增课程，原课程不变。
- 批量复制弹窗已放大，支持横向 / 纵向滚动；弹窗标题栏支持拖动，关闭后位置重置。
- 复制后的教师薪资不继承原课程，仍按当前新增课程 / 薪资规则逻辑处理。
- 课程总表每行操作列已删除单条 `复制` 按钮，避免和批量复制重复。

学生档案页面优化：

- 页面位置：`学生 -> 学生档案`。
- 学生状态和年级选项新增 `已毕业`，且 `已毕业` 纳入非活跃学生状态判断。
- 学生档案当前采用 `当前年级 + 年级阶段界定时间`：点击学生行打开阶段界定弹窗，在独立滚动区查看或编辑 `初一` 至 `高三`、`已毕业` 的阶段起止日期，主表不再插入展开行。
- `批量修改界定时间` 取代旧 `批量升年级`，只写入选中学生的某一个阶段，不直接批量改学生档案年级。
- `已毕业` 阶段只需要起始日期；普通阶段需要起始日期和截止日期。
- 学生入学日期为空时，默认显示该学生第一节课日期；该默认日期只在显示层派生，不写回数据库，不覆盖人工填写日期。
- 学生姓名在表格中为只读文本，不能在普通学生行内直接编辑；新增学生仍通过独立新增弹窗录入姓名。
- 学生档案普通行更紧凑，列宽通过 `colgroup` 固定，表格内容较多时在表格容器内横向滚动，不撑坏页面整体布局。
- 学生档案表格已删除最后一列 `操作` 和单行删除按钮；删除学生档案通过工具栏 `批量删除` 完成。
- `批量删除` 未勾选学生时禁用，勾选后可二次确认；后端复用现有单个学生删除语义，无历史业务数据的学生物理删除，有历史课程、充值或学生单价规则的学生改为 `离校` 并保留档案，操作写入审计事件和操作日志。

老师档案页面优化：

- 页面位置：`教师 -> 老师档案`。
- 老师入职日期为空时，默认显示该老师第一节课日期。
- 该默认日期只在显示层派生，不写回数据库，不覆盖人工填写日期。
- 老师档案仍保留原有新增、编辑、删除、筛选逻辑。

设置中的基础数据：

- 新增 `设置 -> 基础数据` 页面。
- 第一阶段支持维护：教室 `custom_classrooms`、科目 `custom_subjects`、常用时间 `custom_time_slots`、课程状态 `custom_course_statuses`。
- 基础数据复用现有 `settings` 存储机制，没有新增数据库表。
- 教室、科目、常用时间和课程状态候选只采用当前 `lessons` 表仍存在记录的 `used_lesson_lookups` 规范化值；已删除课程、系统默认数组和 settings 孤立值不再补回候选。
- 删除某值的最后一条引用课程后，该值会在下一次 bootstrap 中同时退出基础数据候选、配色和新增课程选择；仍被其他课程引用的值不受影响。
- 自定义设置仍沿用原 settings 和操作日志体系，但只有被当前课程实际使用后才进入四类业务候选。
- 老师、学生仍以档案页为准；年级仍保持固定体系。

复制图片相关优化：

- `学生 -> 学生查询` 按钮为 `复制图片`，图片标题居中，包含学生姓名、起止日期和学生消费查询标题。
- 学生查询图片包含 8 个核心数据，并保留 `月份汇总` 和 `明细课程表`，适合复制后发送给家长。
- `教师 -> 课时明细` 按钮为 `复制图片`，图片标题居中。
- 教师明细图片统计包含有效课时、课程记录、课时薪资、车票 / 交通补贴、薪资统计；薪资统计包含车票 / 交通补贴。
- 教师明细图片表格不显示 `规则薪资`，图片最后展示横向转置的 `车票/交通补贴明细`。

基础数据没有新建专门字典表，仍复用 `settings` 存储。

#### 学生档案与学生查询显示全部学生档案

- `学生 -> 学生档案` 现在显示 `/api/students` 返回的全部学生档案，不再因为学生没有课程记录而隐藏。
- `学生 -> 学生查询` 的学员选择框同样来自全部学生档案；如果学生暂无课程、费用或充值记录，查询时会正常显示空状态。
- 该调整不影响课程、充值、费用和余额计算。

#### 排课与学生模块筛选下拉框按当前数据联动

筛选下拉框会根据“当前月份 / 当前页面数据 / 当前已选筛选条件”动态生成候选项。当前选中值如果暂时不在新的候选项中，会保留显示，不会让页面报错；清空筛选后会恢复当前数据下的全部可选项。

- `排课 -> 课程总表`：老师、学生、状态筛选联动。
- `排课 -> 矩阵课表`：老师、学生筛选联动。
- `学生 -> 费用明细`：学生、授课老师、年级、状态、价格来源筛选联动。
- `学生 -> 费用汇总`：学生、年级筛选联动；余额状态保持固定枚举，但会参与候选项收缩。
- `学生 -> 充值记录`：学生、年级筛选联动；来源保持固定枚举，但会参与候选项收缩。
- 该调整只改变筛选候选项，不改变费用计算、余额结转、课程排序或数据库数据。

#### 非“已上”课程费用和薪资口径

- `学生 -> 费用明细` 中，课程状态不是“已上”的记录会保留明细行，但单人费用为 0。
- 非“已上”课程不计入学生费用汇总中的实际消费、赠送消费和余额扣减。
- `教师 -> 课时明细` 中，课程状态不是“已上”的记录教师薪资按 0 处理，不参与薪资规则匹配，也不能被批量“按规则更新所选薪资”。
- 新增、复制、编辑课程时，如果状态不是“已上”，不会自动填教师薪资。
- 月度核心 Excel 导出复用同一套费用和薪资计算逻辑，因此非“已上”课程在导出中也按上述口径处理。

#### 薪资规则

`教师 -> 薪资规则` 用于维护教师课时薪资的自动计算规则。规则匹配条件为：

```text
老师 + 年级 + 科目 + 学生集合
```

学生集合会做规范化处理：去空、去首尾空格、排序、统一分隔符；学生顺序不同但集合相同，视为同一条规则。

规则金额字段为“每2小时薪资”。系统会按课程时间自动折算：2 小时 = 1 课时，1 小时 = 0.5 课时，3 小时 = 1.5 课时；无法识别时长时默认按 1 课时计算。

课程薪资应用规则：

- 新增课程时，如果命中有效薪资规则，会自动填入教师薪资。
- 复制课程时，不继承原课程薪资，而是按当前规则重新匹配。
- 没有命中规则时，薪资留空，等待人工核对。
- 已有历史课程薪资不会因为新增规则而自动修改。
- 手动薪资永远优先；`teacher_salary = 0` 可以是合法人工值，不会被当作空值自动覆盖。

`教师 -> 课时明细` 增加了规则薪资参考和来源判断，来源大致包括：`无规则`、`待填写`、`自动`、`手动`。当前薪资与规则计算值一致时显示为自动；不一致时显示为手动。课时明细支持勾选课程后点击“按规则更新所选薪资”，只会把所选课程更新为当前规则计算值，未选中的课程不会被修改；没有有效规则或规则金额为 0 的课程不能批量应用。

教师明细页面优化：

- 新增年级、科目、学生、来源、规则状态筛选；筛选项基于当前月份、当前教师和当前明细数据生成。
- 筛选后，表头全选只会选择当前可见且可应用规则的课程。
- “按规则更新所选薪资”按钮移动到明细表附近；复选框列已变窄并居中显示。

进入 `教师 -> 薪资规则` 页面时，系统会根据历史课程自动补齐规则候选。候选规则默认金额为 0，只作为待设置候选显示，不参与自动匹配；当“每2小时薪资”大于 0 时，规则才参与新增课程自动匹配、复制课程重新匹配、规则薪资计算和批量按规则更新。薪资规则页已简化：不显示状态列和操作列，不需要保存 / 启用 / 停用按钮，修改“每2小时薪资”或备注后失焦自动保存。

课时明细页面优化：

- `+ 新增薪资规则` 按钮移动到全局规则卡片标题附近，新增规则使用弹窗。
- 规则列表支持老师、年级、科目、学生搜索、薪资状态筛选。
- 规则前四列“老师、年级、科目、学生集合”只读；只有“每2小时薪资”和“备注”可编辑，并在失焦后自动保存。
- 自动候选备注默认不再写说明文字；薪资大于 0 才参与自动匹配。
- 规则列表按“老师 -> 年级 -> 科目 -> 学生集合”排序；年级固定顺序为“初一 -> 初二 -> 初三 -> 高一 -> 高二 -> 高三”，老师、科目、学生集合按普通字符串排序。

相关数据结构和接口：

- 新增规则表：`teacher_salary_rules`。
- `lessons` 中保留薪资来源字段：`teacher_salary_source`、`teacher_salary_rule_id`。
- 规则接口：
  - `GET /api/teacher-salary-rules`
  - `POST /api/teacher-salary-rules`
  - `PUT /api/teacher-salary-rules/:id`
  - `DELETE /api/teacher-salary-rules/:id`
  - `POST /api/teacher-salary-rules/sync-candidates`
  - `POST /api/teacher-salary-rules/apply-selected`
- `teacher_salary_rules.is_active` 字段仍保留用于兼容历史接口；当前自动匹配以 `salary_per_unit > 0` 为生效条件。

#### 学生单价规则候选

- `学生 -> 学生单价` 改为自动候选表，不再手动新增或删除规则。
- 候选规则从历史课程生成，唯一键为 `学生 + 年级 + 科目 + 学生集合`；学生集合会规范化，学生顺序不同仍视为同一条规则。
- 表格只显示学生、年级、科目、学生集合、单价、来源、备注；前四列只读，单价和备注失焦保存。
- 单价为 0 显示为未设置，不参与费用规则判断；来源只显示自动、手动、未设置。
- 旧的空年级 / 空学生集合规则会保留在数据库中用于审计追溯，但不在页面展示，也不参与新规则匹配。

### 配色方案

- 新增 `data-palette` 配色体系，默认品牌色为黎明蓝 `#002147`
- 支持 13 套配色方案：黎明蓝 / 黑白 / 青绿原版 / 暖日 / 薰衣草 / 水墨 / 密林 / 冰川 / 咖啡 / 香料土 / 莫奈花园 / 睡莲柔粉 / Bauhaus
- 配色方案保存到 `localStorage`（key: `liming:palette`），独立于明暗主题
- `黑白` 配色方案采用黑、白、灰中性色：亮色使用纯白背景、黑色文字、极浅边框和浅灰导航选中态；暗色使用近纯黑背景、白色文字和高对比黑白按钮，减少大面积灰色。左侧导航、按钮、卡片、筛选框、表格、弹窗、日期控件、头像和分页均同步适配，错误、危险和负数保留必要红色提示
- `data-theme` 继续负责亮色 / 暗色 / 跟随系统的切换

### 暗色模式优化

- 暗色模式从灰绿 / 墨绿调整为近黑 / 蓝黑后台风格
- 优化背景、面板、表格、侧栏、文字、边框等暗色 CSS 变量
- 暗色下交互色改为更克制的蓝色系

### 课程通知截图优化

- 截图默认固定黎明蓝 `#002147`，使用 `--shot-*` 专用变量
- 新增「课程截图跟随当前配色方案」开关（`localStorage` key: `liming:shot-follow-palette`），默认关闭
- 开启后截图跟随 `data-palette` 配色方案变化，选择 `黑白` 时截图使用黑白灰；关闭后始终为黎明蓝
- 截图不跟随暗色模式，始终保持适合发给家长的浅色风格
- 设置入口：设置 → 外观设置 → 课程截图配色
- 简化版式：只保留居中标题"课程通知"+ 课程表格，删除底部说明行
- 表格正文取消时间、年级、科目等字段的单独深色背景，改为统一隔行变色

### 冲突检查增强（历史记录）

- 新增「忽略教室为 1 的教室冲突」开关（`localStorage` key: `liming:ignore-room-one-conflict`）
- 能识别 `1` / `"1"` / `" 1 "` / `"1.0"` 等教室编号变体
- 只忽略教室都为 1 且时间重叠的教室冲突，老师冲突、学生冲突、真实教室冲突等其它问题不受影响
- 最新入口已迁移到课程总表，旧周课表已下线，矩阵课表不再显示冲突检查 UI。

### 已下线周课表暗色高亮修复

- 历史周课表曾修复暗色模式下老师分组结束行 `group-break` 过亮的问题；当前该页面已下线。
- 暗色分组行颜色调整为更克制的深蓝灰：`--group: #132033` / `--group-hover: #172033` / `--group-fg: #cbd5e1`

### 右上角用户菜单

- 将用户信息、修改密码、退出登录从左侧栏迁移到右上角用户菜单
- 右上角显示头像首字母、用户名和下拉箭头
- 下拉菜单包含：用户名、角色、修改密码、退出系统
- 复用原有修改密码和退出登录的业务逻辑

### 外观设置迁移

- 主题和配色方案选择从左侧栏迁移到「设置 → 外观设置」页面
- 主题和配色方案下拉框统一只显示一个下拉箭头，避免浏览器原生箭头和自定义箭头重复
- 右上角用户菜单增加「外观设置」入口
- 保持原有 `THEME_KEY` / `PALETTE_KEY` / `applyTheme()` / `applyPalette()` 逻辑不变

### 月份工具栏迁移

- 月份选择、新建月份、删除月份从左侧栏迁移到全局 topbar
- 首页、排课、学生、教师、设置等页面共用的 topbar 已压缩为紧凑布局，折叠按钮、页面标题、月份选择、新建月份、删除月份、用户菜单和顶部操作按钮统一收紧并垂直居中
- 位置在页面操作按钮之后、用户菜单之前，所有页面都能看到当前月份上下文
- 复用原有 `.month-select` / `.new-month` / `.delete-month` 事件绑定
- `activeMonth` 和 `localStorage` 记忆逻辑保持不变

### 侧栏折叠与滚动模型

- 左侧栏折叠布局使用 `#app.sidebar-collapsed` 控制折叠状态
- 新增 `localStorage` key：`liming:sidebar-collapsed`
- 展开时显示图标 + 文字，收缩时只显示图标
- `navGroups` 增加 `icon` 字段
- topbar 左侧增加折叠按钮
- sidebar、main、content 的滚动模型已调整，课程总表等重数据页面优先使用局部刷新减少闪烁

### Bugfix

- 修复课程总表筛选栏因 `.lesson-filter-bar` sticky 定位导致悬浮在表格上方的问题，删除 sticky 属性使筛选栏重新跟随内容区滚动

### 操作日志

- 新增 `operation_logs` 表，独立于技术审计表 `audit_events`，面向管理员可读
- 新增操作日志页面（设置 → 操作日志），包含筛选区、表格、分页
- 筛选项：操作人、操作账号、操作类型、操作内容、操作时间范围
- 表格只保留操作人、操作账号、操作类型、操作内容、结果和操作时间六列；“详情”列、查看按钮、JSON 展开区及其专用样式已删除
- 操作内容列最宽并支持自然多行换行，长日志不再以单行省略号截断；原结构化详情中的目标、前后变化、数量、日期范围、课程时间/老师/教室、学生、金额、导入文件名及成功/失败统计会转为自然语言摘要
- 分页支持 10/20/50 条/页，页码导航
- 日志时间统一按北京时间展示
- 已接入课程增删改、整周复制、批量复制、批量已上、批量删除、月份新建/删除、账号、角色权限和预筛选等业务操作
- 已接入核心 Excel 单月导出、批量导出全部月份、查看备份记录、下载备份、手动备份和自动备份设置修改
- 已接入学生费用按规则更新、教师课时薪资按规则更新、教师车费保存、学生档案阶段保存 / 批量界定时间、学生档案批量删除、学生 / 教师档案新增编辑删除
- 已接入充值记录新增编辑删除 / 批量删除、期初余额新增编辑删除 / 批量删除、学生单价规则修改、教师薪资规则修改和基础数据修改
- 日志内容使用中文描述关键对象、月份、数量和操作者；写入和响应阶段均会过滤密码、passcode、token、secret、cookie、authorization、session 等字段，手机号按掩码展示，不记录完整数据库路径
- `writeOperationLog()` 统一写日志函数，操作内容为人可读的中文描述
- 登录成功、账号不存在、密码错误、账号禁用、参数缺失和退出均记录结果、账号、显示姓名/角色（成功时）、客户端 IP、截断 User-Agent 和当前版本；客户端详情不包含密码、token、Cookie、授权头或会话标识。
- 月份切换、开始/结束排课、排课修改学生、允许冲突保存、家长/老师通知生成与文案/截图复制下载、主题与配色修改均通过统一客户端日志入口汇总记录。
- 账号启用/禁用、角色修改、老师绑定/解绑、账号模板导入与同步继续记录；课程变更详情保存修改前后关键字段、冲突数量和是否强制保存。
- 学生/教师状态及入离校、入离职日期按实际变更字段记录独立类型与修改前后详情；状态、年级、科目配色保存也可按类型筛选。
- 内部校验、Excel 对账、查看历史审计、忽略对账项、单月/全部月份核心导出、教师薪资/学生账单/期初余额导出、查看/手动/下载备份和自动备份设置均写入可筛选日志。
- `operation_logs` 兼容迁移新增结果、IP 和 User-Agent 字段，旧日志按成功读取；附加详情继续脱敏保存以兼容审计，但页面不再直接输出 JSON。日志页的操作类型来自数据库真实值，并保留成功/失败筛选。
- `writeOperationLog()` 捕获持久化异常并输出服务器警告，不让日志失败阻断核心业务；批量操作继续只写汇总日志。

### 金额显示、列名与首页老师统计修正

- 继续补齐页面金额展示的人民币符号：`学生 -> 费用明细` 的单人费用 / 规则费用、`学生 -> 充值记录` 的本月实际充值 / 本月赠送充值、`学生 -> 期初余额` 的期初实际余额 / 期初赠送余额、`学生 -> 学生单价` 的单价、`教师 -> 薪资汇总` 的车票 / 课时合计 / 薪资合计、`教师 -> 课时明细` 的教师薪资 / 规则薪资、`教师 -> 薪资规则` 的每2小时薪资、`设置 -> 费用标准` 的单人费用。
- 页面展示态金额统一复用前端 `formatMoney()` / `formatCurrency` 口径，0 显示为 `¥0.00`，负数显示为 `-¥160.00` 这类格式；金额输入框仍保持纯数字值，不把 `¥` 写入输入值、接口参数、数据库或 Excel 导入导出。
- 充值记录列名由“本月赠送学费”改为“本月赠送充值”；期初余额 UI 列名由“期初现金余额”改为“期初实际余额”，数据库字段与 Excel 数值口径保持不变。
- 费用明细“单人费用”单元格改为金额输入区和“手 / 自 / 未”状态 badge 分离布局，金额继续右对齐，状态字独立占位并垂直居中，避免遮挡金额；备注列表头继续居中、备注数据左对齐。
- 首页老师圆环图统计口径改为教师档案总数，包含在职、离职、暂停等所有状态老师；新增 / 编辑课程、排课模式和批量复制里的老师候选项仍通过在职老师过滤，历史课程仍可显示离职老师。
- 自检建议：逐页检查金额是否带 `¥`、负数是否显示为 `-¥...` 且保留红色提示；确认金额输入框聚焦后仍是纯数字；确认期初余额导入导出和教师薪资导出不报错；确认首页老师圆环图总数等于教师档案总数，同时新增课程老师下拉仍只显示在职老师。

### 筛选框、日期范围与矩阵课表交互统一

- 通用筛选框复用现有 `filterComboControl()` / `multiSelectControl()` / 自定义 `select` 增强层：默认右侧显示下箭头，展开后变为搜索图标，选项 hover 和当前项使用浅灰背景；已选筛选项 hover 时右侧显示灰底小 `×`，点击后立即清空并刷新对应筛选结果。
- 筛选框输入文字后按 Enter 会提交当前筛选并 `blur`，不再停留在打字光标状态，也不会触发页面刷新；普通文本筛选继续保留原有联动、中文输入和失焦保存逻辑。
- 起止日期筛选统一为日期范围选择器，展示为 `YYYY-MM-DD → YYYY-MM-DD`；右侧统一使用新版简洁日历图标，左侧提供昨日、今日、本月、本周、上月、上周、近三个月、近一年、去年、今年，并在独立区域纵向滚动，右侧双月日历固定且决定面板主要高度。单箭头切换月份、双箭头切换年份、先选起始或先选截止、禁用不可选日期、hover 预览和实心选中范围保持不变。
- 已替换的日期范围包括：首页课程趋势、课程总表、矩阵课表、学生费用明细、学生查询、经营概览、日常开销、操作日志、家长群课程截图和老师课程截图；单日期输入如新增课程日期、充值日期、档案入离职日期仍保持单日期选择器。
- 家长群课程截图、老师课程截图中的独立快捷日期按钮已移除，快捷时间整合进日期范围选择器左侧；课程总表在日期范围旁保留 `今日 / 本周 / 下周 / 本月` 快捷按钮，日期计算函数继续复用。
- 课程总表筛选区改为稳定布局，状态筛选从初始状态就在第一行右侧合理位置，搜索框 Enter、清空筛选、重置筛选或重渲染都不会再导致状态筛选跳位。
- 课程总表排课模式和浏览态的时间列、状态列统一字号、字重和背景表现；排课态状态下拉会同步 `已上 / 待上 / 请假 / 试课 / 考试 / 未缴费` 等状态色，减少白色输入框割裂感。
- 矩阵课表新增三种视图：时间课表、老师课表、教室课表。时间课表保留原有时间段矩阵逻辑；老师课表按老师 × 日期展示课程；教室课表按教室 × 日期展示课程，三种视图共用日期范围和老师 / 学生 / 教室 / 年级 / 科目 / 状态筛选结果。
- 自检建议：逐页检查普通筛选框默认箭头、展开搜索图标、已选 hover 小 `×`、Enter 后 blur；检查日期范围面板快捷时间、双月日历、月份 / 年份切换、先选起始或截止、hover 预览和清空；检查课程总表筛选区不跳位、排课态和浏览态时间 / 状态视觉一致；检查矩阵课表三种视图、日期范围和筛选条件均生效。

### 车费明细、薪资汇总与账号保护

- 日期范围选择器未选择时统一显示 `开始日期 → 结束日期`，右侧为日历图标；选择后显示 `YYYY-MM-DD → YYYY-MM-DD`，hover 时显示灰底小 `×`，点击后清空并刷新当前筛选。日历点击改为任意两次日期自动排序成起止日期，支持先点开始或先点结束。
- 筛选框、下拉框、搜索图标、清空图标保持统一尺寸和垂直居中；搜索输入按 Enter 后提交并 blur。
- 课程总表状态列取消 badge 背景，浏览态和排课态的日期、时间、状态列保持字号、字重和背景一致。
- 教师栏目命名调整为 `薪资汇总`、`车费明细`、`课时明细`、`薪资规则`、`教师档案`。`薪资汇总` 页面只读展示车费金额，仅备注可编辑；导出本月功能保留。
- 新增 `教师 -> 车费明细` 页面，可按当前月份编辑每位老师各周车费。保存走 `PUT /api/teacher-travel-fees`，写入 `teacher_travel_fees`，同步 `teacher_adjustments_monthly` 旧 1-4 周字段，并记录操作日志。车费明细前端不再展示备注列，历史兼容备注字段保留。
- 月份周归属规则改为“该周周日在哪个月，该周就归属哪个月”。周一为起始、周日为截止，列名为 `第N周车票(YYYY-MM-DD~YYYY-MM-DD)`，页面按实际周数自动生成列。
- 权限体系新增 `teacherTravelFees` / `车费明细`；老板拥有全部权限，教务默认补齐该权限。无权限账号不能看到页面，也不能直接访问车费明细接口。
- 账号管理禁止删除角色为老板的账号：前端删除按钮禁用并提示 `老板账号不可删除`，后端删除接口同步拒绝并记录拒绝删除尝试。
- 学生档案普通行的行高、单元格 padding、字号、输入框高度、按钮高度和 checkbox 垂直居中继续与教师档案统一；阶段展开行保持自适应。
- 教师课时明细已恢复课程备注列，复制图片和按规则更新所选薪资继续保留；教师车费明细仍不展示备注列。
- 自检建议：检查日期范围清空和日历点击；检查 `车费明细` 保存后 `薪资汇总` 同步；检查角色权限里出现车费明细；检查老板账号前后端均不可删除；检查学生档案行高。

### 日期范围细节、筛选图标与冲突检查迁移

- 日期范围筛选框未选择时改为左右分区展示：左侧 `开始日期`、中间独立箭头 `→`、右侧 `结束日期`，最右侧保留固定宽度日历图标；选择后同样按 `YYYY-MM-DD → YYYY-MM-DD` 分区显示，避免整句 placeholder 挤在一起。
- 日期范围面板点击日期的反馈改为即时可见：第一次点击会保留起点或终点选中态，第二次点击自动排序为完整起止范围并刷新当前页面数据；面板点击处理改为等待日期选择逻辑完成，避免被外层关闭逻辑抢先打断。
- 筛选框右侧图标统一为固定图标区：普通下拉箭头、展开后的搜索图标、新版简洁日期日历图标和 hover 清空 `×` 均放大并垂直居中；清空 `×` 使用浅灰圆形背景，图标与圆形中心对齐。
- 课程总表搜索框和部分纯文本筛选框改为通用文本筛选控件：输入后按 Enter 会提交并 `blur`，hover 有值的搜索框时出现灰底小 `×`，点击后清空并触发原筛选刷新逻辑。
- 各筛选区的统计和重置区域统一右对齐：`已筛选 x / 共 x 条` 与 `重置 / 清空筛选` 按钮整体贴近筛选卡片最右侧，窄屏时换行后仍尽量靠右显示。
- 课程总表状态列改为普通文字显示：浏览态不再使用状态 badge 颜色或背景；排课态状态下拉仍可编辑，但课程总表内覆盖为普通文字视觉，不影响状态含义和保存。
- 冲突检查迁移到课程总表：按钮放在 `批量删除` 后方，按当前筛选结果实时计算冲突数量；`冲突为0` 显示灰色中性状态且不可点击，有冲突时显示红色 `冲突为N` 并可打开 `时间冲突检查` 弹窗。
- 冲突弹窗保留 `忽略教室为 1 的教室冲突` 开关，并显示老师 / 学生 / 教室 / 时间格式冲突统计；每条冲突展示日期、星期、时间、授课老师、教室、年级、科目、学生和状态，可定位到相关课程。
- 矩阵课表删除冲突检查 UI，不再显示冲突检查面板、冲突数量标签、忽略教室 1 开关或冲突列表；页面保留日期范围、重置和矩阵三视图展示，筛选统计和重置按钮贴近筛选区最右侧。旧周课表页面已下线，历史入口会回到课程总表 / 矩阵课表。
- 自检建议：检查日期范围未选中布局、日历图标和 hover 清空；检查日期面板可连续点击两天生成范围；检查课程总表搜索框 Enter 后失焦且 hover 可清空；检查筛选统计区域靠右；检查课程总表冲突按钮和弹窗；检查矩阵课表已无冲突检查区域，旧周课表入口不再停留在独立页面。

### 周课表下线、班级管理与截图模式

- `排课 -> 周课表` 已从菜单、默认角色权限和账号/角色预筛选中移除；旧 `week` 视图会回到课程总表 / 矩阵课表。
- 矩阵课表顶部保留日期范围、重置和 `时间课表 / 老师课表 / 教室课表` 三个视图，不再显示快捷周按钮或 `当前周` 按钮；筛选统计与重置按钮贴近筛选区最右侧。
- 日期范围选择器修复点击响应：第一次点击会立即保留选中态，第二次点击生成范围并刷新；hover 预览不会抢在点击前重绘目标日期。
- 各筛选区的 `已筛选 x / 共 x 条` 与重置按钮统一靠右展示。
- 教师模块入口顺序为 `薪资汇总`、`车费明细`、`课时明细`、`薪资规则`、`教师档案`；`薪资汇总` 中车费金额只读，备注可失焦保存。
- 车费周列表头统一为两行：第一行 `第N周车票`，第二行日期范围。
- 学生模块入口顺序为 `费用汇总`、`费用明细`、`充值记录`、`期初余额`、`学生查询`、`学生单价`、`班级管理`、`学生档案`。
- 新增 `class_groups` 班级管理，唯一键为 `老师 + 年级 + 科目 + 规范化学生集合`，班级候选来自历史课程和教师薪资规则。
- 家长群课程截图和老师课程截图在存在班级名时，学生列显示为 `班级名：学生`。
- 家长群课程截图已移除 `暑期班截图` 当前模式；页面保留默认日常截图逻辑，并继续按个人发送、班级发送、班级合并发送生成对象。

### 车费、矩阵课表、截图简洁模式与学生年级阶段

- `教师 -> 车费明细` 的老师范围改为与当前月份 `薪资汇总` 中出现的老师一致，不再额外合并全部教师档案；切换月份后随薪资汇总同步变化，车费保存接口不变。
- `排课 -> 矩阵课表` 改为按矩阵页自身日期范围请求全局课程数据，不再用顶部月份扩展或限制数据；时间课表、老师课表和教室课表三种视图继续共用老师 / 学生 / 教室 / 年级 / 科目筛选。
- `学生 -> 班级管理` 和 `教师 -> 薪资规则` 默认勾选 `隐藏非在职老师`，按教师档案 `status=在职` 前端筛选；取消勾选后可查看全部候选和规则，不删除任何数据。
- `排课 -> 家长群课程截图` 的个人发送对象覆盖所有课程中的所有学生，1V1、1V2 和小班课学生都会生成个人发送；原班级 / 群发送逻辑保留。
- 家长群截图新增 `预览模式 / 简洁模式`。预览模式保持原卡片预览；简洁模式分为 `个人发送`、`班级发送` 和 `班级合并发送` 三个区域，并按初一至高三分区展示小矩形。
- 简洁模式小矩形点击会在 `复制截图` 和 `复制文案` 之间轮流执行；复制文案成功后小矩形变绿，`清除所有打勾记录` 会同时清除简洁模式绿色状态。
- `排课 -> 课程总表` 在日期范围旁保留 `今日 / 本周 / 下周 / 本月` 四个快捷按钮；`今日` 使用当前真实日期，`本月` 使用顶部当前系统月份的首日至末日。
- `学生 -> 学生档案` 的年级列改为 `当前年级`，当前年级由 `student_grade_stages` 阶段界定时间和当前日期判断；未命中阶段时回退原 `students.grade` 或 `未设置`。
- 学生档案点击学生行打开 `初一` 至 `高三`、`已毕业` 的阶段界定弹窗；普通阶段保存起止日期，`已毕业` 只保存起始日期，取消或关闭只丢弃弹窗草稿。
- 学生档案新增 `批量修改界定时间`，仅勾选学生后可用；批量写入某一个阶段，不影响其他阶段，并写入审计事件和操作日志。
- 学生姓名在普通表格行中只读显示，不再暴露行内姓名输入框；普通行行高、padding、字号、input/select 高度和列宽已进一步收紧，姓名、当前年级、状态、入学日期、离校日期、电话、备注等列固定宽度，表格超宽时在容器内横向滚动。
- 学生档案最后一列 `操作` 和单行删除按钮已删除；新增 `批量删除` 按钮，未勾选禁用，勾选后可二次确认。批量删除沿用现有单个学生删除语义：无历史业务数据的学生物理删除，有历史课程、充值或学生单价规则的学生改为 `离校` 并保留档案；删除后刷新列表、清空选中状态，并写入审计事件和操作日志。
- 学生档案页面移除 `批量升年级` 入口；旧 `students.grade` 字段保留为兼容和 fallback，后台不再在 bootstrap 中自动触发旧升年级逻辑。
- 权限口径：年级阶段单人保存、批量保存和学生批量删除沿用 `学生档案` 页面权限，并继续受只读状态限制；家长群截图模式切换、课程总表快捷日期和隐藏非在职老师开关不新增权限。
- 自检建议：执行 `node --check public/app.js`、`node --check src/server.js`、CSS 大括号平衡检查，并启动服务检查 `/api/version`；登录后重点检查车费老师范围、矩阵课表跨月份日期范围、家长群简洁模式、学生阶段保存与批量保存。

## 月份切换刷新策略

顶部月份选择只表示当前全局工作月份，不再默认触发所有页面的 `content` 区域清空重建。前端统一通过 `onActiveMonthChanged(newMonth, oldMonth)` 处理月份变化，并按当前页面与月份的关系分流：

- **顶部月份无关页面**：课程总表、矩阵课表、家长群课程截图、老师课程截图、期初余额、学生查询、学生单价、班级管理、学生档案、薪资规则、教师档案、外观设置、基础数据、费用标准、账号权限、操作日志等页面只同步顶部月份状态和本地存储，不刷新当前页面。
- **顶部月份相关且局部刷新页面**：首页、学生费用汇总、学生费用明细、充值记录、教师薪资汇总、教师车费明细、教师课时明细、员工薪资、员工考勤、日常开销、经营概览、数据对账等页面只刷新当前页面需要的接口数据、统计、表格和图表，不重新进入全局 `load()`。
- **页面自有日期范围优先**：课程总表、矩阵课表、截图页、学生查询、经营概览和日常开销都以页面内部日期范围为准；顶部月份只有在用户主动点击对应的本月快捷或进入明确月度页面时才参与计算。
- **状态保留规则**：局部刷新保留当前筛选条件、滚动位置、展开状态、选中状态、输入草稿和弹窗状态；只有弹窗内容与月份强相关且必须重算时才关闭或重绘该弹窗。

## 金额结算口径

学生余额不是简单的“现金总池 + 赠送总池”月末一次性扣费，而是按账户批次逐笔处理。

系统先确定本月月初余额：

- 如果当前月有上一有效数据月，使用上一有效月的月末现金余额和赠送余额；
- 如果当前月没有上一有效数据月，使用全局 `student_opening_balances` 期初余额；
- 期初余额只作为账户初始化批次，不计入本月充值。

随后按时间线处理真实充值和已上课程：

1. 期初余额作为第一笔虚拟批次。
2. 每条真实充值记录形成一个新批次；现金充值、赠送充值和负数退费都按充值日期进入时间线。
3. 同一天充值先于课程；多条充值按 `recharge_date`、`id` 排序；课程按日期、`time_slot` 和课程 id 稳定排序。
4. 每节 `已上` 课程使用 `feeDetails()` 算出的单人应收费用扣费；非 `已上` 课程费用为 0，不参与扣费。
5. 扣费从最早未扣完批次开始，每个批次内部先扣现金，再扣赠送，扣完后进入下一批次。
6. 所有批次都不足时，剩余部分形成现金欠费，现金余额可为负数；赠送余额不能为负数。
7. 后续现金充值会先抵扣现金欠费；后续赠送充值不会抵扣现金欠费。
8. 课程扣费前会整理账本中的现金欠费和后续正现金批次，避免赠送仍有余额时现金余额继续变负。

口径要点：

- **本月实际充值 / 本月赠送充值**只统计真实充值记录，0+0 空记录不参与。
- **本月实际消费 / 本月赠送消费**来自批次 FIFO 后每节课落到现金账户或赠送账户的消费拆分。
- **课程总费用 = 本月实际消费 + 本月赠送消费**；现金消费中可以包含欠费部分。
- **本月实际余额 / 本月赠送余额**是时间线处理完成后的月末余额。
- **扣费边界**：例如课前实际余额 120、赠送余额 250、课费 370 时，应拆分为实际消费 120、赠送消费 250，课后实际余额和赠送余额均为 0，不能出现实际余额 -250 且赠送余额仍为 250。
- **finance.revenue 是收入认现口径**：每条有效课时先算 `allocated_revenue = unit_price × (actual_consumption / total_fee)`、`allocated_gift_consumption = unit_price × (gift_consumption / total_fee)`，再汇总到经营概览。学生没付钱的课时不计现金收入，仅在余额上体现为负数或在 `accounts_receivable` 体现（限 `状态=未缴费` 的课）。
- **经营拆分同口径**：老师人效、年级/科目/班型收入、Top 学生贡献均使用 `allocated_revenue`，避免顶部收入按现金认现、底部排行按课程标价的口径混用。

## 数据流向图与审计

金额相关数据流向图见 [docs/money-data-flow.svg](docs/money-data-flow.svg)。

金额口径审计记录见 [docs/financial-data-flow-audit.md](docs/financial-data-flow-audit.md)，其中列明了已修复的口径问题、仍保留的低风险冗余项、服务器清理边界和后续建议。

## 已知 bug / 风险（重点关注金额相关）

按严重程度排序。已复审 `unitPriceFor / studentSummary / financeBase / weightedTeacherTransport / weightedStaffSalary / ensureCarryOver / recomputePricing / patchExpense / upsertStaffSalary / deleteMonth` 等所有金额触达点。

### 修复历史摘要

下表列出已通过代码修复的历史问题，仅供回溯：

| 历史问题 | 修复方式 | 相关代码 |
| --- | --- | --- |
| 跨月课程被丢弃或被错误归入工作簿月份 | 改为按「课程实际日期」决定 `month_key`，跨月行不再被丢弃；导入时按 (date, month_key) 精确删除，不会误覆盖其他月份 | `readXlsxTotalRowsForImport` / `importLessonsFromWorkbook` / `import_workbook.py:lesson_rows` |
| 系统上线前余额曾混在充值/结转口径中 | 拆出全局 `student_opening_balances`；期初余额不属于月份、不计入充值流水，系统起始月从全局期初余额开始结转 | `student_opening_balances` / `previousCarryOverBalances` / `studentSummary` |
| Excel「学生费用明细」手填单价不会回写到系统 | 导入工作簿时按 (lesson_id, student_name) 写入 `fee_overrides`，让历史 Excel 单价成为系统计费的权威值；`findLessonForFeeOverride` 按行实际日期月份匹配，跨月课也能命中 | `importFeeOverridesFromWorkbook` / `findLessonForFeeOverride` |
| Excel 充值/费用核对脚本只能比对单月，无法跨工作簿 | 新增 `scripts/sync_source_workbooks.js` 一键同步多月 + 整体备份；审计脚本支持 `--simulate-sync=YYYY-MM-01,...` 临时库重放；新增「相邻 Excel 结转核对」与跨月汇总去重 | `scripts/sync_source_workbooks.js` / `scripts/audit_source_vs_summary.js` |
| 中文姓名输入框每输入一个字符就整页刷新 | `bindSafeTextInput` 改为输入时只更新草稿，按 Enter / 失焦 / change 才提交并重渲染；增加 IME composition 状态判断，不在合成中触发 | `public/app.js` `bindSafeTextInput` |
| 应收账款口径只统计「状态=未缴费」 | 拆出 `account_debt_receivable` + `unpaid_lesson_receivable`，`accounts_receivable` 按学生取最大值合并去重 | `financeBreakdowns` |
| 学生单价 `custom_price = 0` 会强制覆盖课时价为 0 | 现改为 0 元规则只表示未设置；旧空组合规则不再参与匹配，必要时以 `fee_overrides` 保留历史费用 | `unitPriceFor` / `/api/student-pricing` |
| `recomputePricing` 静默删除手填覆盖 | UI 弹窗显式显示「将清除 N 条手填价格、重算 M 节课」 | `app.js` `.pricing-recompute` |
| 强删月份无确认 | UI 二次确认弹窗，需手输 `month_key` 字符串才能解锁删除按钮；下游月份会按新的 `previousDataMonth` 重新计算月初余额 | `deleteMonth` / `month-delete-confirm` |
| `previousEqualRange` 用滑动窗口处理自然月 | 当 `range` 是完整自然月时，改用真实的上一自然月；自定义区间仍走滑动窗口 | `previousEqualRange` |
| `gross_margin` 用 `pctChange` 误导 | 增加 `mom_pp`（百分点差），UI 切换到 pp 显示 | `metricOverview` |
| `severityCounts` 吞掉非标准 severity | 动态新增桶，`pricing_recompute` 等写入的 `info` 也会被汇总 | `severityCounts` |
| 上游充值补录后，下游余额口径不会刷新 | 余额结转改为按上一有效月月末余额或全局期初余额计算；`POST /api/recharges` 保存后会触发后续月份刷新 | `previousCarryOverBalances` / `refreshCarryOverAfter` / `/api/recharges` |
| 主题选择占用顶栏空间 | 主题下拉移到左侧导航底部，顶栏只保留月份和当前页面操作 | `renderNav` / `renderTopbar` |
| 配色方案与课程截图未联动 | 新增 `data-palette` / `liming:palette` 和 13 套配色（含黑白）；课程通知截图预览、复制图片、下载 PNG 统一读取 `--shot-*` 变量，暗色模式下仍保持浅色家长版 | `public/styles.css` / `courseNoticeCanvas` |
| 暗色模式偏绿偏脏 | 暗色基础变量改为近黑 / 蓝黑后台风格，大面积背景不再使用绿色系；配色方案在暗色下只影响局部强调 | `:root[data-theme="dark"]` / `prefers-color-scheme` |
| 短范围矩阵课表时间段界限弱 | 保留原有宽松列宽和单元格高度，只用符合设计系统的边框色做时间段分组分隔；暗色 / 亮色都可见 | `week-grid-table` CSS |
| 生产样式未完整应用 `_ Design System` | 同步品牌主色、`brand-pale`、语义 token、字体工具类、暖胡桃 system dark，并将亮色侧栏改为 UI Kit 的温暖纸色方案 | `public/styles.css` |
| 课程字段修改触发整页刷新，丢滚动位置和未提交草稿 | 改为 `FIELD_TIERS` 字段分档（A/B/C 三档）：A 档只更新单元格，B 档 tbody 局部重绘 + 乐观更新 + 冲突重算并刷新课程总表冲突按钮，C 档 `dirtyFlags` 标记；事件绑定改为 `contentEl` 一次性委托；`reRenderLessonsTbody` 末尾补调 `enhanceCustomSelects` + `enhanceCustomDateInputs` | `public/app.js:handleLessonFieldChange` / `reRenderLessonsTbody` / `FIELD_TIERS` |
| tbody 局部重绘后自定义 `<select>` / `<input type="date">` 退化为原生组件 | 在 `reRenderLessonsTbody` 末尾补调 `enhanceCustomSelects()` + `enhanceCustomDateInputs()`，两函数均幂等（`data-custom-select` / `data-custom-date` 守卫） | `public/app.js:reRenderLessonsTbody` |

### 高（建议尽快修）

1. **强删月份的业务语义仍需 UI 明确提示**
   `ensureCarryOver` / `refreshCarryOverAfter` 会按当前存在的数据月重算后续自动结转；但 `previousDataMonth` 是基于 `availableMonths()` 的，如果中间月份被删，下游月会跳过被删月直接结转上上月余额。技术上已能刷新，业务上仍需 UI 显式提示「下一月的结转将基于 X 月而不是被删月」。

### 中（语义不一致或边界数据可能踩坑）

2. **未填 `recharge_date` 时 fall back 到月初**
   `rechargesInRange` 用 `COALESCE(NULLIF(recharge_date, ''), month_key)`。在半月级 finance 自定义区间（如 4.16–4.30）下，本月里没填日期的充值会落到 4.1，被错排除。建议要么在录入时强制日期，要么 fall back 到月末。

3. **Finance 自定义区间下，员工薪资和老师车贴按"日数线性比例"**
   `weightedStaffSalary` 和 `weightedTeacherTransport` 用 `overlapDays / monthDays` 做权重。但员工工资是月度发放、不可日切，半月视图会显示一半工资，半月利润分析会被低估。建议自定义区间下显式提示"成本按时间分摊"，或允许用户在 UI 选择"成本不分摊"。

4. **同步学生年级会被 lesson 上的错误年级覆盖**
   `syncStudentsFromLessons` 用 `COALESCE(NULLIF(excluded.grade,''), students.grade)`，最后一次遍历到的非空 lesson 年级会写入档案。某节课误录入错年级会污染学生档案。审计的 `grade_inconsistency` 能兜底告警，但同步本身仍写入。建议只在 `students.grade` 为空时填年级，不要覆盖。

5. **`staff_salary_monthly.salary_actual` 和 `expected_salary` 双源**
   存储字段 `salary_actual` 取 INSERT 时的 `base_salary` 快照，但 SELECT 同时返回 `expected_salary`（用当前 `base_salary` 重算）。员工调薪后历史月份两栏会不一致，UI 上一旦展示 `expected` 会让历史月看起来"工资变了"。`weightedStaffSalary` 用的是 stored `salary_actual`，所以经营概览的运营成本是稳定的。建议 UI 上明确区分"按当时基薪"和"按当前基薪"，或干脆不返回 `expected_salary`。

6. **试课 / 考试的老师课时费完全靠手填**
   `teacherSummary` 只把 `已上 / 未缴费` 计入老师课时合计；试课、考试不计入。`lessons.teacher_salary` 是手填字段，没有跟单价或学生数联动。如果机构对试课/监考有薪资政策，目前需要在每节课的 `teacher_salary` 单元格里手动维护。

### 低（边界 / 体验）

7. **`upsertStaffSalary` 对离职员工的保护过严**
   只要 `staff.status='离职'` 且当月已经存在 salary 行，任何修改都被拒。即使要修复历史录入错误也得先把员工恢复在职。

8. **`expense q` 模糊查询不转义 `%` `_`**
   用户搜 `2_` 之类时会触发 SQL LIKE 通配。本地工具，影响小。

9. **`splitStudents` 不规范化中间空格**
   "小 明" 与 "小明" 被当成两个学生。审计 `student_typo` 通过 `normalizeAuditName` 去空格后做 Levenshtein 兜底，但平时录入时不会自动合并。

10. **`student_names` 修改后 `fee_overrides` 留下孤儿行**
    `fee_overrides` 主键为 `(lesson_id, student_name)`。课程总表中修改某节课的 `student_names`（移除某个学生）后，数据库里该学生的 `fee_overrides` 行不会被级联删除，成为僵尸数据。这是后端 PATCH 处理逻辑的 bug，当前未修复，后续需在 `/api/lessons/:id` PATCH 中补入旧学生名清理逻辑。

## 受限范围（不在 MVP 内）

- 多机构 / 多分店 / 多租户管理
- 移动端 / 离线 / 同步到云
- 微信、企业微信、电话、短信集成
- 财务凭证 / 发票 / 税务
- 学生学习记录 / 错题本 / 测评

## 月度核心 Excel 增强归档

单月核心 Excel 在原有五张人工业务表之后追加原始数据和参考数据工作表。原五张工作表的名称、顺序、字段含义和现有下载入口保持不变；“全部月份核心 Excel”ZIP 继续复用同一份单月工作簿生成逻辑。

新增的“导出说明”会标明导出月份、时间、应用版本、SQLite `user_version`、工作表清单、记录数及数据类型。该文件定位为月度业务归档，不是系统完整恢复备份，不能用于覆盖恢复整个系统。原始表保留数据库 ID、关联字段、月份和备注等现有字段；期初余额只导出所选月份的 `student_opening_balances` 原始记录，不以计算后的月末余额替代。

用户文本全部写为 OOXML 字符串单元格，不生成公式或宏；工作簿不包含密码哈希、Session、Cookie、Token、密钥、服务器绝对路径、Docker/SSH 信息。增强导出的构建过程不改写业务表，原有操作日志仍照常记录导出；旧 `backup_records`、`data/backups/`、期初余额导入导出及旧备份下载逻辑保持不变。

本地回归测试只使用系统临时目录中的合成 SQLite 数据：

```powershell
npm.cmd run test:monthly-core-excel
```
