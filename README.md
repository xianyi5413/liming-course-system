# 黎明教育教务管理系统

面向单店培训机构的课程、学生费用、教师薪资和日常经营管理系统。本文件以当前代码为准，同时服务于机构管理员、新开发者和部署维护人员。

## 1. 项目简介

系统覆盖排课、课程执行、学生收费、教师结算、员工与经营支出、权限、审计、完整 Excel 数据交换及备份。当前技术栈是 Node.js 24、内置 `node:sqlite`、原生 HTML/CSS/JavaScript 和无宏 `.xlsx`；正式环境由 Docker Compose 运行应用与 Nginx，SQLite 及受管文件持久化在 Docker Volume 的 `/app/data`。

这是单体应用：浏览器请求同一个 Node 服务提供的静态资源和 JSON API。SQLite 是当前唯一在线数据库，不应将仓库、容器可写层或 Excel 文件误认为数据库的替代品。

## 2. 功能总览

### 首页与排课

- **首页**：显示当日/近期课程、关键经营指标、数据质量和备份健康状态；“正在上的课程”和页面当前时间统一按 `Asia/Shanghai` 判断。桌面端当前课程、待办和人数统计由同一网格行自然拉齐，课程过多时只滚动课程列表；390px 下恢复自然堆叠。
- **课程总表**：按日期、老师、学生、年级、科目、状态等筛选课程，支持新增、复制、批量状态和行级编辑。会检查同一老师、教室和学生的时间冲突。
- **矩阵课表**：以时间和日期矩阵查看排课，适合快速识别空档与冲突。
- **家长群课程截图**：生成适合家长群发送的课程卡片和文案，支持主题、范围及完成状态。
- **老师课程截图**：按老师生成课程安排截图，便于教学沟通。

### 学生与收费

- **费用汇总**：汇总学生在选定期间内的充值、实际余额、赠送余额和消费；全部展示金额均为 0 的行只在页面隐藏，不删除记录，也不改变完整导出。
- **费用明细**：按课程拆分到学生，展示费用来源与单节金额；时间列固定为 128px，完整单行容纳跨午夜时间段。
- **充值记录**：记录实际充值、赠送充值、充值日期、结构化来源/渠道和备注；表格取消独立操作列，来源/渠道可直接在行内浮层修改，新记录默认微信，旧记录未保存渠道时明确显示“未记录”。
- **期初余额**：每名学生一条全局起点余额，不与月份绑定；页面年级来自学生档案当前年级，历史记录年级只作为导出快照，跨月余额从全局起点累计。表格所有单元格保持单行，备注为单行输入且桌面列宽约 516px；金额仍支持负数和两位小数，且仅本页隐藏浏览器数字步进按钮。
- **学生查询**：聚合查看学生档案、课程、费用和余额。
- **学生单价**：按学生、年级、科目和学生集合配置个性价格，并展示规则有效状态；学生集合以顿号分隔完整单行显示，可勾选当前筛选范围并通过事务接口统一设置单价，单次最多 500 条。
- **班级管理**：维护老师、年级、科目、班级名和学生集合；学生姓名使用顿号完整单行展示，横向空间不足时只滚动外层表格。
- **学生档案**：维护当前年级、各学段日期、监护人、电话、状态、入离校日期和备注；集中提示并定位年级阶段日期冲突。

### 教师、员工与经营

- **薪资汇总**：按有效课程、薪资规则、单节覆盖和月度调整计算教师应付薪资。
- **车费明细**：按月份、周次和老师记录教师车费。
- **课时明细**：先从在职教师档案中选择教师，再逐节查看实际薪资、规则薪资并按规则批量更新所选课程。
- **薪资规则**：按老师、年级、科目和学生集合维护每两小时薪资规则；学生集合以顿号分隔完整单行显示，支持在当前可见范围内批量设置薪资，启用状态和其他规则字段不会随批量操作改变。
- **教师档案**：维护联系方式、在职状态、入离职日期和备注。
- **员工薪资**：维护非教师员工的月度实发、奖金、扣款和备注。
- **员工考勤**：记录考勤日期、状态、计薪单位、工时和原因。
- **日常开销**：记录日期、类别、金额、商家和备注。
- **经营概览**：汇总收入、教师薪资、员工工资、支出和毛利；数据不完整时会显示质量提示。

### 设置与治理

- **外观设置**：亮色、暗色、黑白及业务标签配色。
- **基础数据**：维护教室、科目、常用时间和课程状态等候选项。
- **费用标准**：按年级和人数维护默认单人费用。
- **数据中心**：预检、完整 Excel 导入导出、服务器本地备份、百度网盘副本和备份记录。
- **操作日志**：查询关键写操作和结果，可按需纳入完整 Excel。
- **账号权限**：维护账号、角色、教师绑定、页面权限与个人覆盖。

## 3. 技术架构

| 层 | 当前实现 |
| --- | --- |
| 运行时 | Node.js 24；`package.json` 要求 `>=24` |
| 数据库 | Node 内置 `node:sqlite`，单个 SQLite 主库，线上使用 WAL 模式时会同时存在 `-wal`/`-shm` |
| 前端 | `public/index.html`、`public/app.js`、`public/styles.css`，无前端构建步骤 |
| 后端 | `src/server.js` 提供静态资源、认证、权限、API、初始化和迁移 |
| 领域计算 | `src/domain/` 统一费用、余额和薪资等业务口径 |
| Excel | `src/excel/` 定义 v4 字段、读写、摘要验证和恢复 |
| 备份 | `src/backup/` 管理本地 Excel、调度、保留和百度网盘副本 |
| 权限 | 系统角色默认权限 + 角色权限表 + 账号个人覆盖；后端仍逐路由校验 |
| 部署 | `docker-compose.yml`：`app` + `nginx`，命名 Volume `liming_data` |
| 入口 | 应用容器监听 5177；正式浏览器入口由 Nginx 暴露 80/443 |

当前 `Dockerfile` 使用 `node:24-alpine`，尚未固定镜像 digest。正式重建前应验证实际 Node、Alpine、SQLite 和镜像来源；不要把网络超时误记为供应链验证通过。

## 4. 项目目录

```text
public/                 浏览器页面、交互和样式
src/
  server.js             HTTP 服务、SQLite 初始化/兼容迁移和业务 API
  backup/               完整 Excel 备份、调度、百度网盘客户端
  domain/               费用、余额、薪资等共享业务计算
  excel/                Excel v4 字段契约、编码、验证、导入与恢复
scripts/                数据库、Excel、审计、部署与维护脚本
tests/                  Node 单元、API、集成及 Chromium 页面测试
artifacts/              仅保留合成 Excel 验收样本
data/                   本地运行数据；真实内容由 .gitignore 排除
deploy/                 Nginx 配置
docs/                   部署、迁移和审计补充文档
```

`data/` 的常见运行内容：

```text
data/liming-local.sqlite               SQLite 主库
data/liming-local.sqlite-wal           WAL（存在与大小随运行状态变化）
data/liming-local.sqlite-shm           WAL 共享内存文件
data/source-workbooks/                 源工作簿
data/templates/                        运行时模板
data/uploads/                          临时上传
data/backups/                          历史备份与受管备份父目录
data/backups/full-excel/               当前完整 Excel 受管目录
data/backups/full-excel/.secrets/      百度配置与 Token（不得公开）
```

## 5. 本地开发

### 环境与安装

需要 Git、Node.js 24 或更高版本和 npm。主要 SQLite 测试依赖 Node 24 的 `node:sqlite`。不要为了测试擅自替换机器上的全局 Node；可使用隔离的 Node 24 容器。

```powershell
git clone <repository-url>
cd liming-course-system
npm install
npm run init
npm start
```

浏览器打开 `http://127.0.0.1:5177`。默认端口来自 `src/server.js` 和 `Dockerfile`，均为 **5177**。

PowerShell 使用 `npm.cmd` 可避开本机脚本执行策略：

```powershell
npm.cmd run init
npm.cmd start
```

Git Bash 使用：

```bash
npm run init
npm start
```

### 环境变量

| 变量 | 用途 | 默认值/说明 |
| --- | --- | --- |
| `NODE_ENV` | 运行模式 | Compose 中为 `production` |
| `PORT` | Node 监听端口 | `5177` |
| `DATA_DIR` | 数据目录 | 仓库内 `data/`；容器为 `/app/data` |
| `DB_PATH` | 显式数据库路径 | 默认 `${DATA_DIR}/liming-local.sqlite` |
| `SESSION_COOKIE_SECURE` | Cookie 是否仅 HTTPS | 正式环境应为 `true` |
| `APP_VERSION` | 页面和导出中的应用版本 | 未设置时使用代码内默认值 |
| `APP_GIT_COMMIT` | 导出和诊断中的提交号 | 建议构建/启动时注入完整提交号 |
| `BAIDU_APP_KEY` | 百度应用标识 | 可留空并由数据中心受限配置文件提供 |
| `BAIDU_APP_SECRET` | 百度应用秘密 | 不得提交或回显 |
| `BAIDU_REDIRECT_URI` | OAuth 回调地址 | 必须与百度开放平台完全一致 |
| `PERF_LOG` | 性能日志开关 | `1` 时启用 |
| `DERIVED_CACHE_DEBUG` | 派生缓存调试 | `1` 时启用 |
| `DERIVED_CACHE_LOG_THRESHOLD_MS` | 慢计算阈值 | 默认 120ms |

可从 `.env.example` 创建本地 `.env`，不要提交真实值。

### 5177 端口占用

PowerShell：

```powershell
Get-NetTCPConnection -LocalPort 5177 -ErrorAction SilentlyContinue
Get-Process -Id (Get-NetTCPConnection -LocalPort 5177).OwningProcess
```

确认是本轮启动的测试进程后再停止；不要盲目结束未知服务。也可临时设置 `PORT=5178`，但 Compose、Nginx 和 OAuth 回调仍必须使用各自真实入口。

## 6. 数据库

主库默认是 `data/liming-local.sqlite`。活动数据库可能同时存在 WAL 和 SHM，不能仅复制主库文件来宣称获得一致在线备份，也不能仅凭 WAL 非空断言其中一定有尚未合并的已提交页。

主要表按领域分为：

- 教务：`lessons`、`teachers`、`students`、`student_grade_stages`、`class_groups`。
- 费用：`pricing_standards`、`student_pricing`、`fee_overrides`、`recharge_records`、`student_opening_balances`。
- 薪资与运营：`teacher_salary_rules`、`teacher_adjustments`、`teacher_adjustments_monthly`、`teacher_travel_fees`、`staff`、`staff_salary_monthly`、`staff_attendance`、`operating_expenses`。
- 权限：`users`、`roles`、`role_permissions`、`user_page_permissions`、教师绑定和筛选预设表。
- 治理：`operation_logs`、数据检查表和 `backup_records`。

`npm run init` 和服务启动会执行幂等建表及兼容迁移。当前重要兼容规则包括：

- 期初余额已迁移为每名学生一条全局记录，表中不再保留 `month_key`；冲突记录必须先人工处理。
- `backup_records` 在旧表上按缺失列增量扩展，旧业务归档记录仍保留，不自动迁移、登记或清理。
- 角色权限和个人覆盖会校验白名单；历史老板/教务等角色名称由别名映射兼容。
- 调度设置保存在 `settings`，本地与百度频率、时间、重试、保留和操作日志选项相互独立。
- `recharge_records.channel` 与 `recharge_records.channel_other` 由幂等迁移补充；允许值为 `wechat`、`cash`、`alipay`、`other`，旧记录保持空值，不自动猜测或改写。
- `backup_records` 增加 `job_status`、安全错误码、任务时间和 worker PID 等异步任务列；不保存 Session、Token、App Secret 或远端 dlink。

旧 SQLite 运维脚本仍存在，适合停机维护或迁移演练：

```powershell
npm.cmd run db:backup
npm.cmd run db:export-json
npm.cmd run db:export-postgres
```

数据库恢复会覆盖主库，必须停服务、核对路径并保留恢复前副本，参见 [数据库迁移说明](docs/database-migration.md)。

## 7. 权限与账号

系统角色为老板、教务、小助手、助教、老师：

| 角色 | 默认范围 |
| --- | --- |
| 老板 | 全部页面和管理能力；兼容 `owner`、`boss`、`admin`、老板、管理员等历史值 |
| 教务 | 排课、学生、教师、经营和多数设置；默认不含数据中心和账号管理 |
| 小助手 | 日常课程、学生费用、充值和档案，兼容历史 `finance`；偏只读/有限写入 |
| 助教 | 课程、截图、学生和教师查询；偏只读 |
| 老师 | 课程、本人课时/教师信息及外观；可配合教师绑定限制数据范围 |

页面权限使用现有英文驼峰键，例如 `audit` 代表数据中心。权限来源依次考虑系统角色、`role_permissions` 和启用后的账号个人覆盖 `user_page_permissions`。前端隐藏入口只改善体验，真正授权由后端 API 再次校验。

数据中心由老板或具有 `audit` 显式页面权限的账号访问。下载远端完整备份限制老板。删除备份仍仅限原有高权限账号，但改为不输入密码和确认文字的普通二次确认；完整覆盖恢复、清空百度配置等高风险操作仍保留原有密码和确认文字。角色/账号关系异常会被数据完整性预检列出，不应通过删除账号规避。

## 8. 课程与费用口径

- 课程状态候选来自基础数据，默认包括待上、已上、请假、试课、考试、未缴费。是否收费和是否计教师薪资由共享领域规则决定，不应在页面、Excel 和报表中各写一套。
- 学生费用优先使用单节覆盖，其次学生单价规则，最后按年级和人数查费用标准。费用明细展示最终单人费用及课程上下文。
- 教师薪资优先保留明确的单节手动值，否则按教师薪资规则、课程时长及月度调整计算。缺少规则时经营数据会标记风险。
- 期初余额是全局起点，不是某月余额；月末计算结果不得回写成期初余额。
- 充值分实际充值和赠送充值。扣费按系统统一账本规则消耗余额，实际余额与赠送余额分开呈现。
- 跨月查询从全局期初余额开始，累计截至目标月份的有效充值和收费课程；请假、试课等状态按领域规则处理。
- 金额统一按人民币元存储和展示，禁止通过浮点格式化结果反向改写数据库原值。
- 首页课程日期、时间段和“当前时间”均按北京时间业务语义解释，不依赖 Node 进程或浏览器所在时区。时间区间采用开始含、结束不含规则，结束时刻立即移除；跨午夜课程归属于开课日期并延续到次日结束时刻。
- 薪资汇总和车费明细的教师候选都来自页面所选月份中确有课程记录的教师；档案在职、历史规则或其他月份车费不会单独生成候选。该月有课程的离职教师和薪资为 0 的教师仍显示，教师账号再按本人绑定范围收窄。

详细口径和审计背景见 [财务数据流审计](docs/financial-data-flow-audit.md)。

### 规则批量设置与页面布局

学生单价和教师薪资规则表的第一列都是显式复选框。表头全选只作用于当前筛选后的可见行，并正确显示全选、未选和不确定态；改变筛选会移除已隐藏行的选择，离开页面后重新进入会清空选择。点击普通表格行不会改变选择。批量弹窗分别调用 `PATCH /api/student-pricing/batch`（`{ ids, price }`）和 `PATCH /api/teacher-salary-rules/batch`（`{ ids, salary }`），金额范围为 0 到 100000 元且最多两位小数。

后端先去重并完整校验 ID、金额、条数、页面权限、只读状态和教师绑定范围，再在单一事务中只修改 `custom_price` 或 `salary_per_unit`。不存在的 ID 返回结构化失败且整批不写入；教师账号不能修改其他教师规则。成功响应包含 `processed`、`success`、`failed` 和最新 `rows`，页面只合并返回行并取消成功选择，不重新请求整份 bootstrap，因此当前筛选、月份和滚动位置保持不变。两类操作都写入安全的审计事件和操作日志，并触发规则、课时明细、首页、财务和汇总缓存失效。

薪资规则工具栏在 1440px 和 1280px 下将教师、搜索、价格状态、隐藏离职说明、筛选计数、选择计数、清空选择、批量设置、清筛和新增操作保持同一基线，控件高度差不超过 1px；390px 下按逻辑分组换行且不产生页面级横向滚动。首页桌面端不使用固定卡片高度：待办或人数统计的自然内容决定网格行高，三张卡底边一致；当前课程标题固定在卡内，只有课程列表滚动。移动端取消尺寸隔离，卡片按内容自然堆叠。

### 表格单行布局与充值渠道行内编辑

费用明细、充值记录、期初余额、学生单价和薪资规则统一由最外层 `.table-wrap` 承担横向滚动。普通单元格不设置独立滚动条、`resize` 手柄或省略号，也不通过缩小全局字号压缩内容；390px 下复选框仍在表格最前列可操作，页面 `body` 不产生横向滚动。费用明细的时间列仅在该页固定为 128px，保持水平、垂直居中及 `white-space: nowrap`，不影响课程总表、教师课时明细和截图页。

充值记录最终列顺序为“选择、学生姓名、年级、本月实际充值、本月赠送充值、充值日期、来源/渠道、备注”，不再渲染“操作”表头、编辑按钮或空操作单元格；新增充值弹窗、批量选择和批量删除保持原流程。来源/渠道单元格默认显示轻量标签，整个可写单元格支持鼠标、Enter 和 Space 打开浮层。浮层复用课程自定义选择器的圆角、阴影、边框、选项高度和 `.custom-select-menu` / `.custom-select-option` 样式，挂载到 `body`，并纳入统一浮层清理：再次点击、点击空白、Esc、页面切换和重新渲染都会关闭，DOM 同时最多保留一个渠道浮层，不使用原生 `select` 箭头。

微信、现金、支付宝分别保存 `wechat`、`cash`、`alipay`，选择后立即调用 `PATCH /api/recharges/:id/channel`，请求只包含 `{ channel, channel_other }`。保存期间显示轻量加载状态并禁用重复提交；成功后仅合并返回记录并更新当前单元格，不刷新 bootstrap 或整页，因此筛选、滚动位置和复选状态保持不变。选择普通渠道会清空 `channel_other`。选择“其他”时在同一浮层显示最长 100 字的必填说明、取消和保存；说明去除首尾空格，保存后显示“其他：具体说明”，已有说明会回填，取消不写数据。旧记录空渠道显示“未记录”，仍可直接选择。

专用渠道接口是必要的安全边界：旧的 `PATCH /api/recharges/:id` 继续保留给完整记录编辑兼容，但其实现会按整行字段处理，不适合作为行内最小写入接口。`PATCH /api/recharges/:id/channel` 按 ID 重新读取记录，只接受 `channel` 与 `channel_other`，拒绝额外字段，只更新这两列，并重新执行页面权限、角色写权限和只读校验；只读账号页面不生成可写控件，直接请求返回 403。审计与操作日志只记录记录 ID、变更字段和渠道前后值，不保存完整请求体。该接口不重新计算余额，因为渠道不参与账本金额口径。

期初余额表最小宽度为 1120px，备注列桌面目标宽度为 516px、硬下限为 360px，是表内最宽列；常见迁移备注在 1440px 和 1280px 验收视口完整单行显示，输入仍是单行 `input`，不恢复 `textarea`。学生单价和薪资规则共同调用只读展示函数 `studentSetInlineText(value)`：沿用现有分隔符解析，兼容数组、JSON 数组字符串、顿号、中英文逗号、中英文分号和换行，去除首尾空格、按首次出现顺序去重，以中文顿号连接，空集合显示“—”。该函数只改变渲染，不改数据库学生集合、规则规范化键或严格匹配逻辑。

### 教师课时明细与按规则更新薪资

“教师 → 课时明细”的教师候选只来自 `teachers` 教师档案表中状态严格等于“在职”的规范姓名，不再从当月课程、历史课程或薪资规则拼接候选。老板和教务在现有页面权限及角色预筛选范围内看到在职教师；教师账号只看到本人已绑定且仍在职的教师。此限制只作用于课时明细，不改变课程总表、薪资汇总、教师截图等页面的候选口径。

每次从其他页面进入课时明细，或刷新浏览器后直接进入该页，教师默认都未选择；不会恢复旧自动默认值、选择第一位或选择“全部教师”。页面显示“请先选择教师，再查看课时明细”，表格和选中行为空，全选及批量薪资按钮禁用，也不会发送无教师范围的课时明细查询。日期仍可调整。用户在当前页面主动选择教师后，日期/月切换和数据重载会保留该选择；离开页面再返回时重新清空。候选刷新后若当前教师已离职或不再有权查看，也会清空而不会自动跳到下一位。筛选组件支持搜索、键盘、Esc 和清空；没有候选时显示“暂无可选择的在职教师”。

“实际教师薪资”来自已持久化的 `lessons.teacher_salary`，参与薪资汇总；“规则薪资”来自服务端 `resolveTeacherSalaryRuleForLesson()` 的即时解析，不会先写回课程。解析结果通过 `rule_salary`、`rule_salary_per_2h`、`rule_id`、`rule_match_status`、`rule_match_reason`、`payroll_eligible` 和安全的 `rule_match_diagnostics` 返回，首次加载、局部刷新和批量更新使用同一字段与同一函数，浏览器不维护第二套计算口径。

规则必须与课程的规范教师姓名、年级、科目和学生集合完全相等；不会模糊匹配教师、把“高一”擅自映射为“新高一”，也不会合并“数学”和“高中数学”。学生集合兼容顿号、中英文逗号、中英文分号、换行、数组和历史 JSON 数组字符串；读取时去除姓名多余空格、去重、按稳定顺序生成集合键。顺序不同仍匹配，但不能用包含关系把不同集合视为相同。当前表没有规则日期列，所以空日期按不限时间处理；纯读取兼容函数对可选起止日期使用闭区间，边界当日有效，未到开始日和超过结束日分别报告“规则尚未生效”和“规则已经失效”，本轮不新增日期字段或迁移。

`salary_per_unit` 是每 `unit_hours` 小时金额，当前页面按每 2 小时维护。2 小时返回完整金额，1 小时和其他可识别时长按比例折算；0 元是有效规则金额并显示 `¥0.00`，字符串数字可安全转换。时间段无法识别、0 小时或规则课时口径无效时不再默认按一课时猜算，而是返回“无法计算”及安全原因。

规则启用状态兼容 `1/0`、布尔值、`启用/停用`、`在用/禁用` 和英文历史值。旧版同步候选以 `is_active=0` 创建，旧编辑器只保存金额而未同步激活状态；因此“状态为 0 且已有正金额”只在读取时按历史已配置候选兼容，不会改写数据库。0 金额候选仍视为未配置；新建、保存和显式启用写入 `1`，今后的显式停用写入 `-1`，从而不再混淆候选占位与用户停用。薪资规则页提供明确启用复选框。相同条件若有多条可用规则且没有既有优先级，则返回 `ambiguous`，不再静默选择最新一条。

页面明确展示五种结果：

- **匹配成功**：显示规则金额；如果课程状态不参与实际计薪，仍显示金额并附“当前状态不参与计薪”。
- **未匹配**：显示“未匹配”和教师、年级、科目或学生集合的安全原因。
- **规则不可用**：显示“规则不可用”以及停用、未生效、已失效、金额或课时口径无效等原因。
- **存在多条匹配规则**：显示歧义状态，必须先整理规则，系统不会自行选择。
- **无法计算**：显示课程时长等安全诊断，不显示空白、`undefined` 或 `NaN`。

非成功结果旁的“查看匹配详情”列出教师、年级、科目、学生集合、规则状态、规则日期、课程时长和课程计薪状态。详情不包含 SQL、数据库路径、堆栈、其他教师规则、学生电话、监护人或余额。规则是否匹配与课程是否参与实际计薪是两个独立维度：不参与计薪的课程可以有完整规则核对金额，但批量更新仍按现有业务口径跳过。

勾选课程后点击“按规则更新所选薪资”，请求只发送去重后的 `lesson_ids`，不接受前端指定的新薪资。服务端按 ID 重新读取并校验权限，在事务中逐条重新匹配规则，只更新 `teacher_salary`、`teacher_salary_source='auto'`、`teacher_salary_rule_id` 和更新时间，不修改规则表或课程其他业务字段：

- **更新**：课程参与教师计薪、存在可用规则，且实际薪资与最新规则薪资不同。
- **无需更新**：实际薪资与规则薪资按当前金额精度已经一致；不计为跳过。
- **跳过**：明确区分未匹配、规则停用/无效、规则歧义、无法计算和课程状态不参与计薪，原因与规则薪资列一致。
- **失败**：无效或不存在的 ID 会返回具体原因；越权 ID 在写入前返回 403。

返回结果包含所选、更新、无需更新、跳过、失败计数和逐课程结果。页面显示“已处理 N 节课”汇总，并可展开“查看详情”核对日期、教师、年级、科目、学生、处理结果和原因，不再只提示“全部跳过”。完成后局部重载当前教师和日期范围：更新与无需更新行取消勾选，跳过/失败行保留，日期和教师不丢失，滚动位置尽量恢复。所有写接口仍受后端页面权限、角色写权限和只读状态校验；只读账号可查看但返回 403，不能靠绕过按钮执行更新。

批量操作写入审计事件和操作日志，记录操作人、所选数、更新数、无需更新数、跳过数及失败数，不记录密码、Session、Cookie 或完整请求体。薪资规则新增、修改、启用、停用以及批量写入都会使课时范围、规则列表、首页、财务与薪资汇总相关的前端 GET 缓存和服务端派生缓存失效；课时明细范围请求还会主动跳过五分钟 GET 缓存。保存后下一次进入或局部刷新即可读取最新结果，无需重启服务或 `Ctrl+F5`；强制刷新只用于取得新版本静态 JS/CSS。

### 学生年级阶段冲突

`student_grade_stages` 中同一学生任意两条有效阶段记录，只要闭区间日期有交集，就属于年级阶段时间冲突。起止日期端点相同也算重叠；同一阶段重复、不同阶段交叉、一段完全包含另一段均按相同规则检查。普通阶段只有起止日期都有效时参与判断；空日期不据此推测范围；“已毕业”阶段从起始日延续到未来。系统不会为了通过预检而自动改日期、合并或删除记录，也不会自动修改学生当前年级。

进入“学生 → 学生档案”后，顶部“阶段冲突检查”区域始终显示，并明确区分四种状态：初次进入或重新请求时显示“阶段冲突：正在检查……”且按钮禁用；成功且结果为空时显示“阶段冲突：未发现冲突”和“重新检查”；存在冲突时显示“发现 N 名学生存在年级阶段时间冲突”、冲突组数、“查看冲突”和“重新检查”；接口失败时显示安全的“无权限查看”“接口不存在”“服务器处理失败”或“网络连接失败”以及“重试”，不会把服务端内部错误、SQL、路径、会话或 Token 渲染到页面。重新检查会跳过前端请求缓存，切换页面后再次进入学生档案也会重新查询。

顶部人数统计的是当前账号权限范围内的全部学生阶段数据，不受学生、年级、状态、当前月份、分页、当前滚动范围或“显示历史（含离校）”开关影响。老板账号统计包括在读、离校、已流出、暂停和已毕业在内的全部学生；教师账号仍只统计其绑定课程范围内可查看的学生。表格筛选只决定当前列表可见行，不会改变顶部总数。点击冲突列表中的历史学生时，页面会自动勾选“显示历史（含离校）”，在必要时清除阻止定位的姓名、年级和状态筛选，然后显示该学生、打开阶段编辑器、滚动并聚焦冲突阶段；系统不会自动修改日期。

有冲突时，表格姓名附近继续显示每名学生的冲突数量。点击“查看冲突”可查看学生、当前年级、两组阶段及日期、实际重叠范围和原因；点击“修改该学生”或表格标记会打开现有阶段编辑器并高亮冲突日期。学生档案表的复选框、姓名、当前年级、状态和日期列水平/垂直居中；姓名标签与“阶段冲突 N”标签按组合整体居中，长姓名自然换行；备注列保持左对齐，监护人和电话遵循当前统一表格规则。以上选择器仅作用于学生档案，不改变学生查询、课程总表、费用明细、教师档案或截图页面。

保存阶段时后端会再次运行同一套冲突判断，并以 `409` 和 `STUDENT_GRADE_STAGE_OVERLAP` 返回安全的结构化冲突详情，写入会整体取消，页面保留已填内容。旧库已有冲突仍可打开；只要修改确实减少冲突即可逐步修复。修复保存后页面重新查询，顶部提示和表格标记随结果消失。数据中心预检的冲突详情可“前往学生档案”定位修改，成功后返回数据中心并点击“重新检查”；预检通过前，完整备份不会执行。

## 9. 完整 Excel v4

完整 Excel v4 用于人工查看、空系统初始化和受控完整恢复。它不是 SQLite 文件副本，也不包含备份索引和可重建的审计状态。

### 22 张可见工作表

顺序是格式契约：

| # | 工作表 | 列顺序 |
| ---: | --- | --- |
| 1 | 导出说明 | 项目、内容 |
| 2 | 所有课程数据 | 授课老师、日期、星期、时间、教室、状态、年级、科目、学生、备注 |
| 3 | 所有学生费用明细 | 学生姓名、授课老师、日期、星期、时间、教室、状态、年级、科目、备注、单人费用 |
| 4 | 所有充值记录 | 月份、学生姓名、年级、本月实际充值、本月赠送充值、充值日期、来源/渠道、其他渠道说明、备注 |
| 5 | 期初余额 | 学生姓名、年级、期初实际余额、期初赠送余额、备注 |
| 6 | 所有学生单价 | 学生、年级、科目、学生集合、单价、价格状态、备注 |
| 7 | 所有班级管理 | 老师、年级、科目、学生集合、班级名 |
| 8 | 学生档案 | 姓名、当前年级、各学段日期、监护人、电话、状态、入学日期、离校日期、备注 |
| 9 | 所有教师车费明细 | 月份、教师、周次、开始日期、结束日期、金额、备注 |
| 10 | 所有教师课时明细 | 授课老师、日期、星期、时间、教室、状态、年级、科目、学生、备注、教师薪资 |
| 11 | 所有教师薪资规则 | 老师、年级、科目、学生集合、每2小时薪资、价格状态、备注 |
| 12 | 教师档案 | 姓名、电话、状态、入职日期、离职日期、备注 |
| 13 | 员工 | 姓名、角色、计薪、基础工资、日薪、标准天数、手机、状态、入职、离职、备注 |
| 14 | 所有员工薪资 | 月份、员工姓名、实际工资、奖金、扣款、备注 |
| 15 | 所有员工考勤 | 员工姓名、考勤日期、状态、计薪单位、工时、原因、备注 |
| 16 | 所有日常开销 | 日期、类别、金额、商家、备注 |
| 17 | 系统设置 | 设置项、设置值 |
| 18 | 基础数据 | 类别、名称、状态、排序 |
| 19 | 费用标准 | 年级、人数、单人费用、查找键、说明 |
| 20 | 操作日志 | 操作人、操作账号、操作类型、操作内容、结果、操作时间 |
| 21 | 角色管理 | 角色名称、角色说明、页面权限、操作权限、状态 |
| 22 | 账号管理 | 账号、姓名、角色、绑定教师、页面权限、账号状态 |

准确字段由 `src/excel/field_definitions.js` 唯一定义。学生档案中的各学段日期由 `STUDENT_GRADE_STAGE_COLUMNS` 展开，修改列名或顺序必须升级格式版本和测试。

### 4 张 veryHidden 恢复表

`__恢复元数据`、`__关系映射`、`__账号认证数据`、`__长文本分片` 保存可逆恢复所需的技术关系、账号密码哈希和超长文本分片。它们必须保持 `veryHidden`，摘要会覆盖可见表和内部表。普通人工编辑可能破坏摘要或关系，恢复前必须重新验证。

### 字段与安全规则

- 当前 `format_version=4`；旧版本不会被静默当作 v4 恢复。
- 日期使用 `YYYY-MM-DD`，内部月份键使用 `YYYY-MM-01`，充值可见月份使用中文年月。
- 金额单位为人民币元；空值、显式空字符串和长文本使用固定映射规则。
- 用户文本会防止 Excel 公式注入；工作簿不使用宏。
- 密码哈希仅在 veryHidden 认证表中用于完整账号恢复；不会导出 Session、Cookie、Token、App Secret、服务器绝对路径或 Docker/SSH 凭据。
- 操作日志为可选项：手动完整导出默认包含；服务器本地自动备份默认不包含；百度自动备份默认包含，可独立配置。
- XLSX ZIP 条目使用标准 Deflate（method 8）；金额只在实际数值单元格应用两位小数样式，不再给整列附加默认样式。恢复字段、金额精度、日期、隐藏表和摘要契约不因压缩改变。

### CLI

```powershell
# 导出完整 v4
npm.cmd run excel:full:export -- --db data/liming-local.sqlite --output data/backups/full-data.xlsx

# 验证
npm.cmd run excel:full:verify -- --input data/backups/full-data.xlsx

# 生成空白导入模板
npm.cmd run excel:template -- --output data/templates/黎明教育_全量数据导入模板_v4.xlsx

# 向空业务库初始化导入
npm.cmd run excel:import -- --db data/empty.sqlite --input data/templates/filled.xlsx --mode initialize

# 受控完整恢复（先阅读命令帮助并在临时数据库演练）
npm.cmd run excel:full:restore -- --db data/test-restore.sqlite --input data/backups/full-data.xlsx --confirm OVERWRITE
```

完整恢复必须先通过结构、版本、工作表摘要、关系映射和业务数据校验。初始化导入与覆盖恢复是不同模式；不得把普通月度 Excel 或手工改坏的文件用于覆盖恢复。

## 10. 数据中心

数据中心位于“设置 → 数据中心”，包括：

1. **数据完整性预检**：检查学生年级阶段冲突、角色关系、全局期初余额冲突、引用完整性和其他会阻止导出/恢复的问题；可查看安全摘要、CSV，并从阶段冲突直接前往学生档案修复。
2. **数据导入导出**：下载 v4、按需包含操作日志、下载模板、预览初始化导入或执行受控恢复。
3. **服务器本地备份**：手动创建、自动开关、02:30（默认，Asia/Shanghai）、保留数量和日志选项。
4. **百度网盘备份**：配置应用、OAuth 授权、连接测试、独立频率/时间/保留和远端目录。
5. **备份记录**：区分旧业务归档和 `full_data_excel`，分别显示本地状态、百度状态、校验、失败原因、创建账号和备注。“固定”不再占用独立表格列，固定/取消固定入口位于操作区。

本地受管备份存入 `data/backups/full-excel/`，同时生成 `.sha256`。创建前执行数据预检，发布后验证工作簿和摘要；学生阶段冲突等数据问题会先阻止备份，避免生成语义不一致的恢复文件。下载响应设置 `no-store`。固定记录不参与自动清理；删除会保护旧体系、忙碌记录和最后一个有效成功备份，并分别报告本地 Excel、本地 SHA-256、百度 Excel 和百度 SHA-256 的结果。失败记录不受“最后有效备份”保护。只有四个组件都已删除或本来不存在时，服务才执行 `DELETE FROM backup_records` 并返回 `{ ok: true, deleted: true, backup_id, cleanup }`；此后列表刷新不会再出现该行，任务详情返回 404。任一组件失败时返回 409、`deleted: false` 和逐组件状态，数据库行保留为明确的“删除未完成”状态供重试。

备份创建人由 `created_by_user_id` 关联账号后生成语义标签：手动备份优先显示账号姓名，其次账号名；自动触发统一显示“自动备份”；账号已删除或关系已丢失时显示“已删除账号”；没有创建人信息的旧记录显示“历史记录”。API 同时返回 `created_by_label` 和 `created_by_type`，页面不显示原始数字 ID，也不会为旧记录伪造账号。

备份状态按执行位置解释：本地失败表示完整 Excel 未成功创建；本地成功、百度失败表示服务器副本仍有效，仅远端副本失败；Excel 与 SHA-256 只有一个完成时显示“部分失败”，不会笼统标成整个备份失败。失败记录从现有 `message` 和 `remote_error_safe` 读取稳定错误代码、中文原因及安全详情；学生阶段冲突至少显示第一条学生/阶段摘要和问题总数。历史记录若只有错误代码，会使用通用中文映射并明确提示“旧记录未保存详细信息”，不会伪造姓名或日期。

删除备份不再要求输入老板密码或“删除备份”，但仍有包含文件名和存储位置的普通二次确认弹窗。权限没有扩大，只有原本可删除备份的账号能够调用接口；固定备份、受管目录边界、路径穿越、符号链接、忙碌状态和最后有效备份保护均保留。删除结果及备份 ID、文件名写入操作日志，不记录凭据或远端下载地址。完整覆盖恢复、清空高风险配置的密码和确认机制不受此变化影响。

页面加载失败时应显示安全错误和重试入口；百度未配置、授权未完成或备份目录不可用都应降级为状态提示，不能让整个数据中心白屏。

本轮没有数据库字段迁移，代码回滚应在停止服务并保留当前 SQLite 副本后，对本轮提交执行 `git revert`，再运行完整测试和数据预检。已经完成的单价/薪资金额更新属于业务写入，代码回滚不会自动恢复旧金额；已经物理删除的备份索引行及对应文件也无法靠 Git 恢复，只能从删除前另行保存且已验证的数据库/文件副本恢复。不要根据操作日志猜测并重建备份文件或业务金额。

### 百度手动备份的异步边界

`POST /api/data-center/baidu/backups` 只做权限、配置和数据预检，创建或复用唯一运行任务后立即返回 `202 Accepted`，响应包含 `backup_id`、`job_id` 和 `status`。前端提示“任务已开始”，通过 `GET /api/data-center/backups/:id/job` 轮询；重新进入数据中心也会从 `backup_records` 看到当前阶段和最终结果。重复点击复用现有任务，不创建第二份完整备份。

完整 Excel 导出、ZIP 压缩、工作簿验证、SHA-256、Excel/校验文件上传、远端回下载和完整性校验均由 `scripts/excel_backup/run_remote_manual_backup.js` 独立 Node 子进程执行。Web 主事件循环不再同步生成工作簿或等待百度网络，浏览器请求断开和页面关闭不会取消 worker，普通登录、课程和学生 API 不使用备份文件锁。任务仅在受管备份目录持有自身锁，不持有覆盖整个过程的 SQLite 写事务。

任务阶段为 `queued → preflight → exporting → hashing → uploading_excel → uploading_checksum → verifying_metadata → downloading_for_verification → integrity_check → success`，失败终态为 `partial_failed` 或 `failed`。worker 只接收运行所需路径、应用版本及百度应用配置，不接收内存 Session；记录只保存阶段和安全错误码。子进程异常退出由父进程标记失败，服务启动会检查遗留运行任务：PID 不再存活的任务标记 `BACKUP_WORKER_INTERRUPTED`，不会把半成品伪装成功。

旧实现的 504 根因不是 Nginx 超时太短，而是手动 HTTP 请求同步等待完整 Excel 生成、同步文件处理、百度双文件上传、回下载和校验；其中 CPU/文件同步工作还占用 Web 主 Node 进程，导致其他账号请求一起延迟。新接口不等待这些阶段，因此不依赖加长 `proxy_read_timeout`。后台失败只通过任务状态展示，避免“浏览器显示失败但稍后数据库又成功”的矛盾。

### 真实数据库副本的 XLSX 体积分析（仅数量，无业务值）

2026-07-24 使用在线库的只读 SQLite 一致快照在临时目录生成分析文件，未连接正式服务器、未写源库，也未输出姓名、电话、余额或金额。优化前含日志 XLSX 为 38,647,011 字节，ZIP 条目解压合计 38,643,183 字节；不含日志为 23,821,652 字节，解压合计 23,817,824 字节。ZIP 有 31 个条目、无图片/媒体、无重复工作表、`sharedStrings.xml` 不存在（使用 inline strings），`styles.xml` 仅 915 字节且只有 3 个 cellXfs，不存在异常样式数量或无限空白行/整列格式。使用区域内的空字符串仍按 v4 摘要语义保留，但没有写出使用区域之外的空白单元格。

最大条目是 veryHidden 的 `__关系映射`：46,848 行、8 列、363,189 个非空单元格，XML 30,932,207 字节，Deflate 后 2,318,051 字节；它保存完整关系恢复所需字段，不能删除。其后按解压大小排列的前 20 个工作表条目如下（解压字节 / 压缩字节）：

| 顺序 | 条目/工作表 | 解压 | 压缩 |
| ---: | --- | ---: | ---: |
| 1 | `sheet24.xml` / `__关系映射` | 30,932,207 | 2,318,051 |
| 2 | `sheet3.xml` / 所有学生费用明细 | 2,563,421 | 159,549 |
| 3 | `sheet20.xml` / 操作日志 | 2,046,410 | 139,940 |
| 4 | `sheet10.xml` / 所有教师课时明细 | 1,089,055 | 70,600 |
| 5 | `sheet2.xml` / 所有课程数据 | 1,046,445 | 64,361 |
| 6 | `sheet6.xml` / 所有学生单价 | 518,224 | 35,840 |
| 7 | `sheet8.xml` / 学生档案 | 204,630 | 11,319 |
| 8 | `sheet4.xml` / 所有充值记录 | 150,427 | 11,368 |
| 9 | `sheet11.xml` / 所有教师薪资规则 | 133,028 | 10,430 |
| 10 | `sheet7.xml` / 所有班级管理 | 131,756 | 10,193 |
| 11 | `sheet9.xml` / 所有教师车费明细 | 46,235 | 3,899 |
| 12 | `sheet18.xml` / 基础数据 | 28,428 | 2,997 |
| 13 | `sheet15.xml` / 所有员工考勤 | 22,569 | 2,037 |
| 14 | `sheet5.xml` / 期初余额 | 15,398 | 1,983 |
| 15 | `sheet12.xml` / 教师档案 | 11,932 | 1,410 |
| 16 | `sheet23.xml` / `__恢复元数据` | 10,679 | 2,440 |
| 17 | `sheet1.xml` / 导出说明 | 7,989 | 2,134 |
| 18 | `sheet14.xml` / 所有员工薪资 | 7,622 | 1,064 |
| 19 | `sheet19.xml` / 费用标准 | 7,108 | 1,082 |
| 20 | `sheet22.xml` / 账号管理 | 7,078 | 1,068 |

原写出器把全部 XML 以 ZIP method 0 原样存储，是 30 多 MB 的首要异常原因；操作日志还会同时进入可见日志表和完整恢复关系映射，是有恢复用途的内容而非无意义复制。旧文件中日志带来的增量为 14,825,359 字节，占含日志文件 38.36%。改为 Deflate 并取消整列金额样式后，含日志文件为 2,863,218 字节，减少 35,783,793 字节（92.59%）；不含日志为 1,765,877 字节，减少 22,055,775 字节（92.59%）。优化后日志增量为 1,097,341 字节。操作日志是否纳入仍由用户配置决定，完整恢复字段、隐藏表、摘要、金额和日期精度均未删除。

分析快照中各工作表的行数、列数、非空单元格数和使用区域由 `test:excel-size-analysis` 使用同一 ZIP/工作簿解析器统计；该测试还验证 Deflate、日志包含/排除、无整列金额样式、恢复到空合成库和再次导出。Microsoft Excel 人工打开仍属于发布前人工验收，不以自动解析替代。

## 11. 百度网盘备份

### 配置与授权

1. 在百度网盘开放平台创建应用，准备 App Key 和 App Secret。
2. 将数据中心显示的回调地址原样登记；正式环境必须是实际 HTTPS 地址。
3. 保存配置后发起 OAuth。回调路由是 `/api/data-center/baidu/callback`。
4. 回到页面时使用一次性 `?baidu=connected` 或失败标记展示结果，随后从地址栏清理参数。
5. 执行连接测试，通过后才允许启用远端自动备份，并确认“明文 Excel”风险。

App Secret 和 Token 存在 `data/backups/full-excel/.secrets/` 的受限 JSON 文件中，不回显。环境变量也可提供三项应用配置，但不要同时维护互相冲突的来源。

SQLite 中保存的是本地/百度备份的启用状态、频率、时间、保留数量、远端目录和明文风险确认等计划设置；百度 App Key、App Secret 与 OAuth Token 则保存在上述私密配置文件中。这是两套相互独立的数据。只复制 `liming-local.sqlite` 不会复制百度凭据，恢复或迁移数据库后必须在目标环境单独、安全地配置并授权百度网盘。

### 文件格式与下载链路

每次成功远端备份包含一对普通文件：

```text
黎明教育_全量数据_YYYYMMDD_HHMMSS.xlsx
黎明教育_全量数据_YYYYMMDD_HHMMSS.xlsx.sha256
```

Excel 是**明文**，SHA-256 只证明内容一致，不提供加密。上传完成后分别保存两个响应中的十进制 `fs_id`。下载链路必须是：

```text
上传返回 fs_id
→ filemetas?method=filemetas&fsids=[fs_id]&dlink=1
→ 取得 dlink
→ 携带 User-Agent: pan.baidu.com 跟随重定向下载
→ 下载配对 SHA-256
→ 校验摘要
→ 验证 Excel v4
```

`filemetas` 不使用 `path_list`。`fs_id` 可能超过 JavaScript 安全整数，代码在 JSON 解析前将其保留为十进制字符串，禁止转成 `Number`。正式下载使用记录中的 `remote_file_id` 与 `remote_checksum_file_id`；旧记录缺少 ID 时返回 `BAIDU_REMOTE_FILE_ID_MISSING`，不会猜测路径查询参数。

### 连接测试阶段

```text
authorization → connection → test_directory
→ file_upload → checksum_upload
→ file_metadata → checksum_metadata
→ file_download → checksum_download
→ integrity_check
→ test_delete_file → test_delete_checksum
```

测试只上传随机命名、无业务数据的文本和 SHA-256。核心测试与清理结果互相独立：核心失败仍尝试清理；核心通过但清理失败会给出警告；文件已不存在视为清理完成。

### 故障排查

| 阶段 | 常见错误 | 含义 | 处理 |
| --- | --- | --- | --- |
| `authorization` | 授权缺失/过期 | Token 不存在、已过期或刷新失败 | 重新连接百度网盘，核对回调地址 |
| `file_upload` / `checksum_upload` | API/分片失败 | 网络、目录、配额或百度服务异常 | 查看安全 provider_code，稍后重试 |
| `file_metadata` | `provider_code=2` | 常见为参数错误 | 确认运行版本使用 `fsids=[文件ID]`，没有 `path_list` |
| `checksum_metadata` | 列表为空或无 dlink | 校验文件 ID 不正确或文件已缺失 | 重新生成备份并核对双文件记录 |
| `file_download` | 403、31326、JSON 错误体 | dlink、权限、UA、有效期或服务端拒绝 | 确认 UA 含 `pan.baidu.com`，重新取得 dlink 后重试 |
| `checksum_download` | 下载失败 | SHA-256 文件不能读取 | 不得绕过校验；重新上传配对文件 |
| `integrity_check` | 摘要不一致 | 双文件不配对或内容变化 | 标记远端完整性失败，重新备份 |
| `cleanup` | 部分删除 | 核心结果不受覆盖，但测试文件仍在 | 按页面列出的安全相对路径人工核对 |

安全错误码包括 `BAIDU_FILE_METADATA_FAILED`、`BAIDU_CHECKSUM_METADATA_FAILED`、`BAIDU_DOWNLOAD_LINK_MISSING`、`BAIDU_FILE_DOWNLOAD_FAILED`、`BAIDU_CHECKSUM_DOWNLOAD_FAILED` 和 `BAIDU_REMOTE_SHA256_MISMATCH`。诊断只返回阶段、内部码、provider_code、HTTP 状态和清理状态，不返回 Token、授权码或完整 dlink。

旧 `.enc` 记录来自已下线的应用层加密方案，只保留兼容识别，不能当作当前明文 Excel 直接下载或恢复。

## 12. 自动备份与保留规则

调度器随 Node 服务启动，每分钟检查一次；它启动独立子进程执行到期任务，不使用 systemd timer。所有计划日期以 `Asia/Shanghai` 计算，数据库时间和导出 UTC 时间仍明确记录。

本地与百度调度互不依赖：

| 项目 | 本地默认 | 百度默认 |
| --- | --- | --- |
| 启用 | 否 | 否 |
| 时间 | 02:30 | 03:30 |
| 频率 | 每日 | 每周（也可手动/每日/每月） |
| 日志 | 不包含 | 包含 |
| 重试 | 3 次 | 3 次 |
| 保留 | 每日 14、月度 12、手动 20 | 最近 20 份 |

本地 `schedule_key` 是 `full-data:YYYY-MM-DD`；远端键包含 daily/weekly/monthly 和计划日期。成功键唯一，服务重启会补查但不会重复成功任务。失败重试按 10、30、120 分钟等待。

百度远端计划只有在应用配置完整、OAuth 授权有效且已确认明文备份风险时才会启动子进程。未就绪时状态分别为 `not_configured`、`not_authorized`、`plaintext_not_acknowledged`，调度器不会创建任务，并且相同安全提示只记录一次；配置或设置变化后，下一分钟检查会重新读取 SQLite 与私密文件，无需重启服务。子进程仍会在执行前重复校验，若调度检查后发生竞态并返回 `BAIDU_AUTOMATIC_BACKUP_NOT_READY`，调度器会冷却 10 分钟，避免每分钟重复启动和刷屏。本地自动备份不依赖这些百度状态。

首个成功日备份可晋升为月度保留意义而不复制文件。固定记录不自动删除；任何清理都只处理当前受管格式，跳过忙碌/无效/最后一个可用备份。远端保留按配对文件删除，部分失败会保留独立状态，不能伪装为全部成功。

## 13. 正式部署

正式配置由 `docker-compose.yml` 管理：

- `liming-course-app`：从本仓库构建，监听容器内 5177，以 `/app/data` 使用 `liming_data`。
- `liming-course-nginx`：`nginx:1.27-alpine`，反向代理 `app:5177`，发布 80/443，证书只读挂载自 `/etc/letsencrypt`。

首次部署：

```bash
cd /root/liming-course-system
cp .env.example .env
# 仅在安全编辑器中填写真实配置，不要把 .env 输出到聊天或日志
docker compose config --services
docker compose up -d --build
docker compose ps
docker compose logs --tail=100 app
docker compose exec -T app node -e "fetch('http://127.0.0.1:5177/api/version').then(r=>{if(!r.ok)throw Error(String(r.status));return r.text()}).then(console.log)"
```

更新前先完成下一章的保护步骤，然后：

```bash
cd /root/liming-course-system
git fetch origin --prune
git status --short
git rev-parse HEAD
git pull --ff-only origin main
docker compose build app
docker compose up -d app nginx
docker compose ps
curl -fsS https://www.limingedu.fun/api/version
```

不要在脏工作区直接拉取。`APP_GIT_COMMIT` 应在构建或启动配置中注入待部署提交。部署后用浏览器强制刷新（Windows：`Ctrl+F5`）并确认 `public/index.html` 的静态资源版本已变化，避免旧 CSS/JS 缓存制造假故障。

更完整的证书和部署说明见 [部署说明](docs/deployment.md)。

## 14. 正式部署前保护

每次迁移、重建或恢复前至少完成：

1. 记录 `git rev-parse HEAD`、Compose 工作目录、配置文件和 Volume 名称。
2. 停止写入或在受控方式下取得 SQLite 一致副本；若采用文件级冷备，主库、WAL、SHM 必须作为同一停机状态整体保存。
3. 备份整个 Docker Volume，并在隔离目录验证可读取；只把文件复制到同一磁盘不等于异地灾备。
4. 检查数据库与备份目标的容量、可用空间、设备和权限。
5. 在临时数据库副本执行初始化/迁移，核对表、角色、期初余额、课程、充值和备份记录。
6. 对将要恢复的 Excel 运行 v4 验证和临时库恢复演练。

禁止在未确认 Volume 的情况下执行：

```text
docker volume prune
docker compose down -v
```

也不要在生产 Compose project 上运行会创建测试容器或修改网络/Volume 的回归测试。

## 15. 恢复与回滚

### 代码回滚

代码回滚不应改写 `main` 历史：检出已验证提交或按提交边界执行 `git revert <commit>`，重新构建应用，再验证版本与核心页面。回滚代码不能自动回滚已经发生的数据库迁移。历史版本新增的充值渠道和备份任务列均为可空/有默认值的增量列，旧代码通常会忽略额外列，但正式回滚前仍应使用迁移前一致数据库副本演练，不能直接 `DROP COLUMN`。本轮表格与渠道行内编辑没有新增字段或迁移；回滚本轮代码可执行 `git revert <本轮提交>`，但本轮已经写入的渠道业务值不会随 Git 回滚恢复，必要时只能从已验证的一致数据库副本恢复并明确数据窗口。

### SQLite 回滚

必须先停应用写入，保留当前主库及 WAL/SHM，再从已验证的一致备份恢复。`npm run db:restore -- <backup>` 会覆盖主库且创建恢复前副本，只允许维护人员在核对绝对目标后执行。

### Excel 恢复

- 初始化导入：面向空系统，先预览再写入。
- 完整覆盖恢复：验证 v4、在临时数据库演练、确认备份和停机窗口，再执行覆盖。
- 手工可见表若被修改，隐藏关系和摘要可能不再匹配，应停止而不是强行跳过验证。

### 百度备份

远端下载必须同时取得 Excel 和 `.sha256`，按记录的两个 `fs_id` 下载，先验 SHA-256 再验 v4。缺少 ID、摘要不一致、旧 `.enc` 或下载失败都不能进入恢复。

数据库结构迁移完成后，旧代码可能无法读取新结构。正式回滚必须同时考虑代码、数据库副本和运行时版本，不能只切换 Git 提交。

## 16. 测试

当前 `package.json` 中的专项测试命令：

```powershell
npm.cmd run test:full-excel
npm.cmd run test:excel-import
npm.cmd run test:data-center
npm.cmd run test:data-preflight
npm.cmd run test:student-stage-conflicts
npm.cmd run test:teacher-detail
npm.cmd run test:backup-scheduler
npm.cmd run test:baidu-backup
npm.cmd run test:data-center-contract
npm.cmd run test:data-center-api
npm.cmd run test:data-center-page
npm.cmd run test:page-first-load
npm.cmd run test:opening-balances
npm.cmd run test:unified-filters
npm.cmd run test:dashboard-card
npm.cmd run test:multi-page-regressions
npm.cmd run test:backup-async
npm.cmd run test:excel-size-analysis
npm.cmd run test:batch-pricing-rules
npm.cmd run test:backup-record-list
npm.cmd run test:layout-regressions
npm.cmd run test:table-cell-layouts
npm.cmd run test:recharge-channel-inline
npm.cmd run test:student-balances
```

`test:teacher-detail` 当前至少有 59 个测试点，使用纯读取规范化单元测试、隔离合成 SQLite、临时 Node 服务和真实 Chromium/CDP，覆盖所有合法学生分隔符与 JSON、旧启用状态、空日期及闭区间边界、字段契约、规则金额 0/字符串金额、1/2/非标准/异常时长、不参与计薪但仍显示规则、未匹配、停用、无效、歧义、诊断详情、规则保存即时刷新、批量一致性、只读与越权 403、薪资汇总缓存、1440px/390px 和 Console。`test:batch-pricing-rules` 覆盖两类批量接口的去重、金额边界、500 条上限、事务回滚、字段不变、只读与教师范围 403，以及真实 Chromium 的全选、不确定态、弹窗、局部更新、1440/1280 对齐和 390px。`test:backup-record-list` 覆盖物理删行、四组件清理、部分失败保留、创建人标签、固定/最后有效/路径/忙碌保护、列表 DOM 删除和刷新不回归。`test:table-cell-layouts` 使用 1440/1280/1024/390 四视口验证费用时间边界、充值八列、期初备注实宽、学生集合单行行高、外层滚动和页面无横向溢出；`test:recharge-channel-inline` 覆盖专用接口字段白名单、其他说明、只读 403、安全日志、浮层生命周期、键盘、局部保存、选择和滚动保持。所有 API 测试使用临时服务和合成 SQLite；Excel 测试只用合成数据和临时目录，要求失败、跳过和未解释 Console 错误均为零。

本轮应用与浏览器静态资源版本为 `20260726-table-layout-recharge-channel`，同时写入 `/api/version` 默认值及 `public/index.html` 的 `app.js`、`styles.css` 查询参数。首次取得本轮静态文件时可使用一次 `Ctrl+F5` 并在网络面板确认版本；业务数据刷新不依赖强制刷新或服务重启。

500 名合成学生的相同机器对比：首次加载始终为认证、月份、完整 bootstrap 共 3 个主要请求，main 基线 247.4ms，优化后 119.3ms；单条保存由 PATCH 后重新认证/月份/bootstrap 的 4 个请求、259.6ms，降为单个 PATCH 和局部行更新、11.0ms。HTTP 请求数不随学生数线性增长，输入过程不发请求，失败会恢复原值并显示原因。该数据只用于相对回归，不承诺不同硬件的绝对耗时。

真实格式诊断和 Chromium 验收只操作一致 SQLite 副本或合成库；自动测试只操作临时合成库。本轮未连接或修改正式数据库，也未部署到正式服务器；只有在分支通过本地验收并按正式变更流程部署后才会生效。

提交前还应执行：

```powershell
node --check src/server.js
node --check src/backup/scheduler.js
node --check src/backup/baidu_provider.js
node --check src/backup/backup_service.js
node --check src/domain/beijing_time.js
node --check scripts/excel_backup/run_scheduled_backup.js
node --check scripts/excel_backup/run_remote_manual_backup.js
node --check public/app.js
git diff --check
```

Excel 自动解析通过不等于 Microsoft Excel 人工验收完成。涉及工作表结构、样式或兼容性时，还需在 Windows 版 Microsoft Excel 打开合成验收文件，确认无修复提示、列顺序正确、隐藏表状态正确、筛选和金额/日期格式正常。

余额专项维护命令：

```powershell
npm.cmd run audit:student-balances
npm.cmd run trace:student-balance
```

默认追踪姓名仅是开发脚本参数示例，正式排障不得把真实学生结果提交或公开。

## 17. 安全说明

- 百度网盘保存的是明文完整 Excel；SHA-256 是完整性校验，不是加密。不要公开分享文件或链接。
- App Secret 不回显；Token 和私密配置文件应为 0600，`.secrets` 和受管目录应为 0700。Windows 不具备相同 POSIX 位语义，仍需依赖账户 ACL。
- 普通 API/操作日志不得记录密码、Password Hash、Token、Cookie、Session、授权码、完整 dlink、App Secret 或服务器秘密配置。
- 下载响应应使用 `Cache-Control: no-store`，并由后端完成权限检查。
- Excel 本身包含学生、家长、教师、账号和财务敏感信息；隐藏工作表不构成保密措施。
- `.env`、真实数据库、源工作簿、上传、备份、证书、SSH/Docker 凭据均由 `.gitignore` 或部署边界保护，不得提交。
- Nginx 只代理应用，不应把 `/app/data` 或宿主机备份目录直接设为静态目录。
- 所有恢复、删除和权限变更都应保留可审计结果，并在隔离数据上先验收。

## 18. 常见问题

### 5177 端口被占用

先用系统命令定位进程，确认归属后再停止；或临时换端口。本地直连端口、Compose 内部端口和正式 Nginx 入口不要混淆。

### 首次进入页面没有数据

确认已运行 `npm run init`，查看浏览器网络请求和 Node 日志；新合成库本来可以为空。不要用正式数据库来验证首次加载。

### Excel 版本不兼容或提示损坏

确认文件是当前 v4，未被网盘预览器或办公软件改写，运行 `excel:full:verify`。若 Microsoft Excel 提示修复，文件不能用于恢复。

### 数据预检不通过

打开问题详情，按记录标识修正学生阶段、角色、引用、期初余额等源数据，再点击“重新检查”。学生阶段问题可直接前往学生档案定位冲突日期。不要删除整个旧库或绕过检查。

### 账号角色关系无效

在账号权限页将账号关联到有效系统角色；历史文本别名只用于兼容，不应继续生成新的非标准角色值。

### 期初余额迁移冲突

旧库同一学生可能有多个月份记录。必须人工判断唯一全局起点并解决冲突，不能把最近月末余额自动当期初余额。

### 百度授权失败

核对 App Key、App Secret、HTTPS 回调地址是否与开放平台逐字一致，以及服务器时间和 Token 状态。不要把 Secret 或 Token 粘贴到日志。

### 百度错误码 2

通常是参数错误。当前下载元信息接口必须发送 `method=filemetas`、`fsids=[正确十进制文件ID]` 和 `dlink=1`，不能发送 `path_list`。部署后先确认静态资源和服务端提交均已更新。

### 百度下载 403 或 31326

重新通过 `fs_id` 获取新 dlink，确认下载请求跟随重定向且 User-Agent 含 `pan.baidu.com`。不要缓存或记录完整 dlink。

### 测试文件已上传并删除，但测试仍失败

删除只说明清理完成，不代表核心测试成功。展开测试详情查看 `file_metadata`、`file_download`、`checksum_download` 或 `integrity_check`；核心结果和清理结果是两个维度。

### 浏览器仍显示旧样式或旧逻辑

先确认 `/api/version` 和部署提交，再强制刷新或清理该站点缓存。静态资源 URL 带版本参数，但代理/CDN/浏览器仍可能保留旧页面入口。

## 19. 更新记录

- **2026-07-26**：备份完整清理后物理删除记录并补齐创建人标签；期初余额改为单行；学生单价与教师薪资规则增加事务批量设置；班级学生集合取消内层滚动；首页三卡改为自然等高；补充 1440/1280/1024/390 真实 Chromium 回归。本轮无数据库字段迁移，未连接正式数据库或部署。
- **2026-07-24**：修复历史候选规则保存金额后仍被 `is_active=0` 过滤导致课时明细规则薪资为空的问题；统一旧状态和学生集合读取、规则/计薪双状态、歧义与时长诊断、即时缓存失效、明确页面状态和 59+ 项专项回归。本轮无数据库迁移，正式服务器尚未应用。
- **2026-07-23**：百度自动备份调度增加配置、授权与明文风险就绪检查，未就绪不再启动子进程；竞态 `NOT_READY` 增加冷却，页面补充中文计划状态，并明确 SQLite 计划设置与百度私密凭据相互独立。
- **2026-07-23**：教师课时明细改用在职教师档案候选并默认未选择；规则薪资展示与批量写入统一到服务端解析，补充结构化结果、权限、日志、缓存和 Chromium 专项回归。
- **2026-07-22**：百度下载改为使用上传返回的两个 `fs_id` 查询 `filemetas`；保留超大 ID 精度，补充 dlink User-Agent、分阶段诊断和失败清理回归；README 按当前系统重建。
- **2026-07-22**：数据中心完善本地/百度独立调度、操作日志选项、备份状态与权限；百度远端文件明确为明文 Excel + SHA-256。
- **2026-07-21**：完整 Excel 升级为 v4，统一可见字段、隐藏恢复映射、全局期初余额和导入/恢复边界。
- **2026-07-20**：数据中心页面加载、旧 `backup_records` 兼容、预检和完整 Excel 工作流完善。
- **2026-07-17**：排课编辑、日期/状态列顺序及页面局部刷新稳定化。
- **2026-07-13 至 2026-07-16**：统一筛选、课程冲突、学生选择、截图、导航折叠和操作日志持续优化。
- **2026-06**：统一学生扣费与赠送余额规则，完善角色权限、教师绑定、余额扫描、课程/财务页面性能。
- **2026-05**：Docker/Nginx 部署、HTTPS、源 Excel 同步审计、截图与课程工作流逐步形成。

更细的历史审计资料保留在 [docs](docs/) 和 Git 历史中。历史说明仅用于追溯；运行命令、端口、字段和安全边界以本 README 与当前代码为准。
